{
  "SPEC-001": {
    "spec_file": "specs/SPEC-001-spike-captura-audio-macos.md",
    "unit": [
      {
        "file": "tests/unit/spike-audio/SpikeAudioCapturePage.test.tsx",
        "test_count": 6,
        "description": "Página completa con servicios y bridge window.api mockeados: inicio de captura dual con medidores (AC-01), Mostrar en Finder (AC-09), Alerts destructive de permisos de micrófono y de audio del sistema (AC-10, AC-11), badges de permisos sin prompt (AC-12) y AlertDialog de cierre durante la captura (AC-14)."
      },
      {
        "file": "tests/unit/spike-audio/useAudioCapture.test.ts",
        "test_count": 2,
        "description": "Máquina de estados del hook de captura: detención por el usuario con resultado (duración + archivo) y callback onSaved (AC-04), y desconexión del dispositivo con parada controlada, conservación de lo grabado y error device-disconnected (AC-13)."
      },
      {
        "file": "tests/unit/spike-audio/CaptureSection.test.tsx",
        "test_count": 1,
        "description": "Cronómetro con formato mm:ss y zero-padding en varios instantes (AC-05)."
      },
      {
        "file": "tests/unit/spike-audio/ConfigSection.test.tsx",
        "test_count": 1,
        "description": "Selector de micrófono deshabilitado durante la captura con Tooltip explicativo (AC-15)."
      },
      {
        "file": "tests/unit/spike-audio/wavFileService.test.ts",
        "test_count": 1,
        "description": "Servicio Node de escritura WAV con fs real en directorio temporal: header RIFF (canales=2 en bytes 22-23, sample rate=16000 en 24-27, bits=16 en 34-35) y patch de tamaños en offsets 4 y 40 tras stop (AC-08)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "AC-01": "Componente acústica/física: que los medidores reflejen actividad REAL de micrófono y altavoces exige hardware de audio de macOS; la transición de estado y el renderizado de medidores están automatizados en unit.",
      "AC-02": "Requiere hablar al micrófono físico sin audio del sistema sonando y observar que solo se mueve el medidor de micrófono; no reproducible en jsdom.",
      "AC-03": "Requiere reproducir audio real por los altavoces sin hablar y observar que solo se mueve el medidor de sistema; no reproducible en jsdom.",
      "AC-06": "Distinguibilidad de las dos fuentes en el WAV (canal L=mic, R=sistema) exige una grabación real con señal en ambas fuentes e inspección/escucha del archivo.",
      "AC-07": "Audibilidad e inteligibilidad de ambas señales es una verificación perceptual: hay que reproducir la grabación y escucharla.",
      "AC-08": "Componente física: la verificación de que el audio real capturado queda a 16 kHz/16-bit se hace sobre una grabación real (p. ej. afinfo/ffprobe); la estructura del header y el patch de tamaños están automatizados en unit.",
      "AC-16": "Sesión sostenida de 15 minutos con continuidad de audio y memoria estable: requiere ejecución prolongada de la app real en macOS con monitorización del proceso."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/spike-audio/SpikeAudioCapturePage.test.tsx::SpikeAudioCapturePage::when both permissions are granted::starts capturing both sources and renders the two live level meters when the user clicks \"Iniciar captura\"",
      "AC-02": "MANUAL: requiere hablar al micrófono físico y observar que solo el medidor de micrófono muestra actividad; no reproducible en jsdom",
      "AC-03": "MANUAL: requiere reproducir audio real por los altavoces y observar que solo el medidor de sistema muestra actividad; no reproducible en jsdom",
      "AC-04": "tests/unit/spike-audio/useAudioCapture.test.ts::useAudioCapture::when the user stops an ongoing capture::finalizes the recording, exposes the result with total duration and file path, and notifies onSaved",
      "AC-05": "tests/unit/spike-audio/CaptureSection.test.tsx::CaptureSection::when the capture is running::shows the elapsed time chronometer formatted as mm:ss with zero padding",
      "AC-06": "MANUAL: distinguibilidad de las dos fuentes (L=mic, R=sistema) exige grabación real e inspección/escucha del archivo",
      "AC-07": "MANUAL: audibilidad e inteligibilidad de ambas señales es una verificación perceptual sobre una grabación real",
      "AC-08": "tests/unit/spike-audio/wavFileService.test.ts::wavFileService::when a recording is started, fed PCM chunks and stopped::writes a linear PCM 16-bit 16 kHz stereo RIFF header and patches the size fields at offsets 4 and 40 on stop",
      "AC-09": "tests/unit/spike-audio/SpikeAudioCapturePage.test.tsx::SpikeAudioCapturePage::when both permissions are granted::calls window.api.recording.showInFinder with the recording path when \"Mostrar en Finder\" is clicked",
      "AC-10": "tests/unit/spike-audio/SpikeAudioCapturePage.test.tsx::SpikeAudioCapturePage::when the microphone permission is denied::shows a destructive alert with the microphone settings instruction and does not start the capture",
      "AC-11": "tests/unit/spike-audio/SpikeAudioCapturePage.test.tsx::SpikeAudioCapturePage::when the system audio permission is denied::shows a destructive alert with the screen-and-system-audio settings instruction and does not start the capture",
      "AC-12": "tests/unit/spike-audio/SpikeAudioCapturePage.test.tsx::SpikeAudioCapturePage::when the page loads::shows the current state of each permission as a badge without triggering any permission prompt",
      "AC-13": "tests/unit/spike-audio/useAudioCapture.test.ts::useAudioCapture::when the selected input device disconnects during capture::stops in a controlled way, keeps what was recorded so far and reports a device-disconnected error without calling onSaved",
      "AC-14": "tests/unit/spike-audio/SpikeAudioCapturePage.test.tsx::SpikeAudioCapturePage::when both permissions are granted::shows the \"Detener captura\" AlertDialog when the window close is requested during capture, with cancel and stop-and-save actions",
      "AC-15": "tests/unit/spike-audio/ConfigSection.test.tsx::ConfigSection::when a capture is in progress (disabled)::disables the microphone selector and shows an explanatory tooltip on hover",
      "AC-16": "MANUAL: sesión sostenida de 15 minutos con continuidad de audio y memoria estable; requiere ejecución prolongada de la app real en macOS"
    },
    "notes": "SPEC-002 cambió el contrato (StopResult en recording.stop, api.transcription en el bridge) → helpers (tests/helpers/mockApi.ts) y 2 tests actualizados (SpikeAudioCapturePage.test.tsx y useAudioCapture.test.ts usan StopResult con transcriptPath: null). SPEC-003 amplió StopResult con latency → fixtures actualizadas con latency: null (regresión de contrato esperada, no bug).",
    "created_at": "2026-07-03",
    "updated_at": "2026-07-03"
  },
  "SPEC-002": {
    "spec_file": "specs/SPEC-002-spike-stt-streaming-deepgram.md",
    "unit": [
      {
        "file": "tests/unit/spike-transcription/useTranscription.test.ts",
        "test_count": 1,
        "description": "Hook useTranscription con el bridge window.api.transcription mockeado: parciales por canal, consolidación de finales en líneas y reset al iniciar (AC-01)."
      },
      {
        "file": "tests/unit/spike-transcription/TranscriptLine.test.tsx",
        "test_count": 1,
        "description": "Línea final con Badge de fuente (Micrófono outline / Sistema secondary) y timestamp mm:ss (AC-02)."
      },
      {
        "file": "tests/unit/spike-transcription/TranscriptionSection.test.tsx",
        "test_count": 3,
        "description": "Parcial en curso distinguible (italic + text-muted-foreground, asertado por clase porque jsdom no aplica estilos) (AC-03), Badge de estado con texto por estado Inactiva/Transcribiendo/Desconectado/Sin key (AC-04) y empty state 'Esperando audio…' solo cuando activa sin resultados (AC-09)."
      },
      {
        "file": "tests/unit/spike-transcription/ResultSection.test.tsx",
        "test_count": 1,
        "description": "Fila con la ruta del .transcript.json además del WAV, un solo botón Mostrar en Finder, y ausencia de la fila cuando transcriptPath es null (AC-06)."
      },
      {
        "file": "tests/unit/spike-transcription/SpikeAudioCapturePage.transcription.test.tsx",
        "test_count": 3,
        "description": "Página con servicios y bridge mockeados: sin key la captura sigue y aparece Alert informativo con .env.local/DEEPGRAM_API_KEY (AC-07), key inválida muestra Alert destructive 'clave inválida' con la captura en curso (AC-10), y toast 'Grabación y transcripción guardadas' cuando stop devuelve transcriptPath (mitad renderer de AC-05)."
      },
      {
        "file": "tests/unit/spike-transcription/transcriptionService.test.ts",
        "test_count": 3,
        "description": "Servicio Node de transcripción con DeepgramConnection mockeada (sin red): activación automática con key (connecting→active) (AC-08), cierre limpio CloseStream+flush y persistencia del .transcript.json junto al WAV con fs real (AC-05), y reintento único tras caída con conservación de las líneas recibidas (AC-11)."
      },
      {
        "file": "tests/unit/spike-transcription/deepgramService.test.ts",
        "test_count": 1,
        "description": "Capa de parseo main con el WebSocket global stubbeado: mensajes Results (parcial/final, channel_index) normalizados a eventos tipados; Metadata/JSON corrupto/binarios ignorados; auth por subprotocolo (refuerzo de AC-01/AC-02 en la capa main)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "AC-01": "Componente acústica: transcripción con voz real en vivo (hablar al micrófono y ver parciales/finales reales de Deepgram) requiere red, key válida y humano; la lógica de estados del hook y el parseo main están automatizados en unit.",
      "AC-02": "Componente acústica: voz real por los altavoces transcrita y etiquetada por canal requiere audio real y Deepgram real; el etiquetado UI por canal está automatizado en unit.",
      "AC-03": "Autoscroll del área de transcript: jsdom no hace layout (scrollHeight siempre 0), cualquier assert sería frágil → verificación visual manual; la distinción visual del parcial está automatizada en unit.",
      "AC-05": "Componente de red: el flush real de CloseStream contra Deepgram (últimos finales tras detener) requiere conexión real; la orquestación CloseStream→flush→persistencia y el toast están automatizados en unit.",
      "AC-11": "Componente de red: la pérdida de conexión real con reintento contra Deepgram requiere red real; la lógica de reintento único y conservación de líneas está automatizada en unit (Node).",
      "AC-12": "Persistencia observada end-to-end al cerrar la ventana y confirmar 'Detener y guardar': requiere app real (main+renderer). El camino unit stop→persistTranscript está cubierto por AC-05 y el AlertDialog por SPEC-001 AC-14.",
      "AC-13": "Persistencia observada end-to-end tras desconexión física del micrófono: requiere hardware real. Los caminos unit (parada controlada SPEC-001 AC-13 y persistencia de líneas AC-05/AC-11) están cubiertos."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/spike-transcription/useTranscription.test.ts::useTranscription::when partial and final results arrive from main::shows the in-progress partial per channel and consolidates it as a final line when the phrase ends",
      "AC-02": "tests/unit/spike-transcription/TranscriptLine.test.tsx::TranscriptLine::when rendering final lines from each channel::labels each final line with its source badge (Micrófono/Sistema) and an mm:ss timestamp",
      "AC-03": "tests/unit/spike-transcription/TranscriptionSection.test.tsx::TranscriptionSection::when results are arriving::renders the in-progress partial visually distinct (muted italic) from the final lines (autoscroll: MANUAL, jsdom sin layout)",
      "AC-04": "tests/unit/spike-transcription/TranscriptionSection.test.tsx::TranscriptionSection::when the connection state changes::shows a status badge whose text changes with the state, with \"Transcribiendo\" while connected",
      "AC-05": "tests/unit/spike-transcription/transcriptionService.test.ts::transcriptionService::when the user stops the capture::closes the Deepgram stream cleanly (CloseStream + flush) and persists the final lines as .transcript.json next to the WAV + tests/unit/spike-transcription/SpikeAudioCapturePage.transcription.test.tsx::SpikeAudioCapturePage (transcription)::when stopping a capture that produced a transcript::shows the \"Grabación y transcripción guardadas\" toast when stop returns a transcript path",
      "AC-06": "tests/unit/spike-transcription/ResultSection.test.tsx::ResultSection::when the recording finished with a transcript::shows the transcript file path in addition to the WAV path, sharing a single \"Mostrar en Finder\"",
      "AC-07": "tests/unit/spike-transcription/SpikeAudioCapturePage.transcription.test.tsx::SpikeAudioCapturePage (transcription)::when no Deepgram API key is configured::keeps the capture running and shows an informative (non-destructive) alert with the .env.local / DEEPGRAM_API_KEY instruction",
      "AC-08": "tests/unit/spike-transcription/transcriptionService.test.ts::transcriptionService::when the capture starts with an API key configured::activates the transcription automatically, opening the Deepgram connection with no extra steps",
      "AC-09": "tests/unit/spike-transcription/TranscriptionSection.test.tsx::TranscriptionSection::when the transcription is active but no speech has been detected yet::shows the \"Esperando audio…\" placeholder only while active and without results",
      "AC-10": "tests/unit/spike-transcription/SpikeAudioCapturePage.transcription.test.tsx::SpikeAudioCapturePage (transcription)::when the Deepgram API key is invalid::shows a destructive alert \"clave inválida\" while the audio capture continues without transcription",
      "AC-11": "tests/unit/spike-transcription/transcriptionService.test.ts::transcriptionService::when the connection drops while transcribing::retries once automatically and, if the retry fails, gives up keeping the lines already received",
      "AC-12": "MANUAL: persistencia end-to-end al cerrar la ventana confirmando 'Detener y guardar' requiere app real; caminos unit cubiertos por SPEC-002 AC-05 y SPEC-001 AC-14",
      "AC-13": "MANUAL: persistencia end-to-end tras desconexión física del micrófono requiere hardware real; caminos unit cubiertos por SPEC-001 AC-13 y SPEC-002 AC-05/AC-11"
    },
    "notes": "SPEC-003 cambió la firma de persistTranscript (PersistResult) y la forma del transcript.json (array plano → { lines, latency }) → adaptados los asserts de persistencia de transcriptionService.test.ts (AC-05, AC-11) y las fixtures StopResult de ResultSection.test.tsx y SpikeAudioCapturePage.transcription.test.tsx con latency: null (regresión de contrato esperada, no bug). SPEC-004 hizo requerido TranscriptLine.speaker y añadió speaker a DeepgramResult → fixtures/matchers actualizados con speaker: null en deepgramService.test.ts (además: assert de diarize=true en la URL), useTranscription.test.ts, TranscriptLine.test.tsx, TranscriptionSection.test.tsx y transcriptionService.test.ts (regresión de contrato esperada, no bug).",
    "created_at": "2026-07-03",
    "updated_at": "2026-07-03"
  },
  "SPEC-003": {
    "spec_file": "specs/SPEC-003-spike-medicion-latencia.md",
    "unit": [
      {
        "file": "tests/unit/spike-latency/ResultSection.latency.test.tsx",
        "test_count": 4,
        "description": "Fila 'Latencia STT' de ResultSection: métricas con formato es-ES 1 decimal y contador singular/plural (AC-01), Badge verde 'OK' en la frontera p95Ms=5000 (AC-02), Badge rojo 'Lenta' (data-variant destructive) con p95Ms=5001 —umbral sobre el crudo, no el redondeo— (AC-03), y ausencia de la fila con latency null (AC-05). Colores verificados por clase/variante y texto (lección jsdom)."
      },
      {
        "file": "tests/unit/spike-latency/transcriptionService.latency.test.ts",
        "test_count": 4,
        "description": "computeLatencyStats pura en Node: n=1 → p50=p95=max con count 1 (AC-07) + refuerzo del método nearest-rank (vacío→null, n par → elemento inferior central, datasets a ambos lados del umbral de 5 s). persistTranscript con DeepgramConnection mockeada y fs real: JSON { lines, latency } coherente con lo devuelto (=lo mostrado) y con computeLatencyStats sobre las líneas persistidas (AC-04), y agregación de los finales de ambos tramos de conexión tras caída+reintento (AC-06)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "veredicto-utilizable-en-directo": "El veredicto '¿es utilizable en directo?' es juicio humano sobre una sesión real con red y Deepgram reales (los números de latencia reales dependen del entorno); esta spec entrega las métricas, la decisión go/no-go de latencia es del humano (último ítem de H0). Todos los ACs verificables (7/7) están automatizados en unit."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/spike-latency/ResultSection.latency.test.tsx::ResultSection::when the session produced final transcription results::shows the \"Latencia STT\" row with median, p95, max in seconds (1 decimal, es-ES comma) and the result count",
      "AC-02": "tests/unit/spike-latency/ResultSection.latency.test.tsx::ResultSection::when the session produced final transcription results::shows a green \"OK\" badge when p95 is at or under the 5 s threshold",
      "AC-03": "tests/unit/spike-latency/ResultSection.latency.test.tsx::ResultSection::when the session produced final transcription results::shows a red \"Lenta\" badge when p95 exceeds 5 s (raw value, not the rounded display)",
      "AC-04": "tests/unit/spike-latency/transcriptionService.latency.test.ts::transcriptionService::when persisting a session with final results::writes a { lines, latency } JSON whose latency object matches both the returned stats and computeLatencyStats over the persisted lines",
      "AC-05": "tests/unit/spike-latency/ResultSection.latency.test.tsx::ResultSection::when the session had no final results::does not show the \"Latencia STT\" row when latency is null",
      "AC-06": "tests/unit/spike-latency/transcriptionService.latency.test.ts::transcriptionService::when the session had a connection drop and a successful retry::aggregates the final results of both connection stretches into a single latency dataset",
      "AC-07": "tests/unit/spike-latency/transcriptionService.latency.test.ts::computeLatencyStats::when the session had a single final result::returns that single delta as median, p95 and max with count 1"
    },
    "notes": "SPEC-004 hizo requerido TranscriptLine.speaker y añadió speaker a DeepgramResult → fixtures actualizadas con speaker: null en transcriptionService.latency.test.ts (builder lineWithDelta y llamadas onResult) (regresión de contrato esperada, no bug).",
    "created_at": "2026-07-03",
    "updated_at": "2026-07-03"
  },
  "SPEC-004": {
    "spec_file": "specs/SPEC-004-spike-diarizacion-deepgram.md",
    "unit": [
      {
        "file": "tests/unit/spike-diarization/deepgramService.diarization.test.ts",
        "test_count": 3,
        "description": "Extracción del hablante mayoritario en main (majoritySpeaker vía onResult, WebSocket stubbeado): un solo hablante → su índice, mayoría clara → el mayoritario, empate → primero en aparecer (AC-01 capa main); finales sin words o con words sin speaker → null (AC-04 capa main); interims con words diarizadas → null siempre (AC-06)."
      },
      {
        "file": "tests/unit/spike-diarization/TranscriptLine.diarization.test.tsx",
        "test_count": 2,
        "description": "Etiqueta UI de hablante: speaker 0 → texto muted 'Hablante 1' (speaker + 1) junto al Badge de fuente, sin ser un segundo Badge (AC-01 UI); speaker null → sin etiqueta, solo el Badge de fuente (AC-04 UI, Riesgo #9)."
      },
      {
        "file": "tests/unit/spike-diarization/TranscriptionSection.diarization.test.tsx",
        "test_count": 1,
        "description": "Líneas consecutivas de hablantes distintos por el mismo canal: cada fila con su propia etiqueta 'Hablante 1'/'Hablante 2' y su Badge de fuente, sin agrupar (AC-03)."
      },
      {
        "file": "tests/unit/spike-diarization/transcriptionService.diarization.test.ts",
        "test_count": 2,
        "description": "Persistencia del speaker en el transcript.json (DeepgramConnection mockeada, fs real): índice numérico persistido por línea (AC-02) y null explícito —la clave existe con valor null— cuando no hay dato (AC-04 persistencia)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "calidad-separacion-voces": "La calidad de la separación de voces con dos personas reales hablando por el micrófono es juicio humano sobre una sesión presencial simulada (alimenta el go/no-go de H0, Riesgo #9); requiere voces reales y Deepgram real. Nota de dominio: los índices de speaker se reinician por canal (con multichannel=true Deepgram diariza cada canal por separado), así que 'Hablante 1' del mic y 'Hablante 1' del sistema no son la misma persona."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/spike-diarization/deepgramService.diarization.test.ts::DeepgramConnection (diarización)::when a final result carries diarized words::assigns the majority speaker of the words, resolving ties in favor of the first speaker to appear + tests/unit/spike-diarization/TranscriptLine.diarization.test.tsx::TranscriptLine (diarización)::when the line has an identified speaker::renders the muted \"Hablante N\" label (speaker + 1) next to the source badge",
      "AC-02": "tests/unit/spike-diarization/transcriptionService.diarization.test.ts::transcriptionService (diarización)::when a final line has an identified speaker::persists the numeric speaker index in the transcript line",
      "AC-03": "tests/unit/spike-diarization/TranscriptionSection.diarization.test.tsx::TranscriptionSection (diarización)::when consecutive lines belong to different speakers on the same channel::renders each consecutive line with its own speaker label, without grouping them",
      "AC-04": "tests/unit/spike-diarization/deepgramService.diarization.test.ts::DeepgramConnection (diarización)::when the diarization provides no usable data::emits speaker null for finals without words or whose words carry no speaker index + tests/unit/spike-diarization/TranscriptLine.diarization.test.tsx::TranscriptLine (diarización)::when the line has no speaker information::renders no speaker label when speaker is null, keeping only the source badge + tests/unit/spike-diarization/transcriptionService.diarization.test.ts::transcriptionService (diarización)::when a final line has no speaker information::persists speaker as an explicit null in the transcript line",
      "AC-05": "COVERED BY SPEC-002/003: las suites existentes (transcript, latencia, persistencia, toast) corren íntegramente con speaker: null tras la adaptación de contrato, demostrando que sin datos de diarización el flujo es idéntico — ver SPEC-002 AC-01/AC-05/AC-11 y SPEC-003 AC-04/AC-06/AC-07",
      "AC-06": "tests/unit/spike-diarization/deepgramService.diarization.test.ts::DeepgramConnection (diarización)::when the result is an interim::emits speaker null for interim results even if they carry diarized words"
    },
    "created_at": "2026-07-03"
  },
  "SPEC-005": {
    "spec_file": "specs/SPEC-005-empaquetado-macos.md",
    "unit": [
      {
        "file": "tests/unit/packaging/packagingConfig.test.ts",
        "test_count": 4,
        "description": "Config de empaquetado leída como texto (sin dependencia YAML: segmentación simple + substrings): identidad Maurya en package.json/productName, <title> del renderer, title de la BrowserWindow e icns no vacío (AC-01 parcial); appId com.maurya.app estable + versión semver de package.json + setAppUserModelId (AC-02 parcial); script build:mac y targets dmg+zip arm64 con identity null/notarize false (AC-03 config); extendInfo como MAP (no lista) con NSMicrophoneUsageDescription y NSAudioCaptureUsageDescription en es-ES mencionando micrófono/audio del sistema/entrevista (AC-05 config)."
      },
      {
        "file": "tests/unit/packaging/env.test.ts",
        "test_count": 1,
        "description": "loadLocalEnv con electron mockeado y fs real: el candidato userData va PRIMERO (la app empaquetada lee la key desde el userData de Maurya) y una variable ya definida en process.env no se pisa (AC-09 parcial)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "AC-01": "Nombre 'Maurya' visible en menú de macOS, dock y título de la ventana real: requiere ejecutar la app (dev o empaquetada) y mirar; la identidad en config/código está automatizada.",
      "AC-02": "CFBundleIdentifier y CFBundleShortVersionString del bundle real: requiere generar el .app e inspeccionar su Info.plist; la config origen está automatizada.",
      "AC-03": "Ejecución real de npm run build:mac generando Maurya.app + dmg/zip arm64 sin errores: build real de minutos, la ejecuta el pipeline/humano, no vitest; la config está automatizada.",
      "AC-04": "Doble clic sobre Maurya.app y comprobación de que el harness funciona igual que en dev: requiere el bundle real y macOS.",
      "AC-05": "Presencia de las claves TCC en el Info.plist del bundle generado: requiere build real (defusa el riesgo conocido de extendInfo como lista); la forma MAP y los textos es-ES están automatizados sobre electron-builder.yml.",
      "AC-06": "Atribución del prompt de micrófono a 'Maurya' (no 'Electron'): prompt TCC real de macOS en primera ejecución empaquetada.",
      "AC-07": "Entradas 'Maurya' en los paneles de Privacidad (Micrófono y Grabación de pantalla y audio del sistema) de Ajustes del Sistema: inspección manual del SO.",
      "AC-08": "./start.sh sigue funcionando (dev no regresiona por la config de empaquetado): arranque real del entorno de desarrollo.",
      "AC-09": "Persistencia end-to-end de WAV + transcript.json en el userData de la app empaquetada: requiere captura real dentro de Maurya.app; el orden de candidatos de la key está automatizado."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/packaging/packagingConfig.test.ts::packaging config (SPEC-005)::application identity::names the app \"Maurya\" in package.json, window title, document title and ships a non-empty icns icon (componente visible real: MANUAL)",
      "AC-02": "tests/unit/packaging/packagingConfig.test.ts::packaging config (SPEC-005)::application identity::declares a stable com.<org>.<app> appId and takes the bundle version from package.json (bundle real: MANUAL)",
      "AC-03": "tests/unit/packaging/packagingConfig.test.ts::packaging config (SPEC-005)::macOS build targets::configures build:mac to produce dmg and zip artifacts for arm64 with ad-hoc signing (build real: MANUAL)",
      "AC-04": "MANUAL: doble clic sobre Maurya.app con el harness funcionando requiere el bundle real y macOS",
      "AC-05": "tests/unit/packaging/packagingConfig.test.ts::packaging config (SPEC-005)::TCC usage descriptions::declares NSMicrophoneUsageDescription and NSAudioCaptureUsageDescription as a MAP with Spanish texts (Info.plist del bundle real: MANUAL)",
      "AC-06": "MANUAL: el prompt TCC de micrófono atribuido a 'Maurya' solo se observa en la primera ejecución del bundle real",
      "AC-07": "MANUAL: las entradas 'Maurya' en los paneles de Privacidad de Ajustes del Sistema se inspeccionan a mano en macOS",
      "AC-08": "MANUAL: ./start.sh arrancando el entorno dev real (no regresión de la config de empaquetado)",
      "AC-09": "tests/unit/packaging/env.test.ts::loadLocalEnv::when .env.local exists both in userData and in the app path::reads the userData candidate first and never overwrites variables already defined in process.env (persistencia end-to-end empaquetada: MANUAL)"
    },
    "created_at": "2026-07-03"
  },
  "SPEC-006": {
    "spec_file": "specs/SPEC-006-persistencia-local.md",
    "unit": [
      {
        "file": "tests/unit/persistence/repository.test.ts",
        "test_count": 15,
        "description": "Repositorio de dominio contra el store JSON real en directorio temporal (initStore(baseDir) inyectable; electron mockeado con guard que lanza si algo usara app.getPath). CRUD happy path de las 7 entidades (AC-01..AC-07, incl. orden exacto de bloques/preguntas y updatedAt estrictamente creciente por comparación de strings ISO), validación de name vacío/espacios sin persistir —releyendo el archivo— (AC-09), reference con discoveryId inexistente (AC-10), cascada completa de deleteDiscovery con templates globales intactos (AC-11), SET NULL de templateId al borrar template (AC-12), nota borrada con su entrevista (AC-13), listDiscoveries [] en store recién creado (AC-14), dos escrituras encadenadas ambas en el archivo (AC-16) y nombre con emoji + 500 chars idéntico tras roundtrip de disco con re-init (AC-17)."
      },
      {
        "file": "tests/unit/persistence/store.test.ts",
        "test_count": 2,
        "description": "Store JSON: supervivencia a reinicios automatizada de verdad —re-init del store sobre el mismo baseDir y entidades intactas— (AC-08), y recuperación de corrupción: db.json con basura → initStore no lanza, getStatus expone initError.kind storage, el archivo dañado se conserva como db.json.corrupt-<timestamp> con su contenido original y el almacén nuevo vacío queda operativo (AC-15)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "bridge-ipc-real": "El registro de los canales db:* en ipcMain real, el envelope DbResult atravesando contextBridge/preload y db:get-status consultado desde el renderer solo son verificables con la app Electron real (no se monta un harness de ipcMain por decisión del pipeline); humo en app real ya realizado por el dev en la entrega. La lógica interior (repository/store, errores tipados, initError) está íntegramente automatizada, y la coherencia de tipos del contrato DbApi la garantiza el typecheck."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/persistence/repository.test.ts::repository::entity CRUD happy path::creates a discovery returning the entity with generated id and populated createdAt/updatedAt",
      "AC-02": "tests/unit/persistence/repository.test.ts::repository::entity CRUD happy path::creates a company under a discovery and lists it among the discovery companies",
      "AC-03": "tests/unit/persistence/repository.test.ts::repository::entity CRUD happy path::creates a contact associated to its company",
      "AC-04": "tests/unit/persistence/repository.test.ts::repository::entity CRUD happy path::persists an interview template with two blocks and ordered questions and reads back the exact order and content",
      "AC-05": "tests/unit/persistence/repository.test.ts::repository::entity CRUD happy path::creates an interview in draft status with correct contact and template references",
      "AC-06": "tests/unit/persistence/repository.test.ts::repository::entity CRUD happy path::persists a note template with context and sections and reads it back identical",
      "AC-07": "tests/unit/persistence/repository.test.ts::repository::entity CRUD happy path::updates a field changing updatedAt strictly forward and reading back the new value",
      "AC-08": "tests/unit/persistence/store.test.ts::store::when the app restarts over the same data directory::keeps every entity intact after re-initializing the store from disk",
      "AC-09": "tests/unit/persistence/repository.test.ts::repository::validation::rejects empty or whitespace-only names with a validation error without persisting anything",
      "AC-10": "tests/unit/persistence/repository.test.ts::repository::validation::rejects a company creation with a nonexistent discoveryId with a reference error without persisting anything",
      "AC-11": "tests/unit/persistence/repository.test.ts::repository::referential integrity and deletion::deletes a discovery cascading to its companies, contacts, interviews and notes while global templates survive",
      "AC-12": "tests/unit/persistence/repository.test.ts::repository::referential integrity and deletion::deletes a referenced interview template leaving the interview alive with templateId set to null",
      "AC-13": "tests/unit/persistence/repository.test.ts::repository::referential integrity and deletion::deletes an interview removing its note as well",
      "AC-14": "tests/unit/persistence/repository.test.ts::repository::empty state::lists discoveries as an empty array (not an error) on a freshly initialized store",
      "AC-15": "tests/unit/persistence/store.test.ts::store::when the data file is corrupt at startup::does not crash: reports a typed storage initError, keeps the damaged file as .corrupt-* and starts an empty working store",
      "AC-16": "tests/unit/persistence/repository.test.ts::repository::edge cases::persists both of two chained writes with no loss when re-reading the file",
      "AC-17": "tests/unit/persistence/repository.test.ts::repository::edge cases::recovers names with emoji, special characters and 500-char length identical after a disk round-trip"
    },
    "created_at": "2026-07-04",
    "notes": "SPEC-020 (schema v2) cambió CreateInterviewInput (discoveryId obligatorio, companyId opcional/null): las llamadas a createInterview de repository.test.ts pasan ahora discoveryId y el AC-05 aserta también interview.discoveryId (regresión de contrato presupuestada, no bug). La cobertura nueva del schema v2 (migración, invariantes, listAllInterviews, assignInterviewCompany) vive en tests/unit/persistence/repository.captures.test.ts bajo SPEC-020.",
    "updated_at": "2026-07-10"
  },
  "SPEC-007": {
    "spec_file": "specs/SPEC-007-settings-api-keys.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec: AC-01 engranaje→Ajustes, AC-02 Volver, AC-03 guardar (toast+badge+last4), AC-04 sin plaintext en disco, AC-05 persiste tras reinicio, AC-06 campo write-only vacío, AC-07 clave de Ajustes prevalece sobre env, AC-08 fallback env, AC-09 sin nada→no-key, AC-10 AlertDialog Eliminar clave, AC-11 confirmar eliminación, AC-12 validación inline, AC-13 cifrado no disponible, AC-14 sobrescritura directa, AC-15 skeletons.",
    "unit": [
      {
        "file": "tests/unit/settings/SettingsPage.test.tsx",
        "test_count": 7,
        "description": "Página de Ajustes en MemoryRouter con api.secrets mockeado: guardar con éxito → toast 'Clave de Deepgram guardada' + Badge 'Configurada' + '····abcd' + input limpiado (AC-03 UI), clave configurada representada con badge/last4 e input vacío type password (AC-06), AlertDialog 'Eliminar clave' con consecuencia y Cancelar/Eliminar sin tocar el almacén al cancelar (AC-10), confirmar Eliminar → remove('deepgram') + toast 'Clave eliminada' + 'No configurada' (AC-11 UI), campo vacío → inline 'Introduce una clave' sin llamar al bridge (AC-12), available=false → Alert destructive + ambos Guardar disabled + Tooltip vía span envolvente (AC-13 UI), y Skeletons en las filas mientras getStatus está pendiente (AC-15, ancla por tab tras SPEC-009). El test de 'Volver' (AC-02) se eliminó al derogarse el back button en SPEC-009."
      },
      {
        "file": "tests/unit/settings/secretsService.test.ts",
        "test_count": 6,
        "description": "secretsService en Node con safeStorage mockeado reversible (ENCv1+base64) e isEncryptionAvailable conmutable, fs real en temporal: save → configured con last4 correcto y el ARCHIVO sin el plaintext + roundtrip de descifrado (AC-03 main + AC-04), re-init sobre el mismo baseDir conserva estado y last4 (AC-05), cifrado no disponible → encryption-unavailable con archivo intacto (AC-13 main), remove elimina la entrada del almacén cifrado (AC-11 main), sobrescritura directa actualiza last4 y el valor descifrado sin dejar rastro de la anterior (AC-14), y corrupción de secrets.json → .corrupt-* + almacén nuevo operativo (robustez extra patrón SPEC-006, sin AC propio)."
      },
      {
        "file": "tests/unit/settings/getApiKey.test.ts",
        "test_count": 3,
        "description": "Resolución de la clave Deepgram vía su vía pública (startTranscription con DeepgramConnection mockeada y secretsService real): clave de Ajustes prevalece sobre DEEPGRAM_API_KEY (AC-07), fallback a env sin clave en Ajustes (AC-08), y sin clave en ningún sitio → estado no-key sin intento de conexión, flujo SPEC-002 (AC-09)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "persistencia-reinicio-app-real": "AC-05 end-to-end con la app completa (cerrar y reabrir Maurya y ver 'Configurada' con los mismos últimos 4): requiere la app real; el equivalente automatizado (re-init del almacén sobre el mismo baseDir) está en unit.",
      "keychain-real-macos": "El cifrado real con safeStorage respaldado por el Keychain de macOS (AC-04 con el SO real) solo se verifica en el equipo: los tests usan un safeStorage mockeado reversible por diseño.",
      "precedencia-con-captura-real": "AC-07/AC-08/AC-09 con una captura real contra Deepgram (la clave de Ajustes usándose de verdad en el stream): requiere red, key válida y sesión real; la lógica de resolución está automatizada en unit."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "DEROGADO por SPEC-009 (el engranaje del harness se eliminó; Ajustes se alcanza por el sidebar): cubierto por tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::sidebar::navigates on item click and marks the active item with background and font weight (not color alone) y tests/unit/layout/sections.test.tsx::secciones bajo el layout (SPEC-009)::Captura bajo el layout::renders the working capture harness without the settings gear button",
      "AC-02": "DEROGADO por SPEC-009 (el back button 'Volver' de Ajustes se eliminó, regla 2.3: la navegación la da el sidebar): cubierto por tests/unit/layout/sections.test.tsx::secciones bajo el layout (SPEC-009)::Ajustes bajo el layout::keeps the settings tabs functional and no longer shows the \"Volver\" back button",
      "AC-03": "tests/unit/settings/SettingsPage.test.tsx::SettingsPage::saving a key::shows the \"Clave de Deepgram guardada\" toast and the \"Configurada ····abcd\" status after a successful save + tests/unit/settings/secretsService.test.ts::secretsService::when a key is saved::reports configured status with the correct last4 and never writes the plaintext to disk",
      "AC-04": "tests/unit/settings/secretsService.test.ts::secretsService::when a key is saved::reports configured status with the correct last4 and never writes the plaintext to disk (Keychain real: MANUAL)",
      "AC-05": "tests/unit/settings/secretsService.test.ts::secretsService::when the app restarts over the same data directory::keeps the configured status with the same last4 after re-initializing from disk (app completa real: MANUAL)",
      "AC-06": "tests/unit/settings/SettingsPage.test.tsx::SettingsPage::saving a key::represents a configured key with badge and last4 while keeping the input empty (the key is never re-shown)",
      "AC-07": "tests/unit/settings/getApiKey.test.ts::transcriptionService (resolución de la clave Deepgram)::when a key is saved in Settings AND .env.local has one::opens the Deepgram connection with the Settings key (it takes precedence over the env fallback)",
      "AC-08": "tests/unit/settings/getApiKey.test.ts::transcriptionService (resolución de la clave Deepgram)::when there is no Settings key but .env.local has one::falls back to the DEEPGRAM_API_KEY from the environment (current dev flow)",
      "AC-09": "tests/unit/settings/getApiKey.test.ts::transcriptionService (resolución de la clave Deepgram)::when there is no key anywhere::keeps the SPEC-002 behavior: no-key status and no connection attempt",
      "AC-10": "tests/unit/settings/SettingsPage.test.tsx::SettingsPage::removing a key::opens the \"Eliminar clave\" AlertDialog explaining the consequence, with Cancelar and Eliminar actions",
      "AC-11": "tests/unit/settings/SettingsPage.test.tsx::SettingsPage::removing a key::confirming Eliminar calls remove, shows the \"Clave eliminada\" toast and flips the badge to \"No configurada\" + tests/unit/settings/secretsService.test.ts::secretsService::when a configured key is removed::removes the entry from the encrypted store on disk",
      "AC-12": "tests/unit/settings/SettingsPage.test.tsx::SettingsPage::saving a key::shows the inline \"Introduce una clave\" error without calling the bridge when the field is empty",
      "AC-13": "tests/unit/settings/SettingsPage.test.tsx::SettingsPage::when encryption is not available::shows a destructive alert and disables both Guardar buttons with an explanatory tooltip + tests/unit/settings/secretsService.test.ts::secretsService::when encryption is not available::fails saving with encryption-unavailable and leaves the store file untouched",
      "AC-14": "tests/unit/settings/secretsService.test.ts::secretsService::when a key is saved over an existing one::replaces the previous key directly, updating last4 and the decrypted value",
      "AC-15": "tests/unit/settings/SettingsPage.test.tsx::SettingsPage::while the status is loading::shows skeletons in the key status rows (no page spinner) while getStatus is pending"
    },
    "notes": "Adaptación de infra: tests/helpers/mockApi.ts amplía BridgeApi con secrets: SecretsApi (getStatus con default disponible-sin-claves; save/remove configurables por test) tras el cambio de contrato del preload (window.api.secrets). SPEC-009 derogó AC-01 (engranaje) y AC-02 (Volver): se eliminó tests/unit/settings/SpikeAudioCapturePage.settings.test.tsx y el test de Volver de SettingsPage.test.tsx; navegación remapeada a tests/unit/layout (regresión de contrato presupuestada, no bug). El ancla del test de skeletons pasó de heading 'Ajustes' a la tab 'Claves de IA' (SPEC-009 quitó el h1). SPEC-021 añadió la card 'Coste de IA' a Ajustes con su propio botón Guardar: el test de AC-13 pasa a esperar 3 botones Guardar y aserta deshabilitados SOLO los 2 de las filas de claves (el límite de coste no es un secreto y no depende de safeStorage); mockApi da default seguro a getAiCostSettings para que AiCostCard monte. Regresión de contrato presupuestada, no bug.",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-10"
  },
  "SPEC-008": {
    "spec_file": "specs/SPEC-008-editor-note-templates.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (21): AC-01 tabs con Claves de IA activa, AC-02 cambio de pestaña sin recarga, AC-03 fila nombre+N secciones+acciones, AC-04 empty state, AC-05 skeletons, AC-06 error state+Reintentar, AC-07 Nueva plantilla→editor con sección en blanco, AC-08 crear (toast Plantilla creada + vuelta al listado), AC-09 editar precarga en orden, AC-10 guardar edición (toast Cambios guardados), AC-11 añadir sección con foco, AC-12 subir intercambia + extremos disabled con Tooltip, AC-13 eliminar sección sin diálogo, AC-14 única sección disabled+tooltip literal, AC-15 nombre vacío, AC-16 título de sección vacío, AC-17 contexto opcional, AC-18 Volver con cambios→Descartar cambios, AC-19 Volver sin cambios directo, AC-20 AlertDialog Eliminar plantilla con «nombre», AC-21 confirmar eliminación.",
    "unit": [
      {
        "file": "tests/unit/note-templates/NoteTemplatesTab.test.tsx",
        "test_count": 9,
        "description": "SettingsPage con tabs + listado de plantillas, montado con las rutas reales en MemoryRouter y api.db/api.secrets mockeados: tabs con 'Claves de IA' activa por defecto y pestaña inactiva desmontada —listNoteTemplates no llamado— (AC-01), cambio de pestaña muestra el listado sin recargar y dispara la carga (AC-02), filas con nombre + '6 secciones'/'1 sección' + acciones Editar/Eliminar (AC-03), empty state con texto y CTA 'Crear primera plantilla' (AC-04), skeletons con list pendiente Promise<never> (AC-05), error state con mensaje y Reintentar que rellama al bridge (AC-06), 'Nueva plantilla' navega al editor vacío con una sección en blanco (AC-07), AlertDialog 'Eliminar plantilla' con «nombre» y Cancelar/Eliminar (AC-20) y confirmación → deleteNoteTemplate + toast 'Plantilla eliminada' + fila fuera (AC-21)."
      },
      {
        "file": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx",
        "test_count": 13,
        "description": "Editor en MemoryRouter con probe del listado en /settings: crear con dos secciones ordenadas → createNoteTemplate con {name,context,sections} en orden + toast 'Plantilla creada' + navegación (AC-08), precarga de nombre/contexto/secciones en su orden con getNoteTemplate (AC-09), edición del título → updateNoteTemplate + toast 'Cambios guardados' (AC-10), añadir sección al final con foco en su título —document.activeElement— (AC-11), AC-12 en DOS its con render propio cada uno (lección jsdom+Radix grace area): subir intercambia el orden + tooltip del primer 'Subir sección' deshabilitado, y tooltip del último 'Bajar sección' deshabilitado, eliminar sección sin diálogo (AC-13), única sección → botón disabled con tooltip literal 'La plantilla necesita al menos una sección' (AC-14), nombre vacío → 'Campo requerido' sin llamar al bridge (AC-15), título de sección vacío → ídem bajo ese título con el nombre válido (AC-16), contexto vacío guarda igualmente (AC-17), Volver con cambios → AlertDialog 'Descartar cambios' y Descartar navega (AC-18), y Volver sin cambios navega directo sin diálogo (AC-19)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "persistencia-real-reinicios": "La persistencia real de las plantillas entre reinicios de la app ya está automatizada a nivel de store por SPEC-006 (AC-08 de SPEC-006: re-init sobre el mismo baseDir con getInterviewTemplate/noteTemplates intactos); la verificación con la app completa reiniciada es humo manual.",
      "humo-visual": "Aspecto visual del Layout 4 (tabs), sticky bottom bar, iconos (FileText/AlertTriangle/Pencil/Trash2/ChevronUp/ChevronDown) y foco visible: revisión visual manual — jsdom no hace layout ni estilos."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/note-templates/NoteTemplatesTab.test.tsx::SettingsPage (tabs de Ajustes)::navigation::shows the \"Claves de IA\" and \"Plantillas de notas\" tabs with the API keys tab active by default",
      "AC-02": "tests/unit/note-templates/NoteTemplatesTab.test.tsx::SettingsPage (tabs de Ajustes)::navigation::shows the templates list without reloading the page when the tab is selected",
      "AC-03": "tests/unit/note-templates/NoteTemplatesTab.test.tsx::NoteTemplatesTab (listado)::listing templates::renders each row with name, section count and the Editar/Eliminar inline actions",
      "AC-04": "tests/unit/note-templates/NoteTemplatesTab.test.tsx::NoteTemplatesTab (listado)::listing templates::shows the empty state with its text and the \"Crear primera plantilla\" CTA when there are no templates",
      "AC-05": "tests/unit/note-templates/NoteTemplatesTab.test.tsx::NoteTemplatesTab (listado)::listing templates::shows skeletons in the list area while the templates are loading",
      "AC-06": "tests/unit/note-templates/NoteTemplatesTab.test.tsx::NoteTemplatesTab (listado)::listing templates::shows the error state with the message and a \"Reintentar\" button that reloads the list",
      "AC-07": "tests/unit/note-templates/NoteTemplatesTab.test.tsx::NoteTemplatesTab (listado)::creating from the list::navigates to the empty editor with one blank initial section when \"Nueva plantilla\" is clicked",
      "AC-08": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::creating a template::persists the new template with ordered sections, shows the \"Plantilla creada\" toast and returns to the list",
      "AC-09": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::editing a template::opens preloaded with the name, context and sections in their stored order",
      "AC-10": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::editing a template::persists the edited section title via updateNoteTemplate, shows \"Cambios guardados\" and returns to the list",
      "AC-11": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::managing sections::adds a blank section at the end and moves the focus to its title field",
      "AC-12": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::managing sections::moves a middle section up swapping with the previous one, and the first \"Subir sección\" is disabled with tooltip + tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::managing sections::disables the \"Bajar sección\" button of the last section with its tooltip",
      "AC-13": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::managing sections::removes a section without any confirmation dialog when there are two or more",
      "AC-14": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::managing sections::disables the remove button of the only section with the literal tooltip",
      "AC-15": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::validation::shows the inline \"Campo requerido\" error under the empty name and does not persist",
      "AC-16": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::validation::shows the inline \"Campo requerido\" error under an empty section title and does not persist",
      "AC-17": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::validation::saves normally with an empty context (the context is optional)",
      "AC-18": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::leaving the editor::shows the \"Descartar cambios\" AlertDialog when leaving with unsaved changes, and confirming discards and returns",
      "AC-19": "tests/unit/note-templates/NoteTemplateEditorPage.test.tsx::NoteTemplateEditorPage::leaving the editor::returns directly to the list without any dialog when there are no changes",
      "AC-20": "tests/unit/note-templates/NoteTemplatesTab.test.tsx::NoteTemplatesTab (listado)::deleting a template::opens the \"Eliminar plantilla\" AlertDialog naming the template, with Cancelar and Eliminar",
      "AC-21": "tests/unit/note-templates/NoteTemplatesTab.test.tsx::NoteTemplatesTab (listado)::deleting a template::removes the row and shows the \"Plantilla eliminada\" toast after confirming the deletion"
    },
    "notes": "Sin adaptaciones de infra: los defaults existentes de createMockDbApi (listNoteTemplates → ok []) bastaron; getNoteTemplate/create/update/delete se configuran por test. LECCIÓN (iteración QA 2026-07-04): jsdom + Radix grace area — TooltipContentHoverable calcula el grace area con getBoundingClientRect (rects 0,0,0,0 en jsdom) y tras el PRIMER unhover el flag compartido isPointerInTransit del TooltipProvider queda anclado en true, congelando cualquier tooltip posterior del mismo montaje → máximo 1 hover de tooltip por render; el it de AC-12 se dividió en dos tests con render propio. SPEC-009: el assert de no-recarga del AC-02 pasó del heading 'Ajustes' (h1 eliminado) a las tabs montadas (regresión de contrato presupuestada).",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-04"
  },
  "SPEC-009": {
    "spec_file": "specs/SPEC-009-layout-navegacion.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (14): AC-01 sidebar con 4 items + landmark, AC-02 click navega + item activo fondo+peso, AC-03 colapsar con persistencia localStorage, AC-04 expandir, AC-05 tooltip en colapsado, AC-06 top bar con título de sección, AC-07 index→Captura, AC-08 Discoveries empty, AC-09 hub Plantillas, AC-10 Ajustes sin Volver con tabs funcionales, AC-11 editor conserva Volver, AC-12 Captura sin engranaje, AC-13 idioma español, AC-14 404 con salida.",
    "unit": [
      {
        "file": "tests/unit/layout/Layout.test.tsx",
        "test_count": 9,
        "description": "Shell con la tabla de rutas real de App.tsx (actualizada por SPEC-020: index y /capture legado redirigen a /captures, CapturesPage como home, spike desenrutado) bajo <Layout/> en MemoryRouter (localStorage limpio por test): sidebar con los 4 links — 'Capturas' → /captures — y landmark 'Navegación principal' (AC-01), click navega y el activo lleva bg-accent + font-medium por clases (AC-02), colapsar → labels sr-only + marca 'M' + localStorage 'true' + remontaje arranca colapsado (AC-03), refuerzo de AC-03: primer arranque con innerWidth 800 → colapsado sin escribir localStorage, expandir → labels visibles + localStorage 'false' (AC-04), tooltip con el nombre de sección al hover en colapsado —máx 1 hover por render, lección Radix— (AC-05), top bar con el título por ruta clave: /captures 'Capturas', /discoveries 'Discoveries', /settings/note-templates/new 'Ajustes' (AC-06), index redirige a Capturas (AC-07) y ruta inexistente → 'Página no encontrada' con 'Ir a Captura' funcional que aterriza en /captures vía redirect (AC-14)."
      },
      {
        "file": "tests/unit/layout/sections.test.tsx",
        "test_count": 5,
        "description": "Secciones bajo el layout (misma réplica de rutas, actualizada por SPEC-020): Discoveries con empty state y CTA funcional (AC-08), hub de Plantillas con ambas cards navegando a su destino (AC-09), Ajustes sin back button 'Volver' con tabs funcionales (AC-10), editor de plantillas conservando su 'Volver' bajo el layout y volviendo al listado (AC-11), y la ruta legado /capture redirigiendo al listado global de Capturas — el AC-12 original (harness de spike) queda derogado por SPEC-020."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "humo-visual-shell": "Humo visual del shell: anchos reales del sidebar (240/64px), overflow y scroll del contenedor main, sticky bottom bar del editor bajo el nuevo scroll container, transición de colapso — jsdom no hace layout ni estilos; revisión visual manual."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::sidebar::shows the sidebar with the four section items — \"Capturas\" among them — and the \"Navegación principal\" landmark",
      "AC-02": "tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::sidebar::navigates on item click and marks the active item with background and font weight (not color alone)",
      "AC-03": "tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::sidebar::collapses to icons-only persisting the state in localStorage and across remounts + tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::sidebar::starts collapsed on first launch when the window is narrower than 1024px (refuerzo)",
      "AC-04": "tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::sidebar::expands back showing the labels when the expand button is clicked",
      "AC-05": "tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::sidebar::shows a tooltip with the section name when hovering an item of the collapsed sidebar",
      "AC-06": "tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::top bar::shows the active section title in the banner for each key route",
      "AC-07": "ACTUALIZADO por SPEC-020 AC-02 (el index redirige ahora a /captures): tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::routes::redirects the index route to the Capturas section (SPEC-020 home)",
      "AC-08": "DEROGADO PARCIALMENTE por SPEC-010 (el empty state provisional perdió el texto secundario y ganó CTA funcional): tests/unit/layout/sections.test.tsx::secciones bajo el layout (SPEC-009)::Discoveries::shows the Discoveries empty state with its functional CTA under the layout + cobertura completa del empty state en SPEC-010 AC-03",
      "AC-09": "DEROGADO por SPEC-012 AC-01 (la card de entrevistas pasó a ser clicable → /templates/interview con la descripción 'Cuestionarios base para tus entrevistas'): tests/unit/layout/sections.test.tsx::secciones bajo el layout (SPEC-009)::Plantillas (hub)::shows both template cards, each navigating to its destination",
      "AC-10": "tests/unit/layout/sections.test.tsx::secciones bajo el layout (SPEC-009)::Ajustes bajo el layout::keeps the settings tabs functional and no longer shows the \"Volver\" back button",
      "AC-11": "tests/unit/layout/sections.test.tsx::secciones bajo el layout (SPEC-009)::editor de plantillas bajo el layout::keeps the \"Volver\" back button in the note-template editor (detail flow) under the layout",
      "AC-12": "DEROGADO por SPEC-020 (el harness de spike /capture deja de estar enrutado; su lugar lo ocupa el contrato nuevo): tests/unit/layout/sections.test.tsx::secciones bajo el layout (SPEC-009)::Capturas bajo el layout::redirects the legacy /capture route to the global captures list under the layout",
      "AC-13": "REFERENCIA CRUZADA: cubierto implícitamente por los literales españoles asertados en todos los tests del shell y secciones (sidebar 'Discoveries/Plantillas/Capturas/Ajustes', top bar, 'Aún no hay discoveries', 'Página no encontrada', 'Ir a Captura', tooltips y aria-labels en español); sin test propio.",
      "AC-14": "tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::routes::shows the Spanish 404 page for unknown routes with a working \"Ir a Captura\" link"
    },
    "notes": "SPEC-010 derogó parcialmente AC-08 (empty state de Discoveries): el test de sections.test.tsx se reescribió con findBy* (la página ahora carga del bridge), sin el texto secundario provisional y asertando el CTA 'Crear primer discovery' (regresión de contrato presupuestada, no bug). SPEC-020 reescribió la réplica de rutas y las aserciones de Layout.test.tsx/sections.test.tsx al contrato nuevo ('Capturas' → /captures como home; /capture e index redirigen; SpikeAudioCapturePage desenrutada — los mocks de servicios del spike ya no son necesarios en estos tests). Regresión de contrato presupuestada, no bug.",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-10"
  },
  "SPEC-010": {
    "spec_file": "specs/SPEC-010-gestion-discoveries.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (18): AC-01 filas nombre+fecha+Acciones, AC-02 orden updatedAt desc, AC-03 empty con CTA, AC-04 skeletons, AC-05 error+Reintentar, AC-06 dialog Nuevo discovery con foco, AC-07 crear (click y Enter), AC-08 vacío→Campo requerido, AC-09 Cancelar/Escape, AC-10 renombrar precargado y seleccionado, AC-11 guardar renombrado, AC-12 renombrar vacío, AC-13 AlertDialog cascada con «nombre», AC-14 confirmar eliminación, AC-15 detalle con h1+empty empresas, AC-16 id inválido, AC-17 Volver, AC-18 error de mutación.",
    "unit": [
      {
        "file": "tests/unit/discoveries/DiscoveriesPage.test.tsx",
        "test_count": 16,
        "description": "Sección Discoveries con api.db mockeado y rutas reales (listado+detalle) en MemoryRouter: fila con nombre-link (href al detalle), 'Creado el <fecha es-ES>' calculada con el mismo toLocaleDateString y botón 'Acciones' (AC-01); orden updatedAt desc con fixture desordenado (AC-02); empty con CTA (AC-03); skeletons con Promise<never> (AC-04); error+Reintentar rellama (AC-05); dialog 'Nuevo discovery' con placeholder y foco en Nombre (AC-06); creación con click en Crear —bridge {name}, toast 'Discovery creado', dialog cerrado, fila arriba— y con Enter en dos its (AC-07); vacío/espacios → 'Campo requerido' sin bridge (AC-08); Cancelar y Escape cierran sin crear (AC-09); renombrar precargado y con el texto seleccionado (activeElement + selectionStart/End) (AC-10); Guardar → updateDiscovery + toast + listado actualizado (AC-11); renombrar vacío → error sin bridge (AC-12); AlertDialog 'Eliminar discovery' con la cascada literal interpolando «nombre» (AC-13); confirmar → deleteDiscovery + toast + fila fuera (AC-14); y create {ok:false} → toast de error con el message, dialog abierto y listado intacto (AC-18). Dialogs de fila abiertos vía menú ⋯ con findBy* (setTimeout(0) del dev)."
      },
      {
        "file": "tests/unit/discoveries/DiscoveryDetailPage.test.tsx",
        "test_count": 3,
        "description": "Detalle mínimo (resuelto con listDiscoveries+find): click en el nombre del listado → /discoveries/:id con h1 del discovery, heading 'Empresas' y empty 'Aún no hay empresas' + secundario (AC-15); id inválido → 'Discovery no encontrado' con link 'Volver a Discoveries' funcional (AC-16); y back button 'Volver' que regresa al listado (AC-17)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "humo-visual": "Aspecto visual del listado (hover subrayado del nombre, separadores, iconos del menú ⋯, alineación del DropdownMenu) y del detalle: revisión visual manual — jsdom no hace layout ni estilos."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::listing::renders each row with the clickable name, its creation date and the actions menu button",
      "AC-02": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::listing::orders the discoveries by updatedAt descending (most recent first)",
      "AC-03": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::listing::shows the empty state with the \"Crear primer discovery\" CTA when there are none",
      "AC-04": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::listing::shows skeletons while the list is loading",
      "AC-05": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::listing::shows the error state with the message and a \"Reintentar\" button that reloads",
      "AC-06": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::creation::opens the \"Nuevo discovery\" dialog with the focus on the Nombre field",
      "AC-07": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::creation::creates on \"Crear\" click, closes the dialog, shows the toast and lists the new discovery on top + tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::creation::creates on Enter as well, closing the dialog",
      "AC-08": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::creation::shows the inline \"Campo requerido\" error for empty or whitespace-only names without calling the bridge",
      "AC-09": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::creation::closes the dialog without creating anything on Cancelar and on Escape",
      "AC-10": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::renaming::opens the \"Renombrar discovery\" dialog with the current name preloaded and selected",
      "AC-11": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::renaming::renames on \"Guardar\", shows the toast and updates the list",
      "AC-12": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::renaming::shows the inline \"Campo requerido\" error when renaming to empty without calling the bridge",
      "AC-13": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::deletion::opens the \"Eliminar discovery\" AlertDialog warning about the full cascade with the name",
      "AC-14": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::deletion::removes the row and shows the \"Discovery eliminado\" toast after confirming",
      "AC-15": "DEROGADO PARCIALMENTE por SPEC-011 AC-02 (el empty state de empresas perdió el secundario provisional y ganó el CTA 'Añadir primera empresa'): tests/unit/discoveries/DiscoveryDetailPage.test.tsx::DiscoveryDetailPage::navigating from the list::opens /discoveries/:id from the row name showing the title and the companies empty state (adaptado)",
      "AC-16": "tests/unit/discoveries/DiscoveryDetailPage.test.tsx::DiscoveryDetailPage::invalid id::shows the error state with the \"Volver a Discoveries\" link for a nonexistent id",
      "AC-17": "tests/unit/discoveries/DiscoveryDetailPage.test.tsx::DiscoveryDetailPage::back button::returns to the list when the \"Volver\" back button is clicked",
      "AC-18": "tests/unit/discoveries/DiscoveriesPage.test.tsx::DiscoveriesPage::mutation errors::shows an error toast, keeps the dialog open and leaves the list untouched when the bridge fails"
    },
    "notes": "SPEC-011 derogó parcialmente AC-15 (empty state de empresas del detalle): el test se adaptó con findBy* (la sección ahora carga del bridge), sin el secundario provisional y asertando el CTA 'Añadir primera empresa' (regresión de contrato presupuestada, no bug).",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-04"
  },
  "SPEC-011": {
    "spec_file": "specs/SPEC-011-empresas-contactos.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (20): AC-01 fila de empresa con link+iconos condicionales+Acciones, AC-02 empty empresas con CTA, AC-03 dialog Nueva empresa con foco y 3 campos, AC-04 crear empresa normalizando opcionales, AC-05 nombre vacío, AC-06 editar empresa precargada, AC-07 eliminar empresa con cascada, AC-08 nombre navega al detalle, AC-09 detalle con Volver+h1+enlaces hostname, AC-10 fila de contacto, AC-11 empty contactos, AC-12 dialog Nuevo contacto, AC-13 crear contacto, AC-14 nombre vacío contacto, AC-15 editar contacto, AC-16 eliminar contacto, AC-17 empresa inexistente, AC-18 enlaces externos al navegador del sistema, AC-19 skeletons, AC-20 error de mutación.",
    "unit": [
      {
        "file": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx",
        "test_count": 9,
        "description": "Sección Empresas del detalle de discovery (api.db mockeado, rutas reales): fila con nombre-Link href al detalle e iconos condicionales —con website y sin LinkedIn solo 'Abrir website'— + Acciones (AC-01); dialog 'Nueva empresa' con foco en Nombre sin selección y placeholders de URL (AC-03); crear → createCompany {discoveryId,name,website recortado,linkedinUrl:null} asertando la normalización + toast 'Empresa creada' + fila (AC-04); vacío → 'Campo requerido' sin bridge (AC-05); editar precargada (null→'') → updateCompany con los 3 campos + toast 'Cambios guardados' + fila actualizada (AC-06); AlertDialog 'Eliminar empresa' con la cascada literal «Acme Corp» y confirmación → deleteCompany + toast 'Empresa eliminada' (AC-07); click en el nombre → detalle de empresa (AC-08); skeletons con listCompanies pendiente (AC-19); y createCompany {ok:false} → toast de error con dialog abierto y listado intacto (AC-20). AC-02 (empty + CTA) se cubre con el test adaptado de SPEC-010 AC-15 (sin duplicar)."
      },
      {
        "file": "tests/unit/companies/CompanyDetailPage.test.tsx",
        "test_count": 10,
        "description": "Detalle de empresa y contactos (getCompany mockeado por test — sin default en el helper): Volver al detalle del discovery + h1 + enlaces con hostname visible acme.example/linkedin.com (AC-09); fila de contacto con nombre, posición muted, icono LinkedIn con href y Acciones (AC-10); empty 'Aún no hay contactos' + CTA (AC-11); dialog 'Nuevo contacto' con foco y placeholders (AC-12); crear → createContact {companyId,name,position,linkedinUrl:null} + toast 'Contacto creado' (AC-13); vacío → 'Campo requerido' sin bridge (AC-14); editar precargado → updateContact + toast 'Cambios guardados' (AC-15); AlertDialog simple 'Eliminar contacto' «Jane Doe» → deleteContact + toast 'Contacto eliminado' (AC-16); getCompany not-found → error + link 'Volver a Discoveries' (AC-17); y markup de enlaces externos a[target=_blank][rel=noreferrer] con href exacto (AC-18 unit)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "apertura-navegador-sistema": "AC-18 end-to-end: que el click abra la URL en el navegador por defecto (setWindowOpenHandler → shell.openExternal en main) solo se verifica con la app Electron real; jsdom no ejecuta el handler de main. El markup del enlace (target/rel/href) está automatizado en unit.",
      "humo-visual": "Aspecto visual de filas, iconos Globe/LinkedIn (glifo recreado con createLucideIcon), hover y alineación: revisión visual manual — jsdom no hace layout ni estilos."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx::DiscoveryDetailPage (empresas)::company rows::renders each company row with the name link, conditional external icons and the actions menu",
      "AC-02": "tests/unit/discoveries/DiscoveryDetailPage.test.tsx::DiscoveryDetailPage::navigating from the list::opens /discoveries/:id from the row name showing the title and the companies empty state (test de SPEC-010 AC-15 adaptado: 'Aún no hay empresas' + CTA 'Añadir primera empresa')",
      "AC-03": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx::DiscoveryDetailPage (empresas)::creating a company::opens the \"Nueva empresa\" dialog with focus on Nombre and the optional URL fields",
      "AC-04": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx::DiscoveryDetailPage (empresas)::creating a company::creates via createCompany normalizing empty optionals to null, shows the toast and lists the row",
      "AC-05": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx::DiscoveryDetailPage (empresas)::creating a company::shows the inline \"Campo requerido\" error for an empty name without calling the bridge",
      "AC-06": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx::DiscoveryDetailPage (empresas)::editing a company::opens the edit dialog preloaded (null → empty), saves via updateCompany and shows \"Cambios guardados\"",
      "AC-07": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx::DiscoveryDetailPage (empresas)::deleting a company::confirms in the cascade AlertDialog, deletes via deleteCompany and shows \"Empresa eliminada\"",
      "AC-08": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx::DiscoveryDetailPage (empresas)::navigating to the company detail::navigates to the company detail when the row name is clicked",
      "AC-09": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::header::shows the back button, the company title and its external links with visible hostnames",
      "AC-10": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::contact rows::renders each contact row with name, muted position, LinkedIn icon link and actions menu",
      "AC-11": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::contact rows::shows the contacts empty state with the \"Añadir primer contacto\" CTA",
      "AC-12": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::creating a contact::opens the \"Nuevo contacto\" dialog with focus on Nombre and the optional fields",
      "AC-13": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::creating a contact::creates via createContact with normalized optionals, shows \"Contacto creado\" and lists the row",
      "AC-14": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::creating a contact::shows the inline \"Campo requerido\" error for an empty name without calling the bridge",
      "AC-15": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::editing a contact::opens the edit dialog preloaded, saves via updateContact and shows \"Cambios guardados\"",
      "AC-16": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::deleting a contact::confirms in the simple AlertDialog, deletes via deleteContact and shows \"Contacto eliminado\"",
      "AC-17": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::nonexistent company::shows the error state with the \"Volver a Discoveries\" link when getCompany fails",
      "AC-18": "tests/unit/companies/CompanyDetailPage.test.tsx::CompanyDetailPage::external links markup::renders external links with target=_blank, rel=noreferrer and the exact href (apertura real en el navegador: MANUAL)",
      "AC-19": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx::DiscoveryDetailPage (empresas)::loading and mutation errors::shows skeletons in the companies section while the list is loading",
      "AC-20": "tests/unit/companies/DiscoveryDetailPage.companies.test.tsx::DiscoveryDetailPage (empresas)::loading and mutation errors::shows an error toast and leaves the UI untouched when a mutation fails"
    },
    "notes": "Sin adaptaciones de infra: getCompany se mockea por test (sin default en createMockDbApi, decisión deliberada: no hay valor por defecto sensato para una empresa concreta); listCompanies/listContacts ya tenían default ok []. FIX FLAKY (iteración QA 2026-07-04, confirmado 3/4 fallos contra árbol limpio) en el test de AC-06: LECCIÓN — al abrir un Dialog desde un DropdownMenu (setTimeout(0)), el returnFocus del menú puede robar el foco tras el focus() de onOpenAutoFocus y el FocusScope del Dialog re-enfoca el primer tabbable con select:true → la SELECCIÓN del input precargado es no determinista en jsdom. No asertar selección en dialogs abiertos desde menús (solo en aperturas directas botón→dialog); el foco se aserta con waitFor(toHaveFocus).",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-04"
  },
  "SPEC-012": {
    "spec_file": "specs/SPEC-012-templates-entrevista.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (20): AC-01 card del hub funcional, AC-02 fila con Badge de fase condicional + resumen + menú, AC-03 empty con CTA, AC-04 skeletons y error+Reintentar, AC-05 editor nuevo con 1 bloque y 1 pregunta, AC-06 estructura Nombre+Fase+bloques, AC-07 card de bloque, AC-08 fila de pregunta, AC-09 añadir bloque con foco en su título, AC-10 añadir pregunta con foco en su texto, AC-11 mover bloques/preguntas acotado por nivel con extremos disabled+Tooltip, AC-12 único bloque/única pregunta disabled+tooltips literales, AC-13 guardar con orden exacto y guidance omitida si vacía, AC-14 validaciones anidadas, AC-15 Sin fase → null y sin Badge, AC-16 guard descartar/directo, AC-17 editar precarga en orden exacto, AC-18 duplicar, AC-19 eliminar, AC-20 error de mutación.",
    "unit": [
      {
        "file": "tests/unit/interview-templates/InterviewTemplatesPage.test.tsx",
        "test_count": 7,
        "description": "Hub + listado con api.db mockeado y rutas reales: card del hub clicable → listado (AC-01); filas con Badge 'Problema' condicional —fixture sin fase sin Badge— y resumen '2 bloques · 3 preguntas'/'1 bloque · 1 pregunta' con menú Editar/Duplicar/Eliminar (AC-02); empty + CTA 'Crear primera plantilla' (AC-03); skeletons con Promise<never> y error+Reintentar que rellama, en dos escenarios del mismo it (AC-04); Duplicar → createInterviewTemplate con {name: 'Entrevista MDR (copia)', phase, blocks tal cual} + toast 'Plantilla duplicada' + fila nueva (AC-18); AlertDialog 'Eliminar plantilla' «nombre» → deleteInterviewTemplate + toast 'Plantilla eliminada' + fila fuera (AC-19); y duplicado {ok:false} → toast de error con el listado intacto (AC-20)."
      },
      {
        "file": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx",
        "test_count": 15,
        "description": "Editor de dos niveles (getInterviewTemplate mockeado, probe del listado): nuevo con 1 bloque + 1 pregunta en blanco (AC-05); Nombre + Select de fase con 'Sin fase' default y ayuda metodológica + heading Bloques (AC-06); card de bloque completa —'Bloque 1', placeholders, h4 Preguntas, aria-labels, Añadir pregunta— (AC-07); fila de pregunta completa (AC-08); añadir bloque con foco en su TÍTULO (AC-09); añadir pregunta con foco en su texto (AC-10); AC-11 en DOS its (lección: máx 1 tooltip hover por render): subir bloque intercambia con sus preguntas + tooltip 'Ya es el primer bloque', y bajar pregunta acotada por nivel —fixture 2×2, B1 globalmente tercera con su subir deshabilitado por ser primera de SU bloque— + tooltip 'Ya es la primera pregunta'; AC-12 en DOS its: 'La plantilla necesita al menos un bloque' y 'El bloque necesita al menos una pregunta'; guardar → payload con orden visual exacto, fase vía Select y guidance OMITIDA si vacía (not.toHaveProperty) + toast 'Plantilla creada' + navegación (AC-13); 3 'Campo requerido' anidados sin bridge (AC-14); 'Sin fase' default → phase null en el payload, Badge ausente cubierto por AC-02 (AC-15); guard 'Descartar cambios' con cambios y vuelta directa sin ellos (AC-16); y edición precargada con nombre/fase/bloques/guías/preguntas en su orden exacto (AC-17)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "humo-visual": "Aspecto visual de las cards anidadas del editor (dos niveles), sticky bottom bar, iconos y hover del listado: revisión visual manual — jsdom no hace layout ni estilos."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/interview-templates/InterviewTemplatesPage.test.tsx::InterviewTemplatesPage::access from the hub::navigates from the functional hub card to the interview templates list",
      "AC-02": "tests/unit/interview-templates/InterviewTemplatesPage.test.tsx::InterviewTemplatesPage::listing::renders each row with name, conditional phase badge, block/question summary and the actions menu",
      "AC-03": "tests/unit/interview-templates/InterviewTemplatesPage.test.tsx::InterviewTemplatesPage::listing::shows the empty state with the \"Crear primera plantilla\" CTA when there are none",
      "AC-04": "tests/unit/interview-templates/InterviewTemplatesPage.test.tsx::InterviewTemplatesPage::listing::shows skeletons while loading and the error state with a working \"Reintentar\"",
      "AC-05": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::new template::opens the empty editor with one initial block containing one blank question",
      "AC-06": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::new template::presents the name field, the optional phase select and the blocks list",
      "AC-07": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::new template::renders the block card with header, title, block guidance, questions section and actions",
      "AC-08": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::new template::renders the question row with text, question guidance and its own actions",
      "AC-09": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::new template::adds an empty block at the end (with a blank question) and focuses its title",
      "AC-10": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::new template::adds an empty question at the end of the block and focuses its text",
      "AC-11": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::reordering (2 bloques × 2 preguntas)::swaps blocks with their questions when moved up, disabling the first-block-up with tooltip + tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::reordering (2 bloques × 2 preguntas)::moves a question within its block without crossing block boundaries, with per-block disabled extremes",
      "AC-12": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::reordering (2 bloques × 2 preguntas)::disables removing the only block with its literal tooltip + tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::reordering (2 bloques × 2 preguntas)::disables removing the only question of a block with its literal tooltip",
      "AC-13": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::saving::persists the exact visual order omitting empty guidance keys, shows the toast and returns to the list",
      "AC-14": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::saving::shows nested \"Campo requerido\" errors for name, block title and question text without persisting",
      "AC-15": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::saving::persists phase as null when \"Sin fase\" (default) is kept (Badge ausente en la fila: cubierto por el fixture sin fase de AC-02)",
      "AC-16": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::saving::guards leaving with unsaved changes via \"Descartar cambios\" and returns directly when clean",
      "AC-17": "tests/unit/interview-templates/InterviewTemplateEditorPage.test.tsx::InterviewTemplateEditorPage::editing::loads name, phase, blocks, guidances and questions in their exact stored order",
      "AC-18": "tests/unit/interview-templates/InterviewTemplatesPage.test.tsx::InterviewTemplatesPage::duplicating::creates an immediate full copy named \"«nombre» (copia)\" with the blocks as-is and shows the toast",
      "AC-19": "tests/unit/interview-templates/InterviewTemplatesPage.test.tsx::InterviewTemplatesPage::deleting::confirms in the \"Eliminar plantilla\" AlertDialog, deletes and shows \"Plantilla eliminada\"",
      "AC-20": "tests/unit/interview-templates/InterviewTemplatesPage.test.tsx::InterviewTemplatesPage::mutation errors::shows an error toast and leaves the list untouched when a mutation fails"
    },
    "notes": "Sin adaptaciones de infra: getInterviewTemplate se mockea por test (sin default). SPEC-009 AC-09 derogado y remapeado (card del hub funcional, ruta /templates/interview añadida a la réplica del layout). El fix del flaky preexistente de SPEC-011 AC-06 se documenta en las notes de SPEC-011.",
    "created_at": "2026-07-04"
  },
  "SPEC-013": {
    "spec_file": "specs/SPEC-013-crear-entrevista.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (14): AC-01 fila con título+Badge Borrador+refs+menú, AC-02 empty con CTA, AC-03 skeletons, AC-04 dialog con Título(foco)+Selects opcionales, AC-05 crear con click/Enter en borrador con refs, AC-06 título vacío, AC-07 sin contactos/templates solo sentinels, AC-08 editar precargado, AC-09 editar título vacío, AC-10 eliminar con AlertDialog de notas, AC-11 detalle con Volver+h1+Badge+refs con fallbacks, AC-12 sección Guión empty, AC-13 id inexistente, AC-14 error de mutación.",
    "unit": [
      {
        "file": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx",
        "test_count": 12,
        "description": "Sección Entrevistas del detalle de empresa (api.db mockeado; la página tiene DOS grupos de menús 'Acciones' y DOS AlertDialogs → matching por fila con closest(li) del título, nunca índice global): fila con Link al detalle + Badge 'Borrador' + refs resueltas 'Jane Doe · Entrevista MDR' + menú Editar/Eliminar (AC-01); empty + CTA (AC-02); skeletons con listInterviews pendiente (AC-03); dialog 'Nueva entrevista' con Título enfocado y placeholder 'Discovery con Acme Corp' + Selects con 'Sin contacto'/'Sin template' y el template con su fase entre paréntesis (AC-04); AC-05 en DOS its: crear con click y refs elegidas → createInterview {companyId,title,contactId,templateId} + toast 'Entrevista creada' + fila, y crear con Enter sin tocar Selects → sentinels a null; título vacío → 'Campo requerido' sin bridge (AC-06); sin contactos ni templates → Selects solo con el sentinel y la creación sigue funcionando (AC-07); editar precargado (título + Selects con los valores) → updateInterview con los 3 campos + toast 'Cambios guardados' + fila actualizada (AC-08); editar a vacío → error sin bridge (AC-09); AlertDialog 'Eliminar entrevista' con '«título» y sus notas.' → deleteInterview + toast 'Entrevista eliminada' + fila fuera (AC-10); y create {ok:false} → toast de error + dialog abierto + lista intacta (AC-14)."
      },
      {
        "file": "tests/unit/interviews/InterviewDetailPage.test.tsx",
        "test_count": 3,
        "description": "Detalle mínimo de entrevista (getInterview+getCompany mockeados; nombres resueltos con listContacts/listInterviewTemplates): h1 + Badge 'Borrador' + fila de referencias con fallbacks 'Acme Corp · Sin contacto · Sin template' (fixture con refs null) y Volver → detalle de la empresa (AC-11); sección Guión con 'Aún no hay guión' + 'La generación con IA llegará en la siguiente fase' (AC-12); y getInterview not-found → error + link 'Volver a Discoveries' (AC-13)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "humo-visual": "Aspecto visual de la sección Entrevistas (badges, refs muted, menús ⋯) y del detalle: revisión visual manual — jsdom no hace layout ni estilos."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::listing::renders each interview row with title link, \"Borrador\" badge, resolved refs and its own actions menu",
      "AC-02": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::listing::shows the empty state with the \"Crear primera entrevista\" CTA when there are none",
      "AC-03": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::listing::shows skeletons in the interviews section while the list is loading",
      "AC-04": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::creating an interview::opens the \"Nueva entrevista\" dialog with focused title and the optional contact/template selects",
      "AC-05": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::creating an interview::creates in draft with the chosen refs on \"Crear\" click, shows the toast and lists the row + tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::creating an interview::creates on Enter mapping the untouched selects to null refs",
      "AC-06": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::creating an interview::shows the inline \"Campo requerido\" error for an empty title without calling the bridge",
      "AC-07": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::creating an interview::offers only the sentinels when there are no contacts nor templates, and creation still works",
      "AC-08": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::editing an interview::opens the edit dialog preloaded, saves the three fields via updateInterview and shows \"Cambios guardados\"",
      "AC-09": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::editing an interview::shows the inline \"Campo requerido\" error when editing to an empty title without calling the bridge",
      "AC-10": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::deleting an interview::confirms in the \"Eliminar entrevista\" AlertDialog with the notes warning and deletes with its toast",
      "AC-11": "tests/unit/interviews/InterviewDetailPage.test.tsx::InterviewDetailPage::header and references::shows title, \"Borrador\" badge and refs with the null fallbacks, and \"Volver\" returns to the company",
      "AC-12": "DEROGADO PARCIALMENTE por SPEC-014 AC-08 (el secundario 'La generación con IA llegará en la siguiente fase' desapareció; 'Aún no hay guión' sobrevive): tests/unit/interviews/InterviewDetailPage.test.tsx::InterviewDetailPage::header and references::shows the Guión section with its empty state (adaptado) + cobertura completa del empty state en SPEC-014 AC-08",
      "AC-13": "tests/unit/interviews/InterviewDetailPage.test.tsx::InterviewDetailPage::nonexistent interview::shows the error state with the \"Volver a Discoveries\" link when getInterview fails",
      "AC-14": "tests/unit/interviews/CompanyDetailPage.interviews.test.tsx::CompanyDetailPage (entrevistas)::mutation errors::shows an error toast, keeps the dialog open and leaves the list untouched when the bridge fails"
    },
    "notes": "Sin adaptaciones de infra ni regresión sobre tests previos (la sección Entrevistas es nueva y los tests de SPEC-011 acotan por fila, no por conteos globales). getInterview se mockea por test. SPEC-014 derogó parcialmente AC-12 (secundario del empty state del guión) → test adaptado; el otro test roto (AC-11) solo necesitaba el mock nuevo de api.llm en el helper (TypeError de getStatus), sin cambios en sus asserts (regresión de contrato presupuestada, no bug). SPEC-020 (schema v2): las fixtures de Interview ganan discoveryId: 'd-1' y las aserciones de createInterview incluyen discoveryId (la firma nueva useInterviews(discoveryId, companyId) lo envía desde la ruta). Regresión de contrato presupuestada, no bug.",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-10"
  },
  "SPEC-014": {
    "spec_file": "specs/SPEC-014-guion-ia.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (12): AC-01 generar (loading→guión+objetivos+Preparada+toast), AC-02 sin template disabled+tooltip, AC-03 sin clave Alert+link+disabled, AC-04 contexto histórico en el prompt, AC-05 regenerar con AlertDialog, AC-06 error de API → toast y entrevista intacta, AC-07 lectura pre-wrap+Objetivos, AC-08 empty 'Aún no hay guión'+botón, AC-09 edición (Textarea+objetivos editables), AC-10 guardar, AC-11 cancelar con guard, AC-12 objetivos vacíos descartados.",
    "unit": [
      {
        "file": "tests/unit/script/llmService.test.ts",
        "test_count": 5,
        "description": "llmService en Node con vi.mock('@anthropic-ai/sdk') (clases de error espejo para instanceof + messages.create mockeado) y store/repository REALES en temporal: happy path → persiste scriptMarkdown + objetivos filtrados (vacíos descartados) + status prepared, devuelve la Interview y usa claude-opus-4-8/max_tokens 16000/output_config json_schema sin temperature (AC-01 servicio); sin template → no-template sin llamar al SDK (AC-02 servicio); sin clave resoluble → no-key sin llamar al SDK (AC-03 servicio); contexto histórico → el prompt de usuario contiene '## Entrevistas anteriores', el transcript aplanado '[mic s0] …' y la nota de la misma empresa, NO contiene la de otra empresa, y un transcript corrupto se omite sin lanzar (AC-04); y errores del SDK → AuthenticationError→auth, RateLimitError→rate-limit, APIConnectionError→connection, JSON inválido→format, con la entrevista intacta releída del store en TODOS los casos (AC-06 servicio)."
      },
      {
        "file": "tests/unit/script/ScriptSection.test.tsx",
        "test_count": 10,
        "description": "Sección Guión vía InterviewDetailPage (api.llm + api.db mockeados; DOS botones 'Generar guión' cabecera+empty → getAllBy): generar happy con promesa controlada → 'Generando guión…' disabled → guión + objetivos + Badge 'Preparada' + toast 'Guión generado' (AC-01 UI); sin template → disabled + tooltip 'Asigna un template para generar el guión' (AC-02 UI); sin clave → Alert con Link a /settings + disabled (AC-03 UI); Regenerar → AlertDialog 'Se sobrescribirán el guión y los objetivos actuales.' y confirmar rellama generateScript (AC-05); generateScript {ok:false} → toast con la causa y sin cambios (Badge sigue 'Borrador') (AC-06 UI); lectura con whitespace-pre-wrap + h4 Objetivos + lista (AC-07); empty 'Aún no hay guión' con 2 botones habilitados y sin el secundario derogado (AC-08); edición → Textarea con el markdown + inputs por objetivo + añadir/quitar (AC-09); guardar → updateInterview {scriptMarkdown, objectives filtrados} SIN status + toast 'Cambios guardados' + vuelta a lectura (AC-10 + AC-12 juntos verificando el filtrado); y cancelar con cambios → 'Descartar cambios' con Seguir editando/Descartar, y sin cambios vuelta directa (AC-11)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "generacion-real-api": "Generación real contra la API de Anthropic con una clave válida configurada en Ajustes: pendiente del humano (no hay clave en la máquina de desarrollo, patrón SPEC-002; el pipeline de auth quedó verificado en la fase 0 del dev). Todo lo demás está automatizado con el SDK mockeado a nivel de servicio.",
      "humo-visual": "Aspecto visual del guión pre-wrap, spinner del botón y sticky bar local de la edición: revisión visual manual — jsdom no hace layout ni estilos."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/script/llmService.test.ts::llmService::when the generation succeeds::persists scriptMarkdown, filtered objectives and status prepared, returning the updated interview + tests/unit/script/ScriptSection.test.tsx::ScriptSection::generation::shows the loading button while generating and then the script, objectives, \"Preparada\" badge and toast",
      "AC-02": "tests/unit/script/llmService.test.ts::llmService::prerequisites::fails with no-template without calling the SDK when the interview has no template + tests/unit/script/ScriptSection.test.tsx::ScriptSection::generation::disables the generate button with the template tooltip when the interview has no template",
      "AC-03": "tests/unit/script/llmService.test.ts::llmService::prerequisites::fails with no-key without calling the SDK when no Anthropic key is resolvable + tests/unit/script/ScriptSection.test.tsx::ScriptSection::generation::shows the key alert linking to Ajustes and disables the button when there is no Anthropic key",
      "AC-04": "tests/unit/script/llmService.test.ts::llmService::historical context::injects previous same-company transcripts and notes into the user prompt, omitting other companies and corrupt transcripts",
      "AC-05": "tests/unit/script/ScriptSection.test.tsx::ScriptSection::generation::asks for confirmation in the \"Regenerar guión\" AlertDialog and regenerates on confirm",
      "AC-06": "tests/unit/script/llmService.test.ts::llmService::SDK and format errors::maps SDK errors to typed kinds and leaves the interview untouched in every case + tests/unit/script/ScriptSection.test.tsx::ScriptSection::generation::shows an error toast and leaves the interview unchanged when the generation fails",
      "AC-07": "tests/unit/script/ScriptSection.test.tsx::ScriptSection::reading::renders the script preserving line breaks (pre-wrap) and the Objetivos list below",
      "AC-08": "tests/unit/script/ScriptSection.test.tsx::ScriptSection::reading::shows the \"Aún no hay guión\" empty state with the generate button when prerequisites are met",
      "AC-09": "tests/unit/script/ScriptSection.test.tsx::ScriptSection::editing::switches to a textarea plus editable objectives with add/remove controls",
      "AC-10": "tests/unit/script/ScriptSection.test.tsx::ScriptSection::editing::saves via updateInterview without status, silently dropping empty objectives, and returns to read mode",
      "AC-11": "tests/unit/script/ScriptSection.test.tsx::ScriptSection::editing::asks to discard unsaved changes on cancel, and returns directly when clean",
      "AC-12": "tests/unit/script/ScriptSection.test.tsx::ScriptSection::editing::saves via updateInterview without status, silently dropping empty objectives, and returns to read mode (mismo it que AC-10: el payload aserta el filtrado de vacíos) + filtrado también fijado en el servicio (AC-01 servicio: objetivos vacíos descartados)"
    },
    "notes": "Adaptación de infra: tests/helpers/mockApi.ts amplía BridgeApi con llm: LlmApi (getStatus default {hasAnthropicKey:false}; generateScript configurable por test) tras el cambio de contrato del preload (window.api.llm). SPEC-020 (schema v2): los seeds de llmService.test.ts y las fixtures de ScriptSection.test.tsx ganan discoveryId; el prompt de usuario incluye ahora la sección '## Discovery' también con empresa (los asserts existentes no se ven afectados). La degradación sin empresa se cubre en tests/unit/script/llmService.captures.test.ts bajo SPEC-020.",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-10"
  },
  "SPEC-015": {
    "spec_file": "specs/SPEC-015-grabacion-entrevista.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (14): AC-01 preparación (badges+select+Iniciar), AC-02 permiso denegado no arranca, AC-03 select disabled+tooltip grabando, AC-04 iniciar con asociación + cronómetro + medidores + Detener destructive, AC-05 detener asocia wav/transcript + Grabada + toast, AC-06 navegar fuera auto-guarda, AC-07 close guard del spike, AC-08 desconexión controlada conservando y asociando, AC-09 transcripción en vivo con fuente/hablante y Badge, AC-10 sin key graba con Alert informativo, AC-11 resumen (duración/latencia/rutas/Finder), AC-12 Nueva grabación con AlertDialog de sobrescritura, AC-13 persistencia tras recarga, AC-14 guión visible durante la grabación.",
    "unit": [
      {
        "file": "tests/unit/recording/RecordingSection.test.tsx",
        "test_count": 14,
        "description": "Sección Grabación vía InterviewDetailPage con rutas reales (servicios del spike mockeados con el patrón SPEC-001 + bridge window.api; 'Micrófono' aparece ×2-3 → roles/getAllBy): preparación con badges Concedido×2 + combobox habilitado + Iniciar grabación (AC-01); permiso denegado → Alert destructive con el literal del spike sin arrancar —localizado por título + closest('[role=alert]') porque puede convivir con otros alerts— (AC-02); select disabled con tooltip durante la grabación (AC-03); iniciar → recording.start('i-1') asociando la entrevista + cronómetro 00:00 + 2 progressbars + Detener data-variant destructive (AC-04); detener con StopResult.interview → toast 'Grabación guardada' + Badge 'Grabada' + Estado 3 con Duración 01:35 y rutas (AC-05); navegar fuera (Volver) durante la grabación → cleanup invoca recording.stop sin diálogo (AC-06); close-requested → StopOnCloseDialog 'Detener captura' (AC-07); desconexión del micrófono (track ended con el fake) → Alert 'Dispositivo desconectado' + stop asociando (Badge Grabada + wavPath) — camino finalize compartido con SPEC-001 AC-13 (AC-08); eventos de transcripción → línea con fuente/Hablante 1 + Badge 'Transcribiendo' (AC-09); status no-key → 'Falta la key de Deepgram' con la grabación operativa (AC-10); resumen persistido: rutas + Latencia STT (getTranscriptStats) + Mostrar en Finder → showInFinder(wavPath) (AC-11); 'Nueva grabación' → AlertDialog 'Sobrescribir grabación' con el literal y 'Sobrescribir' vuelve a preparación (AC-12); recarga = montaje directo con la Interview grabada → getTranscriptStats(transcriptPath) + fila de latencia con Badge OK (AC-13); y guión legible (heading + contenido + Objetivos) coexistiendo con el cronómetro (AC-14)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "verificacion-acustica-flujo-real": "Verificación acústica end-to-end en el flujo real de la entrevista (audio real de mic+sistema, transcripción real con Deepgram, archivos reproducibles): requiere hardware/red/humano, igual que en SPEC-001/002.",
      "sesion-60-min": "Sesión sostenida ≥60 minutos sin pérdida de audio ni fugas de memoria (ítem 6 de H4): ejecución prolongada de la app real con monitorización, manual del humano.",
      "navegacion-cierre-desconexion-reales": "Auto-guardado al navegar, close guard y desconexión física del micrófono con la app y hardware reales: los caminos lógicos están automatizados (AC-06/07/08); la observación end-to-end es manual."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::preparation::shows the permission badges, the microphone select and the \"Iniciar grabación\" button",
      "AC-02": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::preparation::does not start and shows the destructive alert with the spike literal when a permission is denied",
      "AC-03": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::preparation::disables the microphone select with a tooltip while recording",
      "AC-04": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::recording::starts the capture associated to the interview showing chronometer, both meters and a destructive Detener",
      "AC-05": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::recording::stops associating wav/transcript to the interview, flips the badge to \"Grabada\" and toasts \"Grabación guardada\"",
      "AC-06": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::recording::auto-stops and saves when navigating away from the detail during a recording",
      "AC-07": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::recording::keeps the spike close guard: closing the app during a recording opens the \"Detener captura\" dialog",
      "AC-08": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::recording::stops in a controlled way on device disconnection, keeping the recording associated and showing the cause (camino finalize compartido: ver también SPEC-001 AC-13)",
      "AC-09": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::live transcription::shows live lines with source badge and speaker label plus the \"Transcribiendo\" status badge",
      "AC-10": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::live transcription::keeps recording without transcription showing the no-key informative alert",
      "AC-11": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::after recording::shows the summary with paths, the latency row and a working \"Mostrar en Finder\"",
      "AC-12": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::after recording::asks to overwrite in the \"Sobrescribir grabación\" dialog and returns to preparation on confirm",
      "AC-13": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::after recording::restores the persisted summary after a reload, reading the latency from the transcript file",
      "AC-14": "tests/unit/recording/RecordingSection.test.tsx::RecordingSection::script visibility::keeps the Guión section visible and readable while the recording is running"
    },
    "notes": "Adaptación de infra: tests/helpers/mockApi.ts añade recording.getTranscriptStats (default null) tras la ampliación del contrato (única rotura de compilación, presupuestada). Los tests previos del detalle de entrevista no requirieron cambios: el bridge del spike ya estaba mockeado por el helper y la sección nueva degrada bien con los defaults. SPEC-016 añadió el panel de objetivos EN VIVO durante la grabación → el test de AC-14 pasó a getAllBy para el h4 'Objetivos' (dos headings: panel del asistente + sección Guión; regresión de contrato presupuestada, no bug). SPEC-020 (schema v2): la fixture de Interview de RecordingSection.test.tsx gana discoveryId: 'd-1' (rotura de compilación presupuestada, no bug). Corrección post-run 2026-07-10 (test-defect pre-existente desde SPEC-019, verificado en 269e5f3): RecordingSection.test.tsx nunca se adaptó al ConsentDialog de SPEC-019 — el helper startRecording y el test de permiso denegado atraviesan ahora el AlertDialog 'Aviso de grabación' confirmando 'Entendido, iniciar grabación' (asertando su heading), con localStorage.clear() en beforeEach para aislar la preferencia 'maurya:recording-consent-dismissed'. Los ACs de SPEC-019 siguen SIN suite propia (gap del pipeline). El assert de AC-04 verifica la firma nueva de recording.start: id 'i-1' + timestamp del consentimiento como ISO 8601 real (regex estricta + round-trip new Date(x).toISOString() === x). Los ACs propios de SPEC-019 tienen ya suite y entrada propias (tests/unit/consent/, 2026-07-10).",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-10"
  },
  "SPEC-016": {
    "spec_file": "specs/SPEC-016-asistencia-tiempo-real.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (15): AC-01 análisis proactivo con sugerencia, AC-02 'Analizando…' sin ocultar la anterior, AC-03 sin clave inactivo con Link a Ajustes, AC-04 transcripción inactiva → asistente inactivo, AC-05 sugerencia en su tamaño justo, AC-06 la nueva sustituye a la anterior, AC-07 dig_deeper con porqué concreto, AC-08 chips de alarma, AC-09 objetivos en vivo acumulativos, AC-10 sin objetivos sin panel, AC-11 feedback 👍/👎, AC-12 registro persistido en el transcript, AC-13 control de frecuencia y coste, AC-14 error discreto con reintento, AC-15 fin de la grabación.",
    "unit": [
      {
        "file": "tests/unit/assistant/assistantService.test.ts",
        "test_count": 8,
        "description": "assistantService en Node (SDK mockeado con clases de error espejo; transcriptionService/repository REALES: las líneas finales se inyectan por la vía pública conexión Deepgram mockeada → finalLineListener; vi.useFakeTimers({toFake:['Date']}) para gobernar el intervalo sin romper promesas): 2 líneas no llaman y la 3ª dispara analyzing→active con la sugerencia, fijando MIN_NEW_FINAL_LINES=3/MIN_INTERVAL_MS=20000 (AC-01+AC-13); material de sobra dentro de la ventana no llama y pasada la ventana sí (AC-13); guard in-flight: promesa colgada + más líneas y tiempo → una sola llamada (AC-13); sin clave → evento no-key e inerte con stop null (AC-03 servicio); objectivesMet acumulativo con índices fuera de rango filtrados (AC-09 servicio); error del SDK → evento error kind auth con contadores intactos y reintento en la siguiente ventana (AC-14 servicio); feedback mutable hasta la siguiente sugerencia + summary {suggestionCount:2, feedback:{up:1,down:1}} y persistTranscript escribiendo el campo assistant en el JSON (AC-11 servicio + AC-12); y stop: listener retirado (líneas posteriores no llaman), timer limpio y respuesta tardía descartada sin eventos nuevos (AC-15 servicio)."
      },
      {
        "file": "tests/unit/assistant/AssistantPanel.test.tsx",
        "test_count": 12,
        "description": "Panel del asistente y de objetivos vía InterviewDetailPage grabando (mocks del spike + emitAssistantUpdate): texto inicial literal (parte AC-01); 'Analizando…' conservando la sugerencia anterior (AC-02); no-key con 'Asistente inactivo — configura tu clave de Anthropic en' + Link a /settings (AC-03 UI); transcripción inactiva → sin actividad del panel, estado inicial persiste (AC-04); sugerencia en su tamaño justo: Badge 'Continúa' + pregunta + porqué y NADA más (sin chips, sin Analizando, sin error, sin texto inicial) (AC-05); la nueva sustituye a la anterior (AC-06); dig_deeper → Badge 'Profundiza' con el porqué concreto (AC-07); chips 'Cumplido'/'Genérico'/'Hipotético' (AC-08); panel de objetivos con line-through en los cubiertos según objectivesMet (AC-09 UI); sin objetivos → sin panel (AC-10); 👍/👎 → sendFeedback('up'|'down') con resaltado bg-accent conmutable (AC-11 UI); línea discreta 'No se pudo analizar (se reintentará): …' bajo la sugerencia conservada (AC-14 UI); y al detener → Estado 3 sin panel (AC-15 UI)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "calidad-sugerencias-llamada-real": "Calidad de las sugerencias del copiloto en una llamada real (¿ayudan sin distraer? Riesgo #2) y latencia percibida del análisis: juicio humano con la clave de Anthropic real configurada (pendiente del humano, patrón SPEC-014).",
      "test-usabilidad": "Test de usabilidad del PRD (Riesgo #2, planificado para H7): observar a un entrevistador real usando el panel durante una entrevista completa.",
      "coste-real": "Consumo real de tokens/coste por sesión con la API real: los parámetros de control (3 líneas / 20 s / 45 s fallback, effort low, max_tokens 1024) están fijados por tests; la factura real la valida el humano."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/assistant/assistantService.test.ts::assistantService::proactive triggering::does not call with fewer than MIN_NEW_FINAL_LINES and analyzes proactively on the third final line + tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::activation states::shows the initial waiting text while recording before any suggestion",
      "AC-02": "tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::activation states::shows the discreet \"Analizando…\" indicator without hiding the previous suggestion",
      "AC-03": "tests/unit/assistant/assistantService.test.ts::assistantService::without an Anthropic key::emits no-key and stays inert: zero calls and stop returns null + tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::activation states::shows the inactive no-key state with a link to Ajustes",
      "AC-04": "tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::activation states::stays in the initial state while the transcription is inactive (no assistant activity)",
      "AC-05": "tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::the suggestion::shows exactly the action badge, the suggested question and the one-line reason — nothing else",
      "AC-06": "tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::the suggestion::replaces the previous suggestion when a new one arrives (only one visible)",
      "AC-07": "tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::the suggestion::shows the amber \"Profundiza\" badge with the concrete reason for dig_deeper",
      "AC-08": "tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::the suggestion::shows the alarm chips with their Spanish labels when alarms are detected",
      "AC-09": "tests/unit/assistant/assistantService.test.ts::assistantService::objectives tracking::accumulates objectivesMet across analyses filtering out-of-range indices + tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::live objectives::shows the objectives panel with pending/covered states from objectivesMet",
      "AC-10": "tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::live objectives::does not show the objectives panel when the interview has no objectives",
      "AC-11": "tests/unit/assistant/assistantService.test.ts::assistantService::feedback and persistence::registers mutable votes per suggestion and persists the session summary in the transcript + tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::feedback::sends the vote and highlights the chosen thumb, switchable until the next suggestion",
      "AC-12": "tests/unit/assistant/assistantService.test.ts::assistantService::feedback and persistence::registers mutable votes per suggestion and persists the session summary in the transcript (mismo it: persistTranscript escribe el campo assistant en el JSON)",
      "AC-13": "tests/unit/assistant/assistantService.test.ts::assistantService::proactive triggering::does not call again before MIN_INTERVAL_MS even with enough new lines, and does after it + tests/unit/assistant/assistantService.test.ts::assistantService::proactive triggering::never runs two analyses at once: new lines during an in-flight call do not trigger another",
      "AC-14": "tests/unit/assistant/assistantService.test.ts::assistantService::API errors::emits a typed error keeping the counters so the next window retries + tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::errors::shows the discreet retry line under the preserved suggestion on analysis error",
      "AC-15": "tests/unit/assistant/assistantService.test.ts::assistantService::stop::detaches the listener and discards late in-flight responses after stop + tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::end of recording::removes the assistant panel when the recording stops into the recorded state"
    },
    "notes": "Adaptación de infra (causa única del breakage, presupuestada): tests/helpers/mockApi.ts amplía BridgeApi con assistant: AssistantApi (onUpdate con unsubscribe real; sendFeedback resuelto) + emitAssistantUpdate en el handle — esto recupera los tests de recording/script/interviews sin tocarlos. La forma PersistedTranscript del test de SPEC-002 gana el campo assistant ({ lines, latency, assistant }). SPEC-020: la fixture de AssistantPanel.test.tsx gana discoveryId; el seed de assistantService.test.ts pasa a una CAPTURA SIN EMPRESA (companyId null) — toda la suite SPEC-016 corre ahora sin empresa asignada y sirve de evidencia de SPEC-020 AC-16 (el asistente solo depende de interviewId). Las garantías de SPEC-016 no cambian: ningún AC dependía de la empresa. Corrección post-run 2026-07-10 (mismo test-defect SPEC-019 que SPEC-015): el helper startRecording de AssistantPanel.test.tsx atraviesa el aviso de consentimiento confirmándolo, con localStorage.clear() en beforeEach. Los ACs propios de SPEC-019 tienen ya suite y entrada propias (tests/unit/consent/, 2026-07-10). SPEC-021: AssistantSessionSummary gana usage (siempre presente) → los toEqual exactos del summary y del bloque assistant del transcript incluyen ahora usage a ceros (el SDK mockeado no trae bloque usage); mockApi añade assistant.resume. Regresión de contrato presupuestada, no bug.",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-10"
  },
  "SPEC-017": {
    "spec_file": "specs/SPEC-017-nota-resumen-entrevista.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (23): AC-01 sección Nota con selector y Generar nota, AC-02 sin grabación empty, AC-03 sin note-templates hint+Link+disabled, AC-04 sin clave aviso+disabled, AC-05 nota con heading por sección del template en orden, AC-06 'Generando nota…' disabled, AC-07 éxito → Resumida+toast+nota, AC-08 AlertDialog Regenerar nota, AC-09 confirmar regenera y sustituye, AC-10 error LLM → Alert destructive con todo intacto, AC-11 lectura con Editar/Exportar/Regenerar, AC-12 Editar → Textarea+Guardar/Descartar, AC-13 Guardar persiste+toast, AC-14 Descartar con cambios → AlertDialog y restaura, AC-15 Descartar sin cambios directo, AC-16 guardar no cambia el estado, AC-17 Sheet de transcripción con hablantes, AC-18 transcripción ilegible → error sin romper, AC-19 exportar nota .md, AC-20 exportar transcripción .md, AC-21 cancelar diálogo neutro, AC-22 fallo de escritura → toast destructive, AC-23 Badge Resumida.",
    "unit": [
      {
        "file": "tests/unit/notes/noteService.test.ts",
        "test_count": 8,
        "description": "noteService en Node (SDK mockeado con clases de error espejo, dialog.showSaveDialog mockeado, store/repository REALES y transcript.json real en temporal): ensamblado de la nota con los títulos del template como fuente de verdad, en orden ('## Dolores…## Citas'), persistida con status summarized SOLO tras parseo válido, y prompt con secciones numeradas + conversación '[Tú]/[Interlocutor 2]' + context del template en el system (AC-05); la regeneración actualiza la nota existente (mismo id) en vez de crear otra (AC-09 servicio); guards no-transcript/no-key con sus literales sin llamar al SDK (refuerzo AC-02+AC-04); AuthenticationError→auth y respuesta que no cubre todas las secciones→format, con nota previa y estado intactos (AC-10 servicio); readTranscriptLines: corrupto/ausente → {ok:false, kind:'unreadable', message:'No se pudo leer la transcripción'} y válido parsea con speaker null por defecto (AC-18 servicio); export nota: defaultPath slugificado 'discovery-con-acme-nota.md', escribe el markdown, y cancelar → {saved:false} sin archivo (AC-19+AC-21 servicio); export transcripción: '**Tú:** …' una línea por intervención con defaultPath '-transcripcion.md' (AC-20 servicio); y fallo de escritura → NoteExportOperationError kind write (AC-22 servicio)."
      },
      {
        "file": "tests/unit/notes/NoteSection.test.tsx",
        "test_count": 22,
        "description": "Sección Nota + TranscriptSheet vía InterviewDetailPage (mocks del spike + bridge; within(section) anclado al heading 'Nota' por colisiones con Grabación/Guión): selector 'Note-template' preseleccionado + 'Generar nota' + 'Ver transcripción' (AC-01); sin grabación → 'Graba la entrevista para poder generar la nota.' sin selector ni botón (AC-02); sin templates → hint con Link 'Gestionar note-templates' a /settings?tab=note-templates + disabled con tooltip 'Necesitas un note-template' (AC-03); sin clave → aviso con Link a Ajustes + disabled (AC-04); 'Generando nota…' disabled con promesa pendiente (AC-06); éxito → generateNote('i-1','nt-1') + Badge 'Resumida' + toast 'Nota generada' + nota con h4 por sección (AC-07); AlertDialog 'Regenerar nota' con el literal de sustitución y Cancelar sin llamada (AC-08); confirmar regenera y sustituye el contenido (AC-09); error → Alert destructive con el mensaje, nota previa y Badge intactos, sin toast (AC-10); lectura con Editar/Exportar/Ver transcripción/Regenerar nota (AC-11); Editar → Textarea 'Nota' con el markdown + Guardar/Descartar (AC-12); Guardar → updateNote + toast 'Nota guardada' + lectura con el contenido nuevo (AC-13); Descartar con cambios → AlertDialog 'Descartar cambios' y restaura lo persistido (AC-14); sin cambios → directo sin diálogo (AC-15); el guardado usa SOLO updateNote {contentMarkdown} sin updateInterview, Badge sigue 'Resumida' (AC-16); Sheet (Dialog de radix) con 'Tú'/'Interlocutor 2' y textos, getTranscriptLines(transcriptPath) (AC-17); ilegible → 'No se pudo leer la transcripción' en el Sheet con la página viva detrás (AC-18 UI); menú Exportar → export('i-1','note') + toast 'Nota exportada' (AC-19); export('i-1','transcript') + toast 'Transcripción exportada' (AC-20); cancelación {saved:false} → sin toast alguno (AC-21); {ok:false} → toast 'No se pudo exportar' (AC-22); y Badge 'Resumida' en la cabecera del detalle (STATUS_LABELS compartido con el listado) (AC-23)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "calidad-resumen-api-real": "Calidad real del resumen (fidelidad, citas textuales, distinción hechos/inferencias — KPI #4, Riesgo #6) con la API real de Anthropic: juicio humano con su clave configurada (patrón SPEC-014/016). El pipeline completo está automatizado con el SDK mockeado.",
      "save-dialog-real": "El diálogo de guardado nativo de macOS (apariencia, filtro .md, carpeta por defecto): dialog.showSaveDialog se mockea en tests; la interacción real es del humano."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/notes/NoteSection.test.tsx::NoteSection::base states::shows the template selector and the \"Generar nota\" button for a recorded interview without note",
      "AC-02": "tests/unit/notes/NoteSection.test.tsx::NoteSection::base states::shows the record-first empty state without selector nor generate button when there is no recording + guard de servicio en tests/unit/notes/noteService.test.ts::noteService::generation::fails typed with no-transcript and no-key guards without calling the SDK",
      "AC-03": "tests/unit/notes/NoteSection.test.tsx::NoteSection::base states::shows the create-template hint with a link and disables generation with its tooltip when there are no note-templates",
      "AC-04": "tests/unit/notes/NoteSection.test.tsx::NoteSection::base states::shows the Anthropic key alert with a link to Ajustes and disables generation when there is no key + guard de servicio en tests/unit/notes/noteService.test.ts::noteService::generation::fails typed with no-transcript and no-key guards without calling the SDK",
      "AC-05": "tests/unit/notes/noteService.test.ts::noteService::generation::assembles the note with the template section titles in order and persists it with status summarized only after a valid parse",
      "AC-06": "tests/unit/notes/NoteSection.test.tsx::NoteSection::generation::shows the disabled \"Generando nota…\" button while the generation is pending",
      "AC-07": "tests/unit/notes/NoteSection.test.tsx::NoteSection::generation::flips the badge to \"Resumida\", toasts \"Nota generada\" and shows the note on success",
      "AC-08": "tests/unit/notes/NoteSection.test.tsx::NoteSection::generation::opens the \"Regenerar nota\" AlertDialog warning that edits will be replaced, with Cancelar and Regenerar",
      "AC-09": "tests/unit/notes/NoteSection.test.tsx::NoteSection::generation::regenerates on confirm replacing the note and toasting \"Nota generada\" + tests/unit/notes/noteService.test.ts::noteService::generation::updates the existing note in place when regenerating instead of creating a second one",
      "AC-10": "tests/unit/notes/NoteSection.test.tsx::NoteSection::generation::shows a destructive alert with the mapped message and keeps interview and previous note intact on LLM error + tests/unit/notes/noteService.test.ts::noteService::generation::maps SDK and format errors leaving the interview and its previous note untouched",
      "AC-11": "tests/unit/notes/NoteSection.test.tsx::NoteSection::reading and editing::shows the note in read mode with Editar, Exportar, Ver transcripción and Regenerar nota",
      "AC-12": "tests/unit/notes/NoteSection.test.tsx::NoteSection::reading and editing::switches to a textarea with Guardar and Descartar when Editar is clicked",
      "AC-13": "tests/unit/notes/NoteSection.test.tsx::NoteSection::reading and editing::persists the edit, returns to read mode and toasts \"Nota guardada\" on Guardar",
      "AC-14": "tests/unit/notes/NoteSection.test.tsx::NoteSection::reading and editing::asks to discard changes and restores the persisted content on confirm",
      "AC-15": "tests/unit/notes/NoteSection.test.tsx::NoteSection::reading and editing::returns to read mode directly without any dialog when discarding with no changes",
      "AC-16": "tests/unit/notes/NoteSection.test.tsx::NoteSection::reading and editing::saves only contentMarkdown so the interview status stays \"Resumida\"",
      "AC-17": "tests/unit/notes/NoteSection.test.tsx::NoteSection::transcript sheet::opens the read-only transcript sheet with speaker-labelled lines",
      "AC-18": "tests/unit/notes/NoteSection.test.tsx::NoteSection::transcript sheet::shows the unreadable-transcript error inside the sheet without breaking the page + tests/unit/notes/noteService.test.ts::noteService::transcript reading::returns the typed unreadable result for corrupt files and parses valid ones with speaker fallback",
      "AC-19": "tests/unit/notes/NoteSection.test.tsx::NoteSection::export::exports the note from the Exportar menu and toasts \"Nota exportada\" + tests/unit/notes/noteService.test.ts::noteService::export::exports the note via the save dialog with the slugified default name, and cancelling writes nothing",
      "AC-20": "tests/unit/notes/NoteSection.test.tsx::NoteSection::export::exports the transcript from the Exportar menu and toasts \"Transcripción exportada\" + tests/unit/notes/noteService.test.ts::noteService::export::exports the transcript as one markdown line per intervention with its speaker",
      "AC-21": "tests/unit/notes/NoteSection.test.tsx::NoteSection::export::shows no toast when the save dialog is cancelled (neutral outcome) + mitad servicio en el it de AC-19 (cancelar → {saved:false} sin archivo)",
      "AC-22": "tests/unit/notes/NoteSection.test.tsx::NoteSection::export::toasts the destructive \"No se pudo exportar\" when the export fails + tests/unit/notes/noteService.test.ts::noteService::export::fails typed with kind write when the file cannot be written",
      "AC-23": "tests/unit/notes/NoteSection.test.tsx::NoteSection::interview status::shows the \"Resumida\" status badge for a summarized interview (STATUS_LABELS.summarized compartido por el listado de la empresa)"
    },
    "notes": "Adaptación de infra (breakage exacto del plan §5): tests/helpers/mockApi.ts añade llm.generateNote, recording.getTranscriptLines (default ok []) y notes: NotesApi (createMockNotesApi exportado, default {saved:true, filePath:'/tmp/x.md'}). GAP documentado: la calidad real del resumen con la API real es juicio humano (manual_verification). SPEC-020 (schema v2): el seed de noteService.test.ts y la fixture de NoteSection.test.tsx ganan discoveryId (rotura de compilación presupuestada, no bug). La degradación de la nota sin empresa se cubre en tests/unit/notes/noteService.captures.test.ts bajo SPEC-020.",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-10"
  },
  "SPEC-018": {
    "spec_file": "specs/SPEC-018-busqueda-global.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec — el conteo real es 19 (apertura ×4, resultados ×7, navegación ×5, estados ×3): AC-01 botón Buscar abre con foco, AC-02 ⌘K/Ctrl+K, AC-03 Escape cierra sin navegar, AC-04 reabrir vacío, AC-05 discovery en su grupo, AC-06 empresa con discoveryName, AC-07 contacto con companyName, AC-08 entrevista con contexto y Badge de estado, AC-09 grupos en orden solo con hits, AC-10 insensible a mayúsculas/acentos, AC-11 máx 8 por grupo, AC-12 clic discovery navega+cierra, AC-13 clic empresa, AC-14 clic contacto→su empresa, AC-15 clic entrevista, AC-16 flechas+Enter, AC-17 estado inicial literal, AC-18 Sin resultados, AC-19 No se pudo buscar.",
    "unit": [
      {
        "file": "tests/unit/search/search.test.ts",
        "test_count": 4,
        "description": "src/main/db/search.ts contra el store REAL inyectado (patrón persistence): normalizeSearchText + coincidencia en ambos sentidos con 'Acmé Córp'↔'acme corp'/'ÁCMÉ' (AC-10); subcadena por tipo con el contexto de navegación resuelto —discoveryName en empresas, companyName+companyId+companyDiscoveryId en contactos, ids anidados+companyName+status en entrevistas— con toEqual exactos (refuerzo AC-05..08); límite de 8 por grupo con 10 coincidencias (AC-11); y query vacía o en blanco → grupos vacíos sin llamar (refuerzo AC-17)."
      },
      {
        "file": "tests/unit/search/GlobalSearch.test.tsx",
        "test_count": 17,
        "description": "TopBar + GlobalSearchDialog con MemoryRouter (TopBar FUERA de las Routes como en el Layout real, probes de destino anidadas) y api.db.search mockeado; debounce 150 ms real → findBy/waitFor. Botón 'Buscar' (accessible name limpio: el kbd ⌘K es aria-hidden) abre con foco en el input —waitFor(toHaveFocus), el autofocus de cmdk funciona en jsdom— (AC-01); fireEvent.keyDown(window) con metaKey y ctrlKey abre (AC-02); Escape cierra sin navegar (AC-03); reabrir → input vacío + mensaje inicial (AC-04); hit por grupo con contexto —LECCIÓN: los nombres de discovery/empresa se repiten como contexto en otros grupos → scoping por [cmdk-group], nunca findByText global— (AC-05..08 con Badge 'Grabada'); solo grupos con hits en el orden fijo vía [cmdk-group-heading] (AC-09); clic en cada tipo navega a la ruta ANIDADA correcta (probes DISCOVERY/COMPANY/COMPANY/INTERVIEW) y cierra (AC-12..15); ArrowDown+Enter navega al seleccionado (AC-16, cmdk coopera en jsdom); estado inicial literal sin llamar al bridge (AC-17); 'Sin resultados' con grupos vacíos (AC-18); y '{ok:false}' → 'No se pudo buscar' dentro del diálogo con la app viva (AC-19)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "atajo-teclado-real": "El atajo ⌘K con el teclado físico de macOS (evento nativo del SO frente al keydown sintético de jsdom) y el aspecto visual del palette (ancho, posición top-20%, scroll de la lista): revisión manual.",
      "volumen-real": "Sensación de latencia con volumen alto de datos reales (el debounce y el límite de 8 están fijados por tests; la percepción es humana)."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::opening and closing::opens the search dialog focusing the input when the \"Buscar\" button is clicked",
      "AC-02": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::opening and closing::opens the dialog on Cmd+K and on Ctrl+K (window-level shortcut)",
      "AC-03": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::opening and closing::closes on Escape without navigating",
      "AC-04": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::opening and closing::reopens with an empty field after closing with text written",
      "AC-05": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::results::shows a matching discovery under the \"Discoveries\" group + refuerzo main en tests/unit/search/search.test.ts::searchGlobal::substring matching with resolved context::matches by substring on each entity type returning the nested navigation context",
      "AC-06": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::results::shows a matching company under \"Empresas\" with its discovery name as context + refuerzo main (mismo it de contexto)",
      "AC-07": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::results::shows a matching contact under \"Contactos\" with its company name as context + refuerzo main (companyDiscoveryId en el hit)",
      "AC-08": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::results::shows a matching interview under \"Entrevistas\" with company context and its status badge + refuerzo main (status en el hit)",
      "AC-09": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::results::renders only the groups with hits, in the fixed order",
      "AC-10": "tests/unit/search/search.test.ts::searchGlobal::normalization::matches names regardless of case and diacritics in both directions",
      "AC-11": "tests/unit/search/search.test.ts::searchGlobal::group limit::caps each group at 8 results",
      "AC-12": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::navigation::navigates to the discovery detail and closes when a discovery hit is clicked",
      "AC-13": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::navigation::navigates to the nested company detail and closes when a company hit is clicked",
      "AC-14": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::navigation::navigates to the contact's company detail and closes when a contact hit is clicked",
      "AC-15": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::navigation::navigates to the nested interview detail and closes when an interview hit is clicked",
      "AC-16": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::navigation::navigates to the arrow-selected result on Enter",
      "AC-17": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::empty and error states::shows the initial hint and no results before typing + refuerzo main en tests/unit/search/search.test.ts::searchGlobal::blank query::returns empty groups for empty or whitespace-only queries",
      "AC-18": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::empty and error states::shows the \"Sin resultados\" empty state for a query without matches",
      "AC-19": "tests/unit/search/GlobalSearch.test.tsx::GlobalSearch::empty and error states::shows \"No se pudo buscar\" inside the dialog when the bridge fails, without breaking the app"
    },
    "notes": "Adaptación de infra (breakage exacto del plan §5): tests/helpers/mockApi.ts añade db.search a createMockDbApi (default sin resultados, overridable). SIN gaps de jsdom: el autofocus de cmdk y las flechas+Enter funcionaron (no hizo falta degradar). Las rutas de destino asertadas son las ANIDADAS reales (desviación documentada del propio código respecto a las planas de la nota técnica). SPEC-020: SearchInterviewHit gana companyId/companyName nullables y discoveryId propio; search.test.ts pasa discoveryId en createInterview (los toEqual existentes ya coinciden con la forma nueva) y los hits sin empresa (contexto 'Sin empresa' + navegación /captures/:id) se cubren en tests/unit/search/search.captures.test.ts y GlobalSearch.captures.test.tsx bajo SPEC-020.",
    "created_at": "2026-07-04",
    "updated_at": "2026-07-10"
  },
  "SPEC-019": {
    "spec_file": "specs/SPEC-019-aviso-consentimiento-grabacion.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (10): aviso antes de grabar ×3 (AC-01 diálogo con recordatorio legal sin arrancar, AC-02 confirmar cierra y arranca, AC-03 Cancelar/Escape cierra sin arrancar ni persistir), desactivar el aviso ×5 (AC-04 casilla marcada + confirmar → preferencia persistida y arranca, AC-05 preferencia activa → arranca sin diálogo, AC-06 la preferencia sobrevive a cerrar/reabrir la app, AC-07 confirmar sin casilla → el aviso vuelve, AC-08 cancelar con casilla marcada → el aviso vuelve, cancelar no persiste), registro de trazabilidad ×2 (AC-09 transcript.json con consent{acknowledgedAt} del inicio, AC-10 spike /capture → consent null sin error).",
    "unit": [
      {
        "file": "tests/unit/consent/ConsentDialog.test.tsx",
        "test_count": 8,
        "description": "ConsentDialog vía InterviewDetailPage → RecordingSection (mocks del spike, arnés SPEC-015; localStorage limpio por test): 'Iniciar grabación' abre el AlertDialog 'Aviso de grabación' con la descripción legal LITERAL, checkbox desmarcado 'No volver a mostrar este aviso' y botones Cancelar/'Entendido, iniciar grabación', SIN arrancar (ni recorder ni recording.start) (AC-01); confirmar cierra el diálogo y arranca el flujo normal —Detener visible, recording.start ×1— (AC-02); Cancelar Y Escape cierran sin arrancar y sin escribir 'maurya:recording-consent-dismissed' (AC-03); casilla marcada + confirmar → localStorage 'true' y arranque (AC-04); preferencia pre-activa → arranque directo sin diálogo, con recording.start('i-1', ISO-8601 estricto) — mitad renderer de AC-09 con el aviso desactivado — (AC-05); preferencia persistida vía UI sobrevive al remontaje (reapertura simulada, patrón SPEC-009 AC-03) y arranca sin diálogo (AC-06); confirmar SIN casilla no persiste y el aviso reaparece en la siguiente grabación (AC-07); cancelar CON casilla marcada no persiste, el aviso reaparece y la casilla vuelve limpia (AC-08). Reparto sin duplicar SPEC-015/016: los asserts de arranque se limitan al mínimo del AC; la firma completa recording.start(id, timestamp ISO con round-trip) al confirmar vive en SPEC-015 AC-04."
      },
      {
        "file": "tests/unit/consent/transcriptionService.consent.test.ts",
        "test_count": 2,
        "description": "transcriptionService en Node (DeepgramConnection mockeada, fs real en temporal, arnés SPEC-002): persistTranscript(wav, null, consent) escribe consent {acknowledgedAt} en el transcript.json junto a lines/latency/assistant sin alterar la forma (AC-09 mitad main); persistTranscript sin tercer argumento (flujo spike /capture) escribe consent: null sin error (AC-10)."
      }
    ],
    "repurposed_tests_note": "El paso por el diálogo dentro del flujo de grabación (confirmar y que la captura arranque con recording.start(id, timestampISO)) está asertado también por las suites de SPEC-015/016 (helper startRecording con heading 'Aviso de grabación' + assert de firma con round-trip ISO en SPEC-015 AC-04), adaptadas el 2026-07-10.",
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "cableado-ipc": "El ciclo de vida de activeConsent en src/main/ipc.ts (recording:start captura el timestamp tras el guard → recording:stop lo consume y resetea en TODOS los caminos, sin filtrarse a la siguiente sesión) vive en handlers de ipcMain, no unit-testeable sin Electron real: la mitad renderer (envío) y la mitad main (persistencia) están automatizadas; el cableado completo start→stop→transcript.json es verificación manual (smoke con la app real).",
      "AC-06": "La reapertura REAL de la app (proceso Electron nuevo releyendo localStorage de la partición) se simula en unit con remontaje; la verificación con cierre/reapertura física de Maurya es manual.",
      "AC-09": "El transcript.json de una grabación real con el timestamp del instante del inicio: automatizadas las dos mitades (envío del ISO y escritura del campo); la lectura del archivo generado por una grabación real es manual."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/consent/ConsentDialog.test.tsx::ConsentDialog (aviso de grabación)::before recording::opens the \"Aviso de grabación\" dialog with the legal reminder on \"Iniciar grabación\" without starting the capture yet",
      "AC-02": "tests/unit/consent/ConsentDialog.test.tsx::ConsentDialog (aviso de grabación)::before recording::closes the dialog and starts the recording with the normal flow on \"Entendido, iniciar grabación\"",
      "AC-03": "tests/unit/consent/ConsentDialog.test.tsx::ConsentDialog (aviso de grabación)::before recording::closes without starting and without persisting any preference on \"Cancelar\" or Escape",
      "AC-04": "tests/unit/consent/ConsentDialog.test.tsx::ConsentDialog (aviso de grabación)::disabling the notice::persists the preference and starts the recording when confirming with the checkbox checked",
      "AC-05": "tests/unit/consent/ConsentDialog.test.tsx::ConsentDialog (aviso de grabación)::disabling the notice::starts directly without the dialog when the do-not-show preference is active",
      "AC-06": "tests/unit/consent/ConsentDialog.test.tsx::ConsentDialog (aviso de grabación)::disabling the notice::keeps the persisted preference active after closing and reopening the app (reapertura real: MANUAL)",
      "AC-07": "tests/unit/consent/ConsentDialog.test.tsx::ConsentDialog (aviso de grabación)::disabling the notice::shows the notice again on the next recording when it was confirmed with the checkbox unchecked",
      "AC-08": "tests/unit/consent/ConsentDialog.test.tsx::ConsentDialog (aviso de grabación)::disabling the notice::shows the notice again with a clean checkbox after cancelling with the checkbox checked (cancel never persists)",
      "AC-09": "tests/unit/consent/transcriptionService.consent.test.ts::transcriptionService (consentimiento SPEC-019)::persists the consent record with its acknowledgedAt timestamp in the transcript.json + tests/unit/recording/RecordingSection.test.tsx::RecordingSection::recording::starts the capture associated to the interview showing chronometer, both meters and a destructive Detener (firma recording.start(id, ISO) con round-trip) + tests/unit/consent/ConsentDialog.test.tsx::ConsentDialog (aviso de grabación)::disabling the notice::starts directly without the dialog when the do-not-show preference is active (envío del ISO con el aviso desactivado; cableado IPC completo: MANUAL)",
      "AC-10": "tests/unit/consent/transcriptionService.consent.test.ts::transcriptionService (consentimiento SPEC-019)::persists consent as null without error when the recording had no acknowledgement (spike flow)"
    },
    "notes": "Entrada creada el 2026-07-10 cerrando el gap detectado durante la QA de SPEC-020 (SPEC-019 se implementó en 199ca08 sin vuelta de QA Dev: las suites de SPEC-015/016 se adaptaron al diálogo ese mismo día). El copy del diálogo y la clave 'maurya:recording-consent-dismissed' se asertan literales (responsabilidad legal). Sin cambios en src/.",
    "created_at": "2026-07-10",
    "updated_at": "2026-07-10"
  },
  "SPEC-020": {
    "spec_file": "specs/SPEC-020-capturas-capture-first.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec — el conteo real es 35 (navegación/listado ×8, nueva captura ×5, detalle ×4, guión sin empresa ×3, asignación ×10, edición/eliminación ×3, integraciones ×2): AC-01 sidebar 'Capturas'→/captures, AC-02 redirects / y /capture→/captures, AC-03 listado global updatedAt desc con fila título+Badge+refs, AC-04 'Sin empresa' como Badge outline, AC-05 filtro 'Sin empresa'+'Limpiar filtros', AC-06 empty state, AC-07 error state+Reintentar, AC-08 Skeletons, AC-09 Dialog nueva captura (Título/Discovery/Plantilla), AC-10 título vacío→error inline, AC-11 discovery vacío→error inline, AC-12 cero discoveries→aviso+link, AC-13 crear→draft companyId null+Toast+navegación, AC-14 detalle cabecera (Volver/h1/Badge/fila muted), AC-15 secciones Grabación→Nota→Guión íntegras, AC-16 asistente sin empresa, AC-17 id inexistente→error+'Volver a Capturas', AC-18 guión sin empresa (template+discovery, sin Empresa/Contacto/Histórico, prepared), AC-19 sin plantilla→no-template sin LLM, AC-20 regenerar tras asignar→contexto completo, AC-21 botón 'Asignar empresa' en detalle, AC-22 acción de fila 'Asignar empresa y contacto', AC-23 Sheet con Selects (empresas del discovery + contacto bloqueado con Tooltip), AC-24 '+ Nueva empresa' inline, AC-25 '+ Nuevo contacto' inline, AC-26 nombre vacío→error sin persistir, AC-27 asignar existente→Toast+cierre+reflejo sin recargar, AC-28 crear-y-asignar en una operación (si falla, nada), AC-29 captura asignada listada bajo su empresa, AC-30 fallo bridge→Sheet abierto+Toast destructive sin estado a medias, AC-31 menú Editar (Contacto solo con empresa)/Eliminar, AC-32 AlertDialog de borrado con copys exactos, AC-33 cascada deleteDiscovery sobre capturas sin empresa, AC-34 búsqueda: hit sin empresa→'Sin empresa'+/captures/:id, AC-35 entrevistas del flujo de empresa en el listado global.",
    "unit": [
      {
        "file": "tests/unit/captures/CapturesPage.test.tsx",
        "test_count": 13,
        "description": "Listado global /captures con window.api mockeado (probes /captures/:id y /discoveries): filas con Link, Badge de estado y refs muted resueltas en el orden que entrega main (AC-03); navegación del título de una entrevista de empresa a /captures/:id (AC-35); 'Sin empresa' como Badge outline (data-variant) solo en filas sin empresa (AC-04); filtro chip 'Sin empresa' + 'Limpiar filtros' con restauración (AC-05, ×2 con el empty por filtro 'No hay capturas sin empresa'); empty global con CTA que abre el Dialog (AC-06); error state con mensaje del bridge y 'Reintentar' que recarga (AC-07); 3+ Skeletons con la promesa pendiente (AC-08); creación capture-first → createInterview {discoveryId, companyId: null, title, templateId} + toast 'Captura creada' + navegación al detalle (AC-13); menú ⋯ con 'Asignar empresa y contacto' SOLO sin empresa abriendo el Sheet (AC-22); 'Editar'/'Eliminar' con Dialog de edición sin campo Contacto sin empresa y CON Contacto precargado con empresa (AC-31, ×2); y AlertDialog 'Eliminar captura' con el copy exacto «título» y sus notas → deleteInterview + fila fuera + toast (AC-32)."
      },
      {
        "file": "tests/unit/captures/NewCaptureDialog.test.tsx",
        "test_count": 4,
        "description": "Dialog 'Nueva captura' aislado (frontera: prop onSubmit; MemoryRouter por el link del aviso): campos Título (placeholder 'Captura dd/mm/aaaa'), Discovery con placeholder y Plantilla default 'Sin template' con etiquetas nombre (fase) (AC-09); 'Campo requerido' bajo Título sin llamar a onSubmit (AC-10); 'Campo requerido' bajo Discovery sin llamar (AC-11); y cero discoveries → Select disabled con 'No hay discoveries' + link 'Crear discovery' a /discoveries (AC-12)."
      },
      {
        "file": "tests/unit/captures/CaptureDetailPage.test.tsx",
        "test_count": 5,
        "description": "Detalle /captures/:id con las secciones REALES montadas (mocks del spike, patrón SPEC-015/016/017) + bridge mockeado: cabecera con 'Volver' → /captures, h1, Badge y fila muted 'discovery · Sin empresa · Sin contacto · Sin template' (AC-14); secciones Grabación → Nota → Guión en ese orden con la Grabación operativa (AC-15 — el comportamiento íntegro de cada sección lo garantizan sus suites SPEC-015/016/017/019, montadas tal cual con {interview, onInterviewUpdated}); error state con link 'Volver a Capturas' (AC-17); botón outline 'Asignar empresa' que abre el Sheet solo sin empresa (con empresa: cabecera resuelta y sin botón) (AC-21); y asignación de empresa existente → assignInterviewCompany + toast 'Empresa asignada' + Sheet cerrado + cabecera reflejada SIN recargar (getInterview una sola vez) (AC-27 UI)."
      },
      {
        "file": "tests/unit/captures/AssignCompanySheet.test.tsx",
        "test_count": 6,
        "description": "Sheet 'Asignar empresa y contacto' aislado (frontera: window.api): descripción con «discovery», Select Empresa con las empresas del discovery + '+ Nueva empresa', Contacto disabled con Tooltip 'Selecciona primero una empresa' (wrapper span tabIndex 0) hasta elegir empresa y luego 'Sin contacto'+contactos+'+ Nuevo contacto' (AC-23); campos inline Nombre/Website/LinkedIn con '+ Nueva empresa' sin segundo modal y contacto reducido a Sin/Nuevo (AC-24); campos inline Nombre/Posición/LinkedIn con '+ Nuevo contacto' (AC-25); nombre de empresa nueva vacío → 'Campo requerido' sin tocar el bridge (AC-26); empresa+contacto nuevos → UNA sola llamada assignInterviewCompany {newCompany, newContact} + onAssigned + toast + cierre (AC-28 UI); y {ok:false} → toast destructive con el Sheet abierto, formulario intacto y reintento posible (AC-30 UI)."
      },
      {
        "file": "tests/unit/persistence/repository.captures.test.ts",
        "test_count": 9,
        "description": "Persistencia SPEC-020 contra el store REAL en temporal: migración v1→v2 (backfill de discoveryId desde la empresa; entrevista con empresa irresoluble eliminada con su nota; schemaVersion 2 persistido); createInterview capture-first (companyId/contactId null, draft) (AC-13 main); invariantes de referencia (empresa∉discovery, contacto sin empresa, contacto de otra empresa) sin persistir nada; updateInterview endurecido (contacto exige empresa); cascada deleteDiscovery sobre capturas sin empresa y sus notas, respetando otros discoveries (AC-33); listAllInterviews con contexto resuelto, nulls defensivos y orden updatedAt desc (AC-03 main); assignInterviewCompany con existentes → entrevista actualizada y listada bajo la empresa (AC-27 main + AC-29); con nuevos → empresa en el discovery de la captura y contacto en esa empresa, todo en una operación (AC-28 main); e inputs inválidos → error tipado con el db.json byte-idéntico (atomicidad, AC-28/AC-30 main)."
      },
      {
        "file": "tests/unit/script/llmService.captures.test.ts",
        "test_count": 3,
        "description": "llmService en Node (SDK mockeado, store real): captura sin empresa → prompt SOLO con '## Discovery' + template (sin '## Empresa'/'## Contacto'/'## Entrevistas anteriores'), system prompt 'al contexto del discovery' y status prepared persistido (AC-18); sin plantilla → no-template sin llamar al SDK (AC-19); y tras assignInterviewCompany real (empresa+contacto nuevos) con entrevista previa de esa empresa → el prompt recupera Empresa/Contacto/Histórico, mantiene '## Discovery' y el system vuelve a 'a la empresa y al contacto concretos' (AC-20)."
      },
      {
        "file": "tests/unit/notes/noteService.captures.test.ts",
        "test_count": 1,
        "description": "noteService en Node (SDK mockeado, store y transcript reales): la nota de una captura sin empresa se genera igual con '## Empresa\\nSin empresa asignada.' y '## Contacto\\nSin contacto asignado.' en el prompt, persistiendo nota y status summarized (refuerzo de AC-15: NoteSection íntegra sin empresa; hallazgo del plan fuera de Notas técnicas)."
      },
      {
        "file": "tests/unit/search/search.captures.test.ts",
        "test_count": 2,
        "description": "searchGlobal en Node contra el store real: hit de captura sin empresa con companyId/companyName null y discoveryId propio (AC-34 main); y omisión defensiva mantenida SOLO para companyId no-null irresoluble (db.json v2 inconsistente escrito a mano) (refuerzo AC-34)."
      },
      {
        "file": "tests/unit/search/GlobalSearch.captures.test.tsx",
        "test_count": 2,
        "description": "TopBar + GlobalSearchDialog (arnés de SPEC-018, probes /captures/:id y anidada): el hit sin empresa muestra 'Sin empresa' como contexto y navega a /captures/:id cerrando el diálogo (AC-34 UI); el hit CON empresa conserva la ruta anidada (refuerzo AC-34)."
      }
    ],
    "repurposed_tests_note": "AC-01/AC-02 se cubren en los tests del shell reescritos por esta spec (tests/unit/layout/Layout.test.tsx y sections.test.tsx, entradas de SPEC-009 actualizadas); AC-16 se cubre con la suite completa de assistantService (SPEC-016), cuyo seed pasa a una captura sin empresa.",
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "AC-15": "Componentes físicos/perceptuales de la 'experiencia completa': grabación con audio real (mic+sistema), transcripción en vivo real de Deepgram, aviso de consentimiento sonoro y export con save dialog real del SO exigen la app real en macOS. El montaje idéntico de las secciones y su orden están automatizados aquí; el comportamiento interno de cada sección está automatizado en sus suites (SPEC-015/016/017/019), que corren sobre los MISMOS componentes montados tal cual.",
      "AC-16": "La sugerencia proactiva del asistente durante una grabación REAL de una captura sin empresa requiere audio y APIs reales; la vía automatizada es la suite de assistantService (SPEC-016) corriendo íntegra sobre una captura sin empresa.",
      "migracion-real": "Migración v1→v2 sobre un db.json real de una instalación existente (smoke del plan): automatizada en unit con un v1 sintético; la verificación sobre datos reales de userData es manual.",
      "responsive": "Cabecera del listado en dos filas en mobile, Sheet a ancho completo y metadata en línea propia: jsdom no hace layout; revisión visual manual."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::sidebar::shows the sidebar with the four section items — \"Capturas\" among them — and the \"Navegación principal\" landmark",
      "AC-02": "tests/unit/layout/Layout.test.tsx::Layout (shell de navegación)::routes::redirects the index route to the Capturas section (SPEC-020 home) + tests/unit/layout/sections.test.tsx::secciones bajo el layout (SPEC-009)::Capturas bajo el layout::redirects the legacy /capture route to the global captures list under the layout",
      "AC-03": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::listing::lists every interview in the system with title link, status badge and the muted refs row + tests/unit/persistence/repository.captures.test.ts::repository (SPEC-020 capturas)::listAllInterviews::returns every interview with resolved names (null for missing refs) ordered by updatedAt descending",
      "AC-04": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::listing::renders \"Sin empresa\" as an outline badge only on rows without company",
      "AC-05": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::listing::filters to companyId-null captures with the \"Sin empresa\" chip and clears with \"Limpiar filtros\" + tests/unit/captures/CapturesPage.test.tsx::CapturesPage::listing::shows the \"No hay capturas sin empresa\" filtered empty state when the filter matches nothing",
      "AC-06": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::listing::shows the empty state with \"Aún no hay capturas\" and a working \"Crear primera captura\" CTA",
      "AC-07": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::listing::shows the error state with the bridge message and recovers on \"Reintentar\"",
      "AC-08": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::listing::shows row skeletons while the list request is pending",
      "AC-09": "tests/unit/captures/NewCaptureDialog.test.tsx::NewCaptureDialog::renders Título (required), Discovery (required) and Plantilla defaulting to \"Sin template\" with name+phase labels",
      "AC-10": "tests/unit/captures/NewCaptureDialog.test.tsx::NewCaptureDialog::shows the inline \"Campo requerido\" error under Título and creates nothing when the title is empty",
      "AC-11": "tests/unit/captures/NewCaptureDialog.test.tsx::NewCaptureDialog::shows the inline \"Campo requerido\" error under Discovery and creates nothing when no discovery is selected",
      "AC-12": "tests/unit/captures/NewCaptureDialog.test.tsx::NewCaptureDialog::disables the Discovery select with \"No hay discoveries\" and links to create one when there are zero discoveries",
      "AC-13": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::creating a capture::creates the capture with discoveryId and companyId null, toasts \"Captura creada\" and navigates to its detail + tests/unit/persistence/repository.captures.test.ts::repository (SPEC-020 capturas)::createInterview capture-first::creates a capture with the chosen discoveryId, null companyId/contactId and draft status",
      "AC-14": "tests/unit/captures/CaptureDetailPage.test.tsx::CaptureDetailPage::header::shows the \"Volver\" back button to /captures, the h1 title, the status badge and the muted refs row with fallbacks",
      "AC-15": "tests/unit/captures/CaptureDetailPage.test.tsx::CaptureDetailPage::sections::mounts the same interview sections in order: Grabación, Nota and Guión + tests/unit/notes/noteService.captures.test.ts::noteService (SPEC-020 capturas)::generates the note for a companyless capture declaring \"Sin empresa asignada.\" in the prompt and persists status summarized (comportamiento interno de cada sección: suites SPEC-015/016/017/019 sobre los mismos componentes; componente físico: MANUAL)",
      "AC-16": "tests/unit/assistant/assistantService.test.ts::assistantService (suite completa de SPEC-016 corriendo sobre una captura SIN empresa — seed SPEC-020): la activación por interviewId, las sugerencias, el feedback y la persistencia funcionan sin empresa asignada (grabación real: MANUAL)",
      "AC-17": "tests/unit/captures/CaptureDetailPage.test.tsx::CaptureDetailPage::error state::shows the error state with a \"Volver a Capturas\" link when the capture id does not exist",
      "AC-18": "tests/unit/script/llmService.captures.test.ts::llmService (SPEC-020 capturas)::generation without a company::generates from the template and the discovery name only, omitting the company/contact/history sections, and sets status prepared",
      "AC-19": "tests/unit/script/llmService.captures.test.ts::llmService (SPEC-020 capturas)::generation without a company::fails with the no-template block without calling the LLM when the capture has no template",
      "AC-20": "tests/unit/script/llmService.captures.test.ts::llmService (SPEC-020 capturas)::regeneration after assigning a company::includes the company, the contact and the company historical context again when regenerating after assignment",
      "AC-21": "tests/unit/captures/CaptureDetailPage.test.tsx::CaptureDetailPage::company assignment::shows the outline \"Asignar empresa\" button opening the Sheet only while the capture has no company",
      "AC-22": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::row actions menu::offers \"Asignar empresa y contacto\" only on unassigned rows and opens the assign Sheet",
      "AC-23": "tests/unit/captures/AssignCompanySheet.test.tsx::AssignCompanySheet::offers the discovery companies plus \"+ Nueva empresa\", and gates the contact select behind choosing a company",
      "AC-24": "tests/unit/captures/AssignCompanySheet.test.tsx::AssignCompanySheet::shows the inline Nombre/Website/LinkedIn fields when \"+ Nueva empresa\" is selected, without a second modal",
      "AC-25": "tests/unit/captures/AssignCompanySheet.test.tsx::AssignCompanySheet::shows the inline Nombre/Posición/LinkedIn fields when \"+ Nuevo contacto\" is selected",
      "AC-26": "tests/unit/captures/AssignCompanySheet.test.tsx::AssignCompanySheet::shows the inline \"Campo requerido\" error and persists nothing when the new company name is empty + tests/unit/persistence/repository.captures.test.ts::repository (SPEC-020 capturas)::assignInterviewCompany::rejects invalid assignments atomically leaving the data file byte-identical (nombre vacío en main)",
      "AC-27": "tests/unit/captures/CaptureDetailPage.test.tsx::CaptureDetailPage::company assignment::assigns an existing company, toasts \"Empresa asignada\", closes the Sheet and refreshes the header without reloading + tests/unit/persistence/repository.captures.test.ts::repository (SPEC-020 capturas)::assignInterviewCompany::assigns an existing company and contact updating the interview, which then lists under that company",
      "AC-28": "tests/unit/captures/AssignCompanySheet.test.tsx::AssignCompanySheet::assigns creating the new company and contact in a single bridge operation and notifies the assignment + tests/unit/persistence/repository.captures.test.ts::repository (SPEC-020 capturas)::assignInterviewCompany::creates the inline company in the capture discovery and the inline contact in that company, assigning both + tests/unit/persistence/repository.captures.test.ts::repository (SPEC-020 capturas)::assignInterviewCompany::rejects invalid assignments atomically leaving the data file byte-identical",
      "AC-29": "tests/unit/persistence/repository.captures.test.ts::repository (SPEC-020 capturas)::assignInterviewCompany::assigns an existing company and contact updating the interview, which then lists under that company (la sección Entrevistas de CompanyDetailPage renderiza listInterviews(companyId): cubierto por SPEC-013 AC-01)",
      "AC-30": "tests/unit/captures/AssignCompanySheet.test.tsx::AssignCompanySheet::keeps the Sheet open with a destructive toast and no half-applied state when the bridge fails + tests/unit/persistence/repository.captures.test.ts::repository (SPEC-020 capturas)::assignInterviewCompany::rejects invalid assignments atomically leaving the data file byte-identical",
      "AC-31": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::row actions menu::includes \"Editar\" and \"Eliminar\" in the menu and opens the edit dialog without a Contacto field for unassigned captures + tests/unit/captures/CapturesPage.test.tsx::CapturesPage::row actions menu::includes the Contacto select preloaded in the edit dialog when the capture has a company",
      "AC-32": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::row actions menu::confirms deletion in the \"Eliminar captura\" AlertDialog with the exact copy and removes the row with its toast",
      "AC-33": "tests/unit/persistence/repository.captures.test.ts::repository (SPEC-020 capturas)::deleteDiscovery cascade::deletes companyless captures and their notes in cascade when their discovery is deleted",
      "AC-34": "tests/unit/search/GlobalSearch.captures.test.tsx::GlobalSearch (SPEC-020 capturas)::shows \"Sin empresa\" as the context of a companyless capture hit and navigates to /captures/:id on click + tests/unit/search/GlobalSearch.captures.test.tsx::GlobalSearch (SPEC-020 capturas)::keeps the nested interview route for hits that do have a company + tests/unit/search/search.captures.test.ts::searchGlobal (SPEC-020 capturas)::emits interview hits without company carrying null companyId/companyName and the interview discoveryId + tests/unit/search/search.captures.test.ts::searchGlobal (SPEC-020 capturas)::still omits interviews whose non-null companyId does not resolve",
      "AC-35": "tests/unit/captures/CapturesPage.test.tsx::CapturesPage::listing::navigates to /captures/:id when the title of a company-flow interview is clicked"
    },
    "notes": "Migración v1→v2 (Notas técnicas, sin AC propio) cubierta en repository.captures.test.ts::v1 to v2 migration; el invariante endurecido de updateInterview (contacto exige empresa) en ::createInterview capture-first::rejects updating a companyless capture with a contactId via updateInterview. Desviación declarada del implementador asumida por los tests: la fila muted del listado omite las partes null y 'Sin empresa' va como Badge outline (wireframe prevalece sobre el literal del AC-03). Breakage presupuestado repuesto: mockApi (+listAllInterviews/+assignInterviewCompany), fixtures de Interview con discoveryId en 8 suites, firmas de createInterview con discoveryId, tests del shell reescritos a 'Capturas'/redirects, seed del asistente sin empresa. Corrección post-run 2026-07-10: el test del empty por filtro de CapturesPage desambigua los DOS botones 'Limpiar filtros' legítimos (barra + empty state) scopeando al contenedor del empty state con within(), asertando que hay exactamente 2 y que el del empty state restaura el listado. SPEC-021 añadió el segmento de coste al final de la fila muted de ambas cabeceras de detalle: los asserts exactos de la fila en CaptureDetailPage.test.tsx pasan a regex (getNodeText conserva el separador final) y el test de AC-14 aserta además 'Sin datos de coste' (captura sin aiUsage). Regresión presupuestada, no bug.",
    "created_at": "2026-07-10",
    "updated_at": "2026-07-10"
  },
  "SPEC-021": {
    "spec_file": "specs/SPEC-021-medicion-control-coste-ia.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (18): medición ×5 (AC-01 guión registra y persiste aiUsage, AC-02 nota suma igual, AC-03 asistente acumula en sesión y vuelca al detener + bloque assistant del transcript, AC-04 llamada fallida no cambia el acumulado, AC-05 entrevista sin aiUsage → 'Sin datos de coste'), visualización ×3 (AC-06 línea 'IA: N llamadas · ~$X' del panel, AC-07 segmento 'IA ~$X' con Tooltip en la cabecera del detalle, AC-08 aiUsage nulo/a cero → sin importe), límite ×7 (AC-09 card de Ajustes persiste + Toast, AC-10 valor inválido → error inline literal, AC-11 límite alcanzado → pausa con aviso y botón, AC-12 Reanudar → analiza y no re-pausa en la sesión, AC-13 pausa no afecta a grabación/transcripción/guión, AC-14 generaciones manuales no bloqueadas, AC-15 sin límite → nunca pausa y mide igual), errores ×3 (AC-16 respuesta sin bloque usage → 0 tokens, AC-17 fallo de medición no rompe la generación, AC-18 ajuste corrupto → sin límite sin crashear).",
    "unit": [
      {
        "file": "tests/unit/ai-cost/aiCost.test.ts",
        "test_count": 4,
        "description": "Módulo puro src/main/aiCost.ts en Node (store real para el registro): tarifa claude-opus-4-8 $5/$25 por MTok con computeCostUsd exacto (refuerzo AC-01..03); roundUpUsd hacia arriba a 2 decimales — la comparación con el límite pausa ANTES de excederlo (refuerzo AC-11); extractUsage defensivo: bloque usage presente → tokens, ausente o malformado → 0 por campo (AC-16); y recordInterviewUsage best-effort: registra el delta (calls default 1, calls explícito para sesiones), y ante fallo (entrevista inexistente) NO lanza, loguea por console.error y no escribe nada (AC-17 — contrato del único punto de registro)."
      },
      {
        "file": "tests/unit/persistence/repository.aiCost.test.ts",
        "test_count": 4,
        "description": "Persistencia en Node con store real: singleton aiCostSettings persiste/lee el límite y null lo limpia (AC-09 main); límites no positivos o no finitos → validationError con el literal exacto sin persistir (AC-10 main); ajuste corrupto (string/objeto malformado/negativo/número suelto) escrito a mano en db.json → getAiCostSettings normaliza a sin límite sin crashear (AC-18); y addInterviewAiUsage acumula campo a campo desde la ausencia SIN tocar updatedAt (el listado de capturas no se reordena) con persistencia en disco (AC-01/AC-03 main)."
      },
      {
        "file": "tests/unit/ai-cost/generation.aiCost.test.ts",
        "test_count": 5,
        "description": "llmService + noteService en Node (SDK mockeado con bloque usage configurable, store real): el guión registra los tokens tras el parseo válido y la Interview devuelta YA incluye aiUsage (AC-01); la nota suma al mismo acumulado (AC-02); una llamada rechazada o con formato inválido no mide nada — el registro va tras el parseo (AC-04); con el límite ya alcanzado la generación manual se ejecuta igualmente y sigue midiendo (AC-14); y una respuesta sin bloque usage contabiliza la llamada con 0 tokens sin romper la generación (AC-16)."
      },
      {
        "file": "tests/unit/ai-cost/assistantService.aiCost.test.ts",
        "test_count": 5,
        "description": "assistantService en Node (arnés SPEC-016: SDK/Deepgram mockeados, transcripción real, fake Date): cada análisis exitoso acumula usage en los eventos 'active', el summary de stopAssistant lo devuelve y persistTranscript lo escribe en el bloque assistant (AC-03); un análisis fallido no acumula (evento de error sin usage, summary a ceros) (AC-04); con persistido+sesión ≥ límite el gate — DESPUÉS de los guards de SPEC-016: con 2 líneas ni pausa — pausa sin llamar al LLM emitiendo UN único evento 'paused' con pauseLimitUsd (AC-11 main); resumeAssistantLimit emite la vuelta (idle sin sugerencia), reanalisis inmediato y NO re-pausa aunque el coste supere el límite de sobra (AC-12 main); y sin límite configurado nunca pausa mientras la medición sigue funcionando (AC-15)."
      },
      {
        "file": "tests/unit/ai-cost/AssistantPanel.aiCost.test.tsx",
        "test_count": 4,
        "description": "Panel del asistente vía InterviewDetailPage (mocks del spike + consentimiento SPEC-019 atravesado): línea 'IA: 4 llamadas · ~$0.12' (testid assistant-usage-line) ausente antes del primer análisis y actualizada tras cada evento con usage (AC-06); evento 'paused' → Alert (testid assistant-paused-alert) con el copy LITERAL 'Límite de coste alcanzado ($1.00). El asistente está en pausa; la grabación y la transcripción continúan.' + botón 'Reanudar asistente', sustituyendo a la sugerencia y conservando la línea de uso (AC-11 UI); click en Reanudar → assistant.resume y el aviso desaparece al volver 'active' (AC-12 UI); y en pausa la transcripción en vivo sigue pintándose, el cronómetro y 'Detener' siguen operativos y el guión sigue legible (AC-13)."
      },
      {
        "file": "tests/unit/ai-cost/AiCostCard.test.tsx",
        "test_count": 3,
        "description": "Card 'Coste de IA' aislada (frontera window.api.db): título, descripción y disclaimer permanentes, precarga del límite persistido en el input (placeholder 'Sin límite') y guardado → setAiCostSettings({limitUsd: 2.5}) + toast 'Ajustes guardados' (AC-09); vacío → {limitUsd: null} y coma decimal es-ES '0,50' → 0.5 (refuerzo AC-09); y '-1'/'gratis' → error inline LITERAL 'Introduce un importe positivo o deja el campo vacío' sin llamar al bridge (AC-10)."
      },
      {
        "file": "tests/unit/ai-cost/detailCost.test.tsx",
        "test_count": 4,
        "description": "AiCostInline en las cabeceras (mocks del spike): InterviewDetailPage con aiUsage → segmento 'IA ~$0.34' (testid interview-ai-cost) y Tooltip con el desglose '6 llamadas · 12.3k tokens entrada · 2.1k tokens salida' (AC-07); CaptureDetailPage comparte el mismo segmento (refuerzo AC-07); entrevista pre-spec sin campo aiUsage → 'Sin datos de coste' sin errores (AC-05); y aiUsage a cero → ningún importe (ni testid ni texto '~$') además del muted (AC-08 — resolución del conflicto AC↔wireframe a favor de los ACs, decisión del plan)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "volcado-ipc (AC-03)": "El cableado de recording:stop en src/main/ipc.ts (recordInterviewUsage(activeInterviewId, summary.usage) ANTES de updateInterview, best-effort) vive en handlers de ipcMain, no unit-testeable sin Electron real: las tres mitades están automatizadas (summary.usage del asistente, recordInterviewUsage con calls explícito, addInterviewAiUsage persistente); el flujo completo grabar→parar→aiUsage en db.json es smoke manual.",
      "AC-17": "El fallo REAL de escritura de db.json durante una generación exitosa (disco lleno/permisos) es un fallo de entorno no reproducible de forma determinista con el store real compartido (rompería también la persistencia del propio guión); el contrato automatizado es que el ÚNICO punto de registro (recordInterviewUsage) jamás lanza y loguea a consola.",
      "coste-real": "La correspondencia entre el coste estimado (~$) y la factura real de Anthropic es orientativa por diseño (disclaimer de la card): verificación humana contra la consola de Anthropic."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/ai-cost/generation.aiCost.test.ts::generation cost measurement (SPEC-021)::records the script generation tokens and persists the interview aiUsage accumulate + tests/unit/persistence/repository.aiCost.test.ts::repository (coste de IA SPEC-021)::addInterviewAiUsage::accumulates the usage field by field from an absent aiUsage without touching updatedAt",
      "AC-02": "tests/unit/ai-cost/generation.aiCost.test.ts::generation cost measurement (SPEC-021)::adds the note generation usage to the same interview accumulate as the script",
      "AC-03": "tests/unit/ai-cost/assistantService.aiCost.test.ts::assistantService (coste de IA SPEC-021)::accumulates the usage of each successful analysis in the session, returns it in the stop summary and persists it in the transcript assistant block + tests/unit/ai-cost/aiCost.test.ts::aiCost::records the usage delta on the interview and never throws on failure, logging to console instead (volcado con calls explícito; cableado recording:stop: MANUAL)",
      "AC-04": "tests/unit/ai-cost/generation.aiCost.test.ts::generation cost measurement (SPEC-021)::leaves the accumulate untouched when the LLM call fails + tests/unit/ai-cost/assistantService.aiCost.test.ts::assistantService (coste de IA SPEC-021)::does not accumulate usage when the analysis call fails",
      "AC-05": "tests/unit/ai-cost/detailCost.test.tsx::detail header AI cost (SPEC-021)::shows \"Sin datos de coste\" without errors for a pre-spec interview with no aiUsage field",
      "AC-06": "tests/unit/ai-cost/AssistantPanel.aiCost.test.tsx::AssistantPanel (coste de IA SPEC-021)::shows the discreet session line \"IA: N llamadas · ~$X.XX\" updated after each analysis",
      "AC-07": "tests/unit/ai-cost/detailCost.test.tsx::detail header AI cost (SPEC-021)::shows \"IA ~$0.34\" in the interview header refs row with a tooltip breaking down calls and tokens + tests/unit/ai-cost/detailCost.test.tsx::detail header AI cost (SPEC-021)::shows the same cost segment in the capture detail header",
      "AC-08": "tests/unit/ai-cost/detailCost.test.tsx::detail header AI cost (SPEC-021)::renders no amount at all when aiUsage exists but is at zero",
      "AC-09": "tests/unit/ai-cost/AiCostCard.test.tsx::AiCostCard::renders the \"Coste de IA\" card preloading the persisted limit and saves it with the \"Ajustes guardados\" toast + tests/unit/ai-cost/AiCostCard.test.tsx::AiCostCard::saves an empty field as no limit and normalizes the Spanish decimal comma + tests/unit/persistence/repository.aiCost.test.ts::repository (coste de IA SPEC-021)::aiCostSettings::persists the limit singleton and reads it back, with null clearing the limit",
      "AC-10": "tests/unit/ai-cost/AiCostCard.test.tsx::AiCostCard::shows the exact inline error and persists nothing for non-numeric or negative values + tests/unit/persistence/repository.aiCost.test.ts::repository (coste de IA SPEC-021)::aiCostSettings::rejects non-positive or non-finite limits with the exact validation message persisting nothing",
      "AC-11": "tests/unit/ai-cost/assistantService.aiCost.test.ts::assistantService (coste de IA SPEC-021)::pauses the assistant when persisted+session cost reaches the limit, after the SPEC-016 triggers and emitting a single paused event + tests/unit/ai-cost/AssistantPanel.aiCost.test.tsx::AssistantPanel (coste de IA SPEC-021)::replaces the suggestion with the paused alert showing the exact limit copy and the \"Reanudar asistente\" button + tests/unit/ai-cost/aiCost.test.ts::aiCost::rounds up to 2 decimals for the limit comparison",
      "AC-12": "tests/unit/ai-cost/assistantService.aiCost.test.ts::assistantService (coste de IA SPEC-021)::resumes analyses after resumeAssistantLimit and never re-pauses in the same session even beyond the limit + tests/unit/ai-cost/AssistantPanel.aiCost.test.tsx::AssistantPanel (coste de IA SPEC-021)::calls assistant.resume on \"Reanudar asistente\" and clears the alert when the assistant becomes active again",
      "AC-13": "tests/unit/ai-cost/AssistantPanel.aiCost.test.tsx::AssistantPanel (coste de IA SPEC-021)::keeps the recording, the live transcription and the visible script untouched while paused by limit (main: el gate solo omite llamadas al LLM — verificado en el test de AC-11 de assistantService)",
      "AC-14": "tests/unit/ai-cost/generation.aiCost.test.ts::generation cost measurement (SPEC-021)::runs the manual generation even when the interview cost already reached the configured limit",
      "AC-15": "tests/unit/ai-cost/assistantService.aiCost.test.ts::assistantService (coste de IA SPEC-021)::never pauses by cost without a configured limit while the measurement keeps working",
      "AC-16": "tests/unit/ai-cost/aiCost.test.ts::aiCost::extracts the token usage from the response and degrades to 0 tokens when the usage block is missing or malformed + tests/unit/ai-cost/generation.aiCost.test.ts::generation cost measurement (SPEC-021)::counts the call with 0 tokens without breaking the generation when the response has no usage block",
      "AC-17": "tests/unit/ai-cost/aiCost.test.ts::aiCost::records the usage delta on the interview and never throws on failure, logging to console instead (fallo real de disco durante una generación: MANUAL)",
      "AC-18": "tests/unit/persistence/repository.aiCost.test.ts::repository (coste de IA SPEC-021)::aiCostSettings::normalizes a corrupt or unreadable persisted setting to \"no limit\" without crashing"
    },
    "notes": "Breakage presupuestado repuesto: mockApi (+getAiCostSettings con default seguro/+setAiCostSettings/+assistant.resume), toEqual del summary y del transcript de assistantService.test.ts con usage a ceros, SettingsPage AC-13 con 3 botones Guardar (los 2 de claves deshabilitados), asserts exactos de la fila muted de CaptureDetailPage → regex (el segmento de coste añade un separador final). Decisión del plan asumida por los tests: 'Sin datos de coste' como texto muted (mandan los ACs sobre el wireframe, Riesgo 1). SPEC-023 amplió extractUsage a 4 campos (componentes de caché): el test de AC-16 de aiCost.test.ts se repuso con la forma nueva (incluye el caso null del SDK), sin debilitar lo que garantizaba; computeCostUsd/recordInterviewUsage retrocompatibles → resto de la suite intacta.",
    "created_at": "2026-07-10",
    "updated_at": "2026-07-10"
  },
  "SPEC-022": {
    "spec_file": "specs/SPEC-022-degradacion-sin-diarizacion.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (10): fallback ×5 (AC-01 intento único sin diarización al agotar los reintentos never-opened, AC-02 fallback abre → speaker null + status degradado, AC-03 fallback falla → rendición actual con mensaje intacto, AC-04 fallo auth → sin fallback, AC-05 stop degradado → transcript.json con la forma actual), visibilidad ×3 (AC-06 Alert informativo con copy literal persistente en sesión, AC-07 líneas con etiquetas genéricas sin errores, AC-08 sesión normal → el Alert nunca aparece), asistencia/nota ×2 (AC-09 asistente con líneas sin índice de hablante y sugerencias con normalidad, AC-10 nota generada y export con etiquetas genéricas).",
    "unit": [
      {
        "file": "tests/unit/degradation/transcriptionService.degraded.test.ts",
        "test_count": 6,
        "description": "Fallback en Node (arnés SPEC-002: DeepgramConnection mockeada CAPTURANDO las opciones del constructor; classifyConnectionFailure configurable): reintentos never-opened agotados → 3ª conexión con {diarize:false} y la misma key, con el status 'Reintentando…' intacto, el reintento intermedio aún con diarización y SIN emitir la rendición durante el intento silencioso (AC-01); el fallback abre → evento EXACTO {status:'active', degraded:true} y resultados fluyendo con speaker null (AC-02); el fallback también cae → rendición con el mensaje LITERAL actual y shape EXACTO {status, error} sin campo degraded (SPEC-022-iter-1), 3 instancias en total y ningún evento de la sesión con degraded:true (AC-03); clasificación 'auth' → AUTH_ERROR literal, UNA sola instancia (ni reintento ni fallback) y ningún evento degraded (AC-04); persistTranscript de la sesión degradada → forma ACTUAL exacta (keys lines/latency/assistant/consent) con speaker null (AC-05); y sesión normal → shape exacto {status} sin campo degraded en NINGÚN evento (AC-08 main). La semántica sesión-vs-conexión (sesión que ABRIÓ y cae NO dispara fallback) queda fijada por el test existente de SPEC-002 AC-11 (2 instancias y rendición) — referenciado, no duplicado."
      },
      {
        "file": "tests/unit/degradation/deepgramService.degraded.test.ts",
        "test_count": 2,
        "description": "URL de conexión con WebSocket global stubbeado (arnés SPEC-002): {diarize:false} → URL sin 'diarize' conservando encoding/sample_rate/channels/multichannel/interim_results/language y la auth por subprotocolo (refuerzo AC-01); sin opciones → URL histórica con diarize=true BYTE A BYTE (igualdad exacta, no toContain) — garantía de compatibilidad del builder (refuerzo AC-01)."
      },
      {
        "file": "tests/unit/degradation/RecordingSection.degraded.test.tsx",
        "test_count": 4,
        "description": "Sección Grabación vía InterviewDetailPage (mocks del spike + consentimiento SPEC-019 atravesado): evento {status:'active', degraded:true} → Alert testid transcription-degraded-alert con el copy LITERAL 'Transcribiendo sin atribución de hablante. La transcripción y el asistente siguen funcionando.', no destructivo, persistente ante más eventos y AUSENTE tras Detener (AC-06); líneas finales degradadas (speaker null) pintadas sin huecos ni chip 'Hablante N' con la grabación operativa (AC-07 — las etiquetas 'Tú'/'Interlocutor N' del Sheet y el export las fijan las suites de SPEC-017); y sesión normal (status sin degraded, speaker 0 → 'Hablante 1') sin que el Alert aparezca nunca (AC-08 UI). Refuerzo SPEC-022-iter-1: la secuencia real de rendición tras fallback fallido (reintento + 'No se pudo restablecer…', ambos sin degraded) muestra SOLO el error de conexión, jamás el Alert de modo degradado, con la grabación en curso."
      },
      {
        "file": "tests/unit/degradation/assistantService.degraded.test.ts",
        "test_count": 1,
        "description": "Asistente en Node (arnés SPEC-016) con las 3 líneas finales inyectadas con speaker null por la vía pública: el prompt usa el formato existente '[mic] texto'/'[system] texto' SIN sufijo ' sN' (regex negativa) y la sugerencia llega con normalidad en el evento 'active' (AC-09)."
      },
      {
        "file": "tests/unit/degradation/noteService.degraded.test.ts",
        "test_count": 1,
        "description": "Nota/export en Node (arnés SPEC-017) sobre un transcript degradado real (todas las líneas speaker null): la generación funciona (status summarized + nota ensamblada), el prompt etiqueta '[Tú]'/'[Interlocutor 1]' y el export produce '**Tú:** …' / '**Interlocutor 1:** …' una línea por intervención (AC-10)."
      }
    ],
    "breakage_note": "Verificado el 'cero roturas' presupuestado por el plan: npx tsc -p tsconfig.test.json --noEmit salió limpio ANTES de añadir esta suite (el 3er argumento opcional del constructor no rompe los mocks de 2 argumentos) y las decisiones de diseño que protegen los tests existentes (degraded OMITIDO cuando es false; fallback condicionado a everOpened de sesión) se asertan explícitamente aquí (AC-08 main) y quedan guardadas por el test de SPEC-002 AC-11.",
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "rechazo-real": "Forzar el rechazo real del handshake de Deepgram por la diarización (plan/modelo sin soporte) exige una cuenta real con esa limitación: el smoke del plan (Alert + 'Interlocutor 1' + transcript.json + nota tras detener) es humano; la orquestación del fallback está íntegramente automatizada con la conexión mockeada.",
      "aspecto-visual": "Colocación del Alert junto a los avisos de conexión existentes y su comportamiento responsive: jsdom no hace layout; revisión visual manual."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/degradation/transcriptionService.degraded.test.ts::transcriptionService (degradación SPEC-022)::attempts exactly one extra connection without diarization after exhausting the never-opened retries + tests/unit/degradation/deepgramService.degraded.test.ts::DeepgramConnection (degradación SPEC-022)::builds the fallback URL without diarize=true keeping every other stream parameter and the subprotocol auth + tests/unit/degradation/deepgramService.degraded.test.ts::DeepgramConnection (degradación SPEC-022)::keeps the historic diarized URL byte-identical when no options are passed (sesión que llegó a abrir NO dispara el fallback: tests/unit/spike-transcription/transcriptionService.test.ts::transcriptionService::when the connection drops while transcribing::retries once automatically and, if the retry fails, gives up keeping the lines already received)",
      "AC-02": "tests/unit/degradation/transcriptionService.degraded.test.ts::transcriptionService (degradación SPEC-022)::continues degraded when the fallback opens: active status carries degraded true and lines flow with speaker null (speaker null sin words diarizadas: fijado por tests/unit/spike-diarization/deepgramService.diarization.test.ts)",
      "AC-03": "tests/unit/degradation/transcriptionService.degraded.test.ts::transcriptionService (degradación SPEC-022)::gives up with the current literal message and exact shape when the no-diarization attempt also fails (contrato SPEC-022-iter-1: shape exacto {status, error} SIN campo degraded + assert de que ningún evento de la sesión llevó degraded:true)",
      "AC-04": "tests/unit/degradation/transcriptionService.degraded.test.ts::transcriptionService (degradación SPEC-022)::does not try the fallback on an auth-classified failure, keeping the current auth message",
      "AC-05": "tests/unit/degradation/transcriptionService.degraded.test.ts::transcriptionService (degradación SPEC-022)::persists the transcript with the current shape (speaker null lines) when a degraded session stops",
      "AC-06": "tests/unit/degradation/RecordingSection.degraded.test.tsx::RecordingSection (degradación SPEC-022)::shows the informative degraded alert with the literal copy while the degraded session lasts and hides it when the recording stops + tests/unit/degradation/RecordingSection.degraded.test.tsx::RecordingSection (degradación SPEC-022)::does not show the degraded alert when the fallback surrender arrives without the degraded mark (refuerzo SPEC-022-iter-1: el Alert solo en modo degradado real)",
      "AC-07": "tests/unit/degradation/RecordingSection.degraded.test.tsx::RecordingSection (degradación SPEC-022)::renders degraded final lines with the generic labels and no speaker chip, without errors + REFERENCIAS: tests/unit/spike-diarization/TranscriptLine.diarization.test.tsx::TranscriptLine (diarización)::when the line has no speaker information::renders no speaker label when speaker is null, keeping only the source badge; etiquetas \"Tú\"/\"Interlocutor N\" del Sheet/export en las suites de SPEC-017 (NoteSection AC-17, noteService AC-18/AC-20)",
      "AC-08": "tests/unit/degradation/RecordingSection.degraded.test.tsx::RecordingSection (degradación SPEC-022)::never shows the degraded alert during a normal session with working diarization + tests/unit/degradation/transcriptionService.degraded.test.ts::transcriptionService (degradación SPEC-022)::emits the exact current event shape without any degraded field during a normal session (las suites de SPEC-002/003/015 ya asertan igualdad exacta de los eventos de sesión normal)",
      "AC-09": "tests/unit/degradation/assistantService.degraded.test.ts::assistantService (degradación SPEC-022)::builds the LLM prompt without speaker indices for speaker-null lines and keeps delivering suggestions",
      "AC-10": "tests/unit/degradation/noteService.degraded.test.ts::noteService (degradación SPEC-022)::generates the note from a degraded recording and exports the transcript with the generic speaker labels"
    },
    "notes": "Cero reposiciones necesarias (breakage presupuestado del plan verificado como cero). Suites nuevas en tests/unit/degradation/ reutilizando los arneses de spike-transcription (mock de DeepgramConnection ampliado localmente para capturar el 3er argumento options), SPEC-016 y SPEC-017. Sin cambios en tests/helpers/mockApi.ts. SPEC-022-iter-1 (fix 5596ae4, originado por el gap #1 de esta QA): emitStatus exige ahora degraded && everOpened — la suite se actualizó al contrato corregido (AC-03 sin campo degraded en la rendición + test renderer nuevo) y se pasó prettier a tests/unit/degradation/ (2 warnings nuevos resueltos).",
    "created_at": "2026-07-10",
    "updated_at": "2026-07-10",
    "iteration_spec_file": "specs/SPEC-022-iter-1-degradacion-sin-diarizacion.md"
  },
  "SPEC-023": {
    "spec_file": "specs/SPEC-023-optimizacion-latencia-asistente.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec (10): caché del prefijo ×3 (AC-01 bloque fijo con cache_control ephemeral y solo la ventana fuera, AC-02 prefijo byte a byte idéntico entre llamadas, AC-03 guión/nota sin cambios), salida acotada ×3 (AC-04 maxLength en el schema + brevedad en el prompt, AC-05 la sugerencia conserva la estructura visible del panel, AC-06 max_tokens 1024→512), coste con caché ×2 (AC-07 tarifas reales 1,25×/0,1× e inputTokens = suma de los 3 componentes, AC-08 sin campos de caché → cálculo retrocompatible), validación de latencia ×2 (AC-09 mediana re-medida < 5 s registrada en MEMORY.md — MANUAL instrumentado, AC-10 mediana ≥ 5 s → escalar el cambio de modelo al humano).",
    "unit": [
      {
        "file": "tests/unit/latency/assistantService.latency.test.ts",
        "test_count": 5,
        "description": "assistantService en Node (arnés SPEC-016): system como ARRAY de bloques con cache_control {type:'ephemeral'} SOLO en el último — instrucciones en el 1º; objetivos numerados 0-based SIN estado [cubierto|pendiente] + guión en el 2º; la ventana de conversación fuera del prefijo y presente solo en el user (AC-01); dos análisis de la misma sesión → system BYTE A BYTE idéntico (JSON.stringify) aunque objectivesMet y la conversación cambien, con el estado dinámico compacto en el user ('## Objetivos ya cubiertos (índices)' con 'ninguno'→'0') y las secciones variables históricas intactas (Conversación reciente/Tu sugerencia anterior/Tarea) (AC-02); sin objetivos ni guión → un único bloque cacheado y sección de índices omitida (refuerzo AC-01/02); schema enviado con maxLength 200/140 y el prompt instruyendo LOS MISMOS topes ('UNA sola frase corta (máximo 140 caracteres)') (AC-04); y max_tokens === 512 (AC-06)."
      },
      {
        "file": "tests/unit/latency/aiCost.cache.test.ts",
        "test_count": 4,
        "description": "aiCost en Node con store real: constantes CACHE_WRITE 6.25/CACHE_READ 0.5 y computeCostUsd con 4 tarifas, retrocompatible con 2 argumentos (idéntico a los defaults 0) (AC-07/AC-08); recordInterviewUsage pliega los componentes en inputTokens (suma de los 3) y tarifica el caché con SUS tarifas — assert explícito de que el coste ≠ tarificar el total plegado a $5/MTok (AC-07); sin campos de caché el registro es byte-idéntico al histórico (AC-08); y la DESVIACIÓN DECLARADA del implementador: estimatedCostUsd explícito calculado por componentes (camino recording:stop, AiUsage ya plegado) se respeta sin recomputar (AC-07)."
      },
      {
        "file": "tests/unit/latency/generation.nocache.test.ts",
        "test_count": 1,
        "description": "llmService + noteService en Node (SDK mockeado, store real): las llamadas de guión y nota quedan SIN cambios — system como string, ningún cache_control en los params serializados y max_tokens histórico 16000 en ambas (AC-03)."
      }
    ],
    "repurposed_tests_note": "El test de extractUsage de SPEC-021 (tests/unit/ai-cost/aiCost.test.ts::aiCost::extracts the token usage from the response and degrades to 0 tokens when the usage block is missing or malformed) se repuso con la forma de 4 campos incluyendo el caso null del SDK — cubre la mitad de extracción de AC-07/AC-08. AC-05 (estructura visible del panel) queda garantizado por las suites existentes de SPEC-016 (AssistantPanel AC-05: badge+pregunta+razón; AC-08: chips de alarma) — la optimización no toca el renderer.",
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "AC-09": "Re-medición instrumentada del cierre: protocolo del baseline documentado en docs/MEMORY.md (2026-07-10) — app dev con --remote-debugging-port, listeners CDP sobre assistant:update, guion de ~10 frases con say por altavoces, ≥3 ciclos analyzing→active, mediana de los deltas. Criterio: mediana < 5 s + cache_read_input_tokens > 0 en la 2ª+ llamada; registrar los números en MEMORY.md. No automatizable en unit (latencia real de APIs y audio).",
      "AC-10": "Decisión de escalado: si la mediana re-medida no baja de 5 s, el cambio de modelo del asistente se ESCALA al humano (contradice la elección de modelo de CLAUDE.md) — es una regla de proceso para el orquestador, no un comportamiento de código testeable.",
      "cache-real": "El acierto real del caché (mínimo cacheable ~1024 tokens de prefijo en Opus; con guiones cortos puede no aplicar — degradación silenciosa, riesgo 2 del plan) solo se observa contra la API real durante la re-medición de AC-09."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/latency/assistantService.latency.test.ts::assistantService (latencia SPEC-023)::sends the fixed prefix (instructions + script/objectives) as system blocks with ephemeral cache_control only on the last block, keeping the transcript window out + tests/unit/latency/assistantService.latency.test.ts::assistantService (latencia SPEC-023)::omits the covered-objectives section and caches the single instructions block when the interview has no objectives nor script",
      "AC-02": "tests/unit/latency/assistantService.latency.test.ts::assistantService (latencia SPEC-023)::keeps the cached prefix byte-identical across two analyses while only the variable user message changes",
      "AC-03": "tests/unit/latency/generation.nocache.test.ts::script and note generation (no-cache SPEC-023)::keeps the script and note calls unchanged: string system, no cache_control anywhere and their historic max_tokens",
      "AC-04": "tests/unit/latency/assistantService.latency.test.ts::assistantService (latencia SPEC-023)::declares maxLength 200/140 in the structured output schema sent and instructs the same brevity limits in the prompt",
      "AC-05": "GARANTIZADO por las suites existentes de SPEC-016 (la optimización no toca el renderer): tests/unit/assistant/AssistantPanel.test.tsx::AssistantPanel::the suggestion::shows exactly the action badge, the suggested question and the one-line reason — nothing else + ::shows the alarm chips with their Spanish labels when alarms are detected",
      "AC-06": "tests/unit/latency/assistantService.latency.test.ts::assistantService (latencia SPEC-023)::sends max_tokens 512 for the assistant analysis calls",
      "AC-07": "tests/unit/latency/aiCost.cache.test.ts::aiCost (prompt caching SPEC-023)::computes the cost with the four rates and stays byte-compatible with the historic 2-argument calls + tests/unit/latency/aiCost.cache.test.ts::aiCost (prompt caching SPEC-023)::folds the cache components into inputTokens and prices them with their own rates when recording + tests/unit/latency/aiCost.cache.test.ts::aiCost (prompt caching SPEC-023)::respects an explicit estimatedCostUsd computed by components instead of recomputing from folded tokens (desviación declarada) + tests/unit/ai-cost/aiCost.test.ts::aiCost::extracts the token usage from the response and degrades to 0 tokens when the usage block is missing or malformed (extracción de los 4 campos)",
      "AC-08": "tests/unit/latency/aiCost.cache.test.ts::aiCost (prompt caching SPEC-023)::keeps the historic recording untouched when the response carries no cache fields + tests/unit/latency/aiCost.cache.test.ts::aiCost (prompt caching SPEC-023)::computes the cost with the four rates and stays byte-compatible with the historic 2-argument calls (retrocompatibilidad de 2 args) + tests/unit/ai-cost/aiCost.test.ts::aiCost::extracts the token usage from the response and degrades to 0 tokens when the usage block is missing or malformed (campos de caché null/ausentes → 0)",
      "AC-09": "MANUAL: re-medición instrumentada con el protocolo del baseline (docs/MEMORY.md 2026-07-10) — mediana de ≥3 ciclos analyzing→active < 5 s, verificando cache_read_input_tokens > 0; registro en MEMORY.md. No reproducible en unit (latencia real de Claude/Deepgram/audio).",
      "AC-10": "MANUAL/proceso: si la mediana ≥ 5 s, la decisión de cambiar el modelo se escala al humano (regla para el orquestador; contradiría CLAUDE.md tomarla aquí). Sin comportamiento de código que testear."
    },
    "notes": "Breakage presupuestado verificado y repuesto: SOLO el test de extractUsage de tests/unit/ai-cost/aiCost.test.ts (toEqual exacto de 2 campos → 4 campos con caso null; rotura de runtime, no de tipos — tsc ya salía limpio). El resto de la suite de SPEC-021 pasa sin cambios (coste de sesión recomputado desde componentes, computeCostUsd retrocompatible, user string con conversación intacta — verificado contra assistantService.aiCost.test.ts y assistantService.degraded.test.ts).",
    "created_at": "2026-07-10",
    "updated_at": "2026-07-10"
  },
  "SPEC-024": {
    "spec_file": "specs/SPEC-024-empaquetado-hardening-final.md",
    "ac_numbering_note": "ACs numerados por orden de aparición en la spec — el conteo real es 11 (entitlements/firma ×3, versión/metadatos ×2, hardening UI ×2, verificación del bundle ×4): AC-01 device.audio-input + allow-jit en el plist, AC-02 eliminación de allow-unsigned-executable-memory y allow-dyld-environment-variables, AC-03 firma ad-hoc activa con el camino a Developer ID/notarización documentado en comentarios, AC-04 version 1.0.0, AC-05 LSApplicationCategoryType en el MAP extendInfo con las TCC intactas, AC-06 guard de doble submit en AiCostCard, AC-07 observaciones menores aceptadas sin cambio (documental), AC-08 build:mac produce DMG/ZIP 1.0.0 arm64, AC-09 Info.plist del bundle con las 3 claves, AC-10 codesign ad-hoc válido con entitlements embebidos = plist, AC-11 humo: la app abre con la UI de Capturas.",
    "unit": [
      {
        "file": "tests/unit/packaging/hardening.test.ts",
        "test_count": 5,
        "description": "Config del repo como fixtures (fs + texto, convención de packagingConfig.test.ts de SPEC-005 — decisión deliberada: unit estable mejor que MANUAL para archivos commiteados): plist con device.audio-input y allow-jit adyacentes a <true/> (AC-01); sin los dos entitlements del scaffold (AC-02); separación líneas ACTIVAS vs COMENTARIOS del yml — ad-hoc activo (identity: null/notarize: false), 'notarize: true' y 'Developer ID Application' SOLO en comentarios, con APPLE_ID/APPLE_APP_SPECIFIC_PASSWORD/APPLE_TEAM_ID documentadas (AC-03); version 1.0.0 exacta + artifactName derivado sin literal 0.1.0 (AC-04); y LSApplicationCategoryType como entrada de MAP en extendInfo (no lista — regresión conocida de SPEC-005) con ambas descripciones TCC intactas (AC-05)."
      },
      {
        "file": "tests/unit/ai-cost/AiCostCard.submitGuard.test.tsx",
        "test_count": 1,
        "description": "Guard de doble submit (AC-06, cierra la observación de QA de SPEC-021): con setAiCostSettings EN VUELO (promesa controlada) el botón Guardar queda disabled con spinner inline (.animate-spin); los reintentos de click (userEvent y fireEvent directo) no producen un segundo envío (setAiCostSettings ×1 con {limitUsd: 2.5}); al resolver, toast 'Ajustes guardados' y botón operativo de nuevo sin spinner."
      },
      {
        "file": "tests/unit/packaging/afterPack.test.ts",
        "test_count": 3,
        "description": "SPEC-024-iter-1 (hook afterPack que sella el bundle con firma ad-hoc real) — FALLBACK PACTADO APLICADO tras el run 2026-07-10T16:34Z (vi.mock('child_process') no intercepta el require del CJS bajo vite-node; los 2 tests del camino darwin ejecutaban codesign real y se eliminaron): quedan las verificaciones estables sin mock — línea ACTIVA 'afterPack: build/afterPack.js' en electron-builder.yml (no comentario); build/afterPack.js carga como CommonJS y exporta función; y guard de plataforma no-op fuera de darwin (contexto win32/linux mínimo A PROPÓSITO sin appOutDir/packager: si el guard no cortara, lanzaría antes de devolver)."
      }
    ],
    "e2e": [],
    "e2e_note": "e2e no aplica: app Electron local sin public link (decisión humana 2026-07-03); verificación end-to-end manual",
    "manual_verification": {
      "AC-07": "Documental: el drift de céntimos por suma de deltas en estimatedCostUsd y el desglose del Tooltip con caché plegado quedan ACEPTADOS SIN CAMBIO en la propia spec (sección Decisiones asumidas) — no hay código que testear; las suites de SPEC-021/023 siguen fijando el comportamiento vigente sin modificaciones.",
      "AC-08": "Ejecución del orquestador tras esta QA (plan §4): npm run build:mac → dist/Maurya-1.0.0.dmg + .zip arm64 sin errores con typecheck previo en verde (electron-builder tarda minutos; no reproducible en unit).",
      "AC-09": "Ejecución del orquestador: plutil -p sobre el Info.plist del .app generado → NSMicrophoneUsageDescription, NSAudioCaptureUsageDescription y LSApplicationCategoryType presentes (la mitad de configuración está automatizada en el test de AC-05).",
      "AC-10": "Ejecución del orquestador: codesign --verify + codesign -d --entitlements - sobre el .app → firma ad-hoc válida y entitlements embebidos = build/entitlements.mac.plist (la mitad de configuración está automatizada en los tests de AC-01/AC-02). SPEC-024-iter-1: la re-verificación tras el fix del sello (codesign --verify --deep --strict OK, Signature=adhoc, recursos sellados, entitlements embebidos = plist) la ejecuta el orquestador; la orquestación del hook está automatizada en tests/unit/packaging/afterPack.test.ts.",
      "AC-11": "Ejecución del orquestador: open del .app → humo con la ventana de Capturas; los prompts TCC atribuidos a 'Maurya' y el flujo completo empaquetado quedan como verificación humana final (ya listada en MEMORY). La UI de Capturas en sí está automatizada por las suites de SPEC-020.",
      "ITER-1-camino-darwin": "Orquestación darwin del hook (firma con entitlements + verificación + fallo→build rojo): NO automatizable en unit sin tocar build/afterPack.js — vi.mock('child_process') no intercepta el require de un CJS cargado por vite-node (test-defect de infraestructura confirmado por el tester, run 2026-07-10T16:34Z; los tests mockeados ejecutaban codesign real contra /tmp). Cobertura trasladada a la verificación del orquestador sobre el bundle real (2026-07-10): hook ejecutado en npm run build:mac, codesign --verify --deep --strict sin errores, Signature=adhoc, recursos sellados, entitlements embebidos coincidentes con build/entitlements.mac.plist."
    },
    "acceptance_criteria_coverage": {
      "AC-01": "tests/unit/packaging/hardening.test.ts::packaging hardening (SPEC-024)::entitlements for real signing::declares device.audio-input and keeps allow-jit in build/entitlements.mac.plist",
      "AC-02": "tests/unit/packaging/hardening.test.ts::packaging hardening (SPEC-024)::entitlements for real signing::removes the scaffold entitlements that weaken the hardened runtime",
      "AC-03": "tests/unit/packaging/hardening.test.ts::packaging hardening (SPEC-024)::signing path documentation::keeps ad-hoc signing active while documenting the Developer ID / notarization path as comments",
      "AC-04": "tests/unit/packaging/hardening.test.ts::packaging hardening (SPEC-024)::MVP version and metadata::bumps package.json version to 1.0.0 (MVP artifacts become Maurya-1.0.0.*)",
      "AC-05": "tests/unit/packaging/hardening.test.ts::packaging hardening (SPEC-024)::MVP version and metadata::declares LSApplicationCategoryType in the extendInfo MAP keeping both TCC descriptions intact",
      "AC-06": "tests/unit/ai-cost/AiCostCard.submitGuard.test.tsx::AiCostCard (guard de doble submit SPEC-024)::disables Guardar with an inline spinner while the save is in flight, ignores extra submits and re-enables on resolve",
      "AC-07": "MANUAL/documental: aceptación sin cambio registrada en la spec (Decisiones asumidas); sin código que testear — las suites de SPEC-021/023 fijan el comportamiento vigente.",
      "AC-08": "MANUAL/orquestador: npm run build:mac → dist/Maurya-1.0.0.dmg + .zip arm64 (plan §4, se registrará el resultado al cierre).",
      "AC-09": "MANUAL/orquestador: plutil sobre el Info.plist del bundle (config automatizada en tests/unit/packaging/hardening.test.ts::packaging hardening (SPEC-024)::MVP version and metadata::declares LSApplicationCategoryType in the extendInfo MAP keeping both TCC descriptions intact).",
      "AC-10": "MANUAL/orquestador: codesign --verify + entitlements embebidos (config automatizada en los tests de AC-01/AC-02 de tests/unit/packaging/hardening.test.ts).",
      "AC-11": "MANUAL/orquestador: humo del .app (ventana con la UI de Capturas — automatizada en unit por las suites de SPEC-020); prompts TCC como \"Maurya\": verificación humana final listada en MEMORY.",
      "ITER-1": "tests/unit/packaging/afterPack.test.ts::afterPack hook (SPEC-024-iter-1)::registers build/afterPack.js as the active afterPack hook in electron-builder.yml + tests/unit/packaging/afterPack.test.ts::afterPack hook (SPEC-024-iter-1)::ships build/afterPack.js as a loadable CommonJS module exporting a function + tests/unit/packaging/afterPack.test.ts::afterPack hook (SPEC-024-iter-1)::is a no-op outside darwin: returns without touching the context and never throws + MANUAL/orquestador (cobertura TRASLADADA, no perdida — fallback pactado): la orquestación darwin (ruta <appOutDir>/<productFilename>.app, firmar --force --deep --sign - --entitlements → verificar --verify --deep --strict, lanzar si falla) está VERIFICADA sobre el bundle auténtico el 2026-07-10 — hook ejecutado en build real, codesign --verify --deep --strict OK, Signature=adhoc, recursos sellados, entitlements embebidos = plist — evidencia más fuerte que el mock."
    },
    "notes": "Breakage presupuestado verificado: cero (la suite de SPEC-005 aserta la versión por regex semver, no literal, y no toca entitlements). Decisión documentada (a petición del orquestador): los ACs de configuración (AC-01..AC-05) se cubren con unit de Node leyendo los archivos del repo como fixtures — deterministas, estables y más valiosos que MANUAL porque fijan la config contra regresiones futuras (p. ej. reintroducir un entitlement del scaffold o convertir extendInfo en lista); la verificación del BUNDLE generado sigue siendo del orquestador (MANUAL, AC-08..AC-11). SPEC-024-iter-1 (fix 68b7226, originado en la verificación del bundle del orquestador: identity null hacía que electron-builder OMITIERA la firma — sello nominal): suite del hook afterPack añadida (3 tests estables tras aplicar el fallback pactado: el riesgo declarado se confirmó en el run 2026-07-10T16:34Z — vi.mock('child_process') no intercepta el require del CJS y los 2 tests del camino darwin ejecutaban codesign real → eliminados y su cobertura trasladada a MANUAL/orquestador con el comportamiento YA verificado sobre el bundle auténtico).",
    "created_at": "2026-07-10",
    "updated_at": "2026-07-10",
    "iteration_spec_file": "specs/SPEC-024-iter-1-empaquetado-hardening-final.md"
  }
}
