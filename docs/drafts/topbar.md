 No me gusta. Hay estilos mezclados sin criterior. Te pongo aquí unos diagramas para cada caso de la Topbar (donde [NOMBRE] = "Capturas" o "Discoveries" en función de en qué pantalla estemos) y lo peloteamos:

# 1. Permisos no concedidos, sin grabación

> [NOMBRE] | Micrófono (No concedido) Audio del sistema (No concedido) | (Abrir ajustes del sistema) | (Iniciar grabación) | (Buscar) (Modo)

Donde:
- (Abrir ajustes del sistema) tiene el estilo "danger" en rojo que tiene actualmente
- (Iniciar grabación) está disabled
- (Buscar) es el componente de búsqueda
- (Modo) es el botón para cambiar entre modo claro y modo oscuro

# 2. Permisos concedidos, sin grabación

> [NOMBRE] | Micrófono (Concedido) Audio del sistema (Concedido) | (Iniciar grabación) | (Buscar) (Modo)

Donde:
- (Iniciar grabación) está enabled
- (Buscar) es el componente de búsqueda
- (Modo) es el botón para cambiar entre modo claro y modo oscuro

# 3. Permisos no concedidos, con grabación

> [NOMBRE] | Micrófono (No concedido) Audio del sistema (No concedido) | (Abrir ajustes del sistema) | Grabada (Mostrar en Finder) (Nueva grabación) | (Buscar) (Modo)

Donde:
- (Abrir ajustes del sistema) tiene el estilo "danger" en rojo que tiene actualmente
- (Mostrar en Finder) es un botón sin texto, solo el icono
- (Iniciar grabación) es un boton sin texto, solo el icono
- (Iniciar grabación) está disabled
- (Buscar) es el componente de búsqueda
- (Modo) es el botón para cambiar entre modo claro y modo oscuro

# 4. Permisos concedidos, con grabación

> [NOMBRE] | Micrófono (Concedido) Audio del sistema (Concedido) | Grabada (Mostrar en Finder) (Nueva grabación) | (Buscar) (Modo)

Donde:
- (Abrir ajustes del sistema) tiene el estilo "danger" en rojo que tiene actualmente
- (Mostrar en Finder) es un botón sin texto, solo el icono
- (Iniciar grabación) es un boton sin texto, solo el icono
- (Buscar) es el componente de búsqueda
- (Modo) es el botón para cambiar entre modo claro y modo oscuro


