# MEMORY.md — memoria persistente del loop de desarrollo

> Vive fuera de la conversación. El agente lo lee al empezar cada vuelta y lo actualiza al cerrar cada spec.
> Tres secciones: PROBADO (qué se intentó y su resultado), VERIFICADO (hechos confirmados), ABIERTO (qué queda).

## Estado actual del loop
- Spec en curso: _ninguna_
- Decisión humana (2026-07-03): H0 se implementa como **proyecto Electron local** en este repo (electron-vite), NO en Lovable (no puede capturar audio de sistema). QA adaptado: Vitest local; e2e Playwright contra public link no aplica al spike.
- Última spec cerrada: **SPEC-012** (2026-07-04, unit 174/174 PASS; commit código 280bc16; templates de entrevista con editor de dos niveles, duplicar, fase; flaky SPEC-011 estabilizado). **H2 COMPLETO (9/9)**. Antes: SPEC-011 (empresas/contactos), SPEC-010 (discoveries), SPEC-009 (layout, H1 COMPLETO), SPEC-008..001.
- Lección QA (2026-07-04): dialogs abiertos desde DropdownMenu — el returnFocus del menú compite con onOpenAutoFocus del Dialog (FocusScope re-enfoca con select:true) → NO asertar selección ni foco síncrono; usar waitFor(toHaveFocus). Y: toasts de sonner son <li> → no contar listitems globales con toasts visibles.
- Lección QA (2026-07-04): con Dialog modal Radix abierto, el fondo queda aria-hidden → queries por rol del contenido de fondo necesitan {hidden:true}.
- Lección QA nueva (2026-07-04): jsdom + Radix Tooltip grace area (rects 0×0) → tras el primer unhover, isPointerInTransit queda anclado y ningún tooltip posterior abre en el mismo render. Regla: máx 1 hover de tooltip por it/render.
- H0 ítem 6 (go/no-go): BORRADOR con GO provisional en docs/spike-audio-go-no-go.md; la FIRMA final espera la sesión de validación del humano (transcripción en vivo, latencia real, diarización, 15 min). **Decisión del loop 2026-07-03: continuar con H1 "a riesgo"** — el riesgo mayor (captura) ya está verificado por humano y el trabajo de H1 es independiente/reversible.
- Última spec cerrada: **SPEC-014** (2026-07-04, unit 204/204 PASS; commit 077c24c; guión+objetivos con claude-opus-4-8, structured outputs, contexto histórico, edición). **H3 COMPLETO (5/5)** — generación real pendiente de que el humano configure su clave Anthropic en Ajustes. Antes: SPEC-013 (crear entrevista), H2 y H1 completos, spike H0 (5/6, go/no-go pendiente firma).
- @anthropic-ai/sdk 0.110.0 pinneado (solo main; electron-vite lo externaliza y builder lo empaqueta desde dependencies). Patrón LLM: llmService en main, key secrets→env, errores tipados kinds, structured outputs json_schema, persistir solo tras parseo válido.
- Última spec cerrada: **SPEC-015** (2026-07-04, unit 218/218 PASS; commit 5ec1fcd; grabación integrada en la entrevista, componentes extraídos a components/recording/, /capture intacta). H4 ítems 1-5 cerrados; **ítem 6 (sesión ≥60 min) manual pendiente del humano**.
- Última spec cerrada: **SPEC-016** (2026-07-04, unit 239/239 PASS; commit 60d57cf; asistente proactivo Mom Test con disparadores 3 líneas/20 s/45 s, panel de sugerencia única, objetivos en vivo, alarmas, 👍/👎 persistido). **H5 COMPLETO (8/8) — EL DIFERENCIADOR HECHO.**
- Última spec cerrada: **SPEC-017** (2026-07-04, unit 269/269 PASS a la primera; código 3f3b232; nota de resumen con claude-opus-4-8 según note-template, edición, Sheet de transcripción, export .md con save dialog). **H6 COMPLETO (5/5) — FLUJO END-TO-END CERRADO** (RF-NOTE-002/003 acreditados a SPEC-002/015/014). Observación: 1 run de vitest mostró "Errors 1" transitorio no reproducible (flaky de teardown, sin acción).
- Última spec cerrada: **SPEC-018** (2026-07-04, unit 290/290 PASS a la primera; código 4f9504f; búsqueda global ⌘K con cmdk 1.1.1 — única dep nueva —, db:search en main con normalización NFD, grupos con contexto, rutas ANIDADAS reales). H7 1/7. Lección QA nueva: en el command palette los nombres se repiten como contexto de otros grupos → scoping por [cmdk-group], nunca findByText global.
- Última spec cerrada: **SPEC-020** (2026-07-10, unit 335/335 PASS; spec 327e102, plan c8d3725, código e7aa33e, tests 13484b7+1d90135; rama moisesvilar/capture-first). **Capture-first**: sidebar "Capturas" (/captures listado global + /captures/:id detalle reutilizando Recording/Note/ScriptSection), Interview.discoveryId + companyId nullable (schema v2 con migración y backfill), asignación diferida de empresa/contacto (Sheet con creación inline, mutación compuesta atómica), guión/nota degradados sin empresa, search con hits sin empresa, spike /capture desenrutado (redirect). Origen: petición humana directa (no estaba en checklist → nada que marcar [x]). OJO: el slot "SPEC-020 coste IA" previsto en esta memoria quedó desplazado; coste IA será SPEC-021+.
- Lección QA (2026-07-10): **SPEC-019 se implementó sin generar tests ni entrada en spec-test-map.json** (gap del pipeline: su vuelta saltó los pasos 5-7). Consecuencia: el ConsentDialog rompió 23 tests de SPEC-015/016 y nadie lo vio hasta la vuelta de SPEC-020. Las suites ya están adaptadas (helper de arranque atraviesa y aserta el "Aviso de grabación"); la QA propia de SPEC-019 sigue PENDIENTE y su ítem H7 sin marcar.
- Lección QA (2026-07-10): el "Errors 1" transitorio de vitest (visto en SPEC-017) reapareció: unhandled `window is not defined` en teardown por el leak documentado de useAudioCapture (interval 100 ms sin limpiar, origina en SpikeAudioCapturePage.transcription.test.tsx) → exit 1 con 335/335 PASS. Tratado como flaky conocido: 1 re-ejecución → PASS. Si se repite, valorar corregir el leak (ya no es "aceptable en spike": bloquea el exit code del tester).
- **SPEC-019 cerrada de verdad** (2026-07-10): suite generada a posteriori (10/10 ACs, commit 6d00b23), tester PASS a la primera; ítem H7 consentimiento marcado [x]. Desviación menor documentada para el humano: la spec decía "Escape/click fuera = Cancelar" pero Radix AlertDialog ignora el click fuera por diseño (solo Escape/botones) — se acepta por ser más conservador para un aviso legal; iterar la spec solo si el humano lo pide.
- Última spec cerrada: **SPEC-021 (+iter-1)** (2026-07-10, unit 374/374 PASS con exit 0 limpio; spec fbb41f2, plan b8addc8, código d0fefa7, tests 35b4621, iter-1 4544efc+57fc14a). Coste de IA por entrevista: aiUsage (tokens+USD, tarifa $5/$25 MTok opus-4-8 en src/main/aiCost.ts), acumulación best-effort en guión/nota/asistente, límite configurable en Ajustes (singleton aiCostSettings sin bump de schema) que pausa SOLO el asistente ('paused'+Reanudar), coste en panel en vivo y cabeceras de detalle. iter-1: residuo IEEE-754 en roundUpUsd (1.1→1.11) — code-defect detectado por QA, fix quirúrgico.
- **Leak de useAudioCapture CORREGIDO** (2026-07-10, commit 60114e6, tarea técnica directa sin spec): cleanup de solo-desmontaje del interval de 100 ms. Motivo: el unhandled "window is not defined" tras teardown rompía el exit code del tester de forma semi-determinista (2 runs seguidos). Ya no aplica la nota de "aceptable en spike".
- Observaciones menores de QA en SPEC-021 sin accionar (no violan ACs): AiCostCard sin guard de doble submit; drift de céntimos por suma de deltas en estimatedCostUsd (cosmético); candidatas a hardening H7.
- Última spec cerrada: **SPEC-022 (+iter-1)** (2026-07-10, suite completa PASS; spec 66d4d7d, plan 4a6838d, código 8c06e5f, tests 496b989, iter-1 ce15f34+5596ae4+b35b0b5). Degradación sin diarización: fallback único sin diarize en retryOrGiveUp (solo sesión never-opened, auth excluido), evento status con degraded?:true condicionado a everOpened (iter-1: la rendición de un fallback que nunca abrió no marca modo degradado), Alert transcription-degraded-alert. iter-1 nació de un gap detectado por QA Dev en revisión (no por test rojo) — patrón útil: los gaps del QA son señal de defecto tan válida como un FAIL.
- **GO de H0 FIRMADO por el humano (2026-07-10)**: "El spike es un GO como una casa, ya lo usé en entrevistas reales." Registrado en docs/spike-audio-go-no-go.md; ítem H0 marcado [x] → **H0 completo (6/6)**. El uso en entrevistas reales acredita de facto las verificaciones humanas pendientes de SPEC-001/002 (captura acústica y transcripción con voz real en condiciones reales); siguen pendientes las cuantitativas: sesión ≥60 min medida (H4) y números de latencia (H7).
- **NFR §4.5 sesión ≥60 min VALIDADO (2026-07-10) → H4 completo (6/6).** Sesión real instrumentada por el agente: app dev lanzada con `npx electron-vite dev -- --remote-debugging-port=9222` y manejada por CDP (driver en scratchpad); grabación 16:35→17:36 sobre la captura "Sesión continua 60 min (NFR 4.5)". Evidencia: WAV spike-2026-07-10T14-35-16-174Z.wav = 234,7 MB = 61 min 07 s de PCM (aritmética exacta a 64 kB/s); monitor cada minuto: crecimiento constante 3.833.856-3.866.624 B/min SIN una sola parada; cronómetro continuo hasta 60:56; "Transcribiendo" activo en los 13 checkpoints; heap renderer oscilando plano 33→57→48 MB (sin fuga). Caveats: (1) la sesión terminó por SIGTERM externo (se detuvieron las tareas background del agente, no fallo de la app) → el stop ordenado tras 60 min no quedó ejercitado en ESTA sesión (sí en uso real y unit tests); header del WAV reparado a mano (RIFF/data sizes). (2) TCC "Audio del sistema" hubo que re-concederlo: el panel de macOS no lista Electron hasta que la app INTENTA capturar (getDisplayMedia forzado por CDP la registró); truco reutilizable.
- Última spec cerrada: **SPEC-023** (2026-07-10, suite PASS + re-medición real; spec 1b0db9d, plan e9747a8, código c805aa4, tests 2d2b576). Latencia del asistente optimizada: **mediana 3,70 s** (3,71/3,70/2,86) vs baseline 5,85 s (5,85/6,06/4,95) = −37%, con prompt caching (systemBlocks estables por sesión, cache_control ephemeral, objetivos sin estado en el prefijo) + salida acotada (maxLength 200/140 en schema, MAX_TOKENS 512). Verificado en vivo: la API acepta maxLength en json_schema (gap de QA despejado); acierto de caché confirmado por aritmética de coste ($0,0196 real vs $0,0288 a tarifa plena, −34%). aiCost tarifica caché (write $6,25 / read $0,50 por MTok); extractUsage de 4 campos. Protocolo de medición reproducible: app dev con --remote-debugging-port + listeners CDP en assistant:update + guion `say` de 10 frases (mismo guion baseline y re-medición).
- Última spec cerrada: **SPEC-025** (2026-07-10, unit 433/433 PASS; rama moisesvilar/highlight-goals: spec+checklist 9aefcf0, código 5026e69, tests 2549d1f, fix arnés c61adf0). **Objetivos destacados + evaluación post-grabación (H8 post-MVP, petición humana directa)**: sección "Objetivos" única entre cabecera y Grabación (asume el seguimiento en vivo; DEROGA el panel de SPEC-016 AC-09 UI/AC-10 y el bloque de objetivos en lectura de SPEC-014 AC-07 — ObjectivesPanel.tsx eliminado), evaluación LLM al detener (objectiveEvaluationService en main: structured outputs, fire-and-forget tras updateInterview en recording:stop, guards silenciosos sin clave/objetivos/líneas/límite SPEC-021, eventos llm:objective-evaluation), Interview.objectiveResults opcional (patrón aiUsage; el repositorio lo descarta si cambia objectives), botón "Evaluar objetivos" manual (reintento + backfill; sin guard de límite: intención explícita, patrón Reanudar), motivo ≤50 palabras bajo cada objetivo (cumplido CheckCircle2 verde / no cumplido Target muted, sin tachado por a11y+legibilidad). Lección QA: el arnés nuevo salió con findSection() sin renderDetail() → 14 FAIL con <body/> vacío; patrón trivial de detectar (defecto de test). MANUAL pendiente: humo real con clave (calidad de motivos), glue ipc verificado solo por lectura.
- Última spec cerrada antes: **SPEC-024 (+iter-1)** (2026-07-10, suite 407/407 PASS + bundle verificado; spec+plan 53e3a41, código cb5b9a1, tests 82dc538, iter-1 d4934e2+68b7226+f623ecb+1932656). Empaquetado 1.0.0: entitlements de firma real (audio-input+allow-jit, fuera los del scaffold), LSApplicationCategoryType, guard doble-submit AiCostCard. **Hallazgo clave de la fase 4**: `identity: null` hace que electron-builder NO firme nada (bundle solo linker-signed, entitlements sin embeber) → iter-1 añadió hook `build/afterPack.js` (codesign --force --deep --sign - + entitlements + verify, lanza si falla). Verificado sobre el bundle real: --verify --deep --strict OK, Signature=adhoc, Identifier=com.maurya.app, recursos sellados, entitlements embebidos, humo de arranque OK. Camino Developer ID/notarización documentado en el yml sin activar. Lección QA: vi.mock('child_process') NO intercepta el require de módulos CJS del repo bajo vite-node → camino darwin del hook cubierto como MANUAL con evidencia del build real (fallback pre-pactado, cobertura trasladada).
- **ESTADO DEL LOOP (2026-07-10): sin tareas automatizables restantes.** Todo lo pendiente del checklist requiere humano: H0 firma go/no-go (borrador GO en docs/spike-audio-go-no-go.md), H4 sesión ≥60 min, H7 latencia real (<3-5 s), H7 tests de usabilidad (Riesgo #2), H7 empaquetado/hardening FINAL (decisiones humanas: Developer ID/notarización; el build ad-hoc ya existe por SPEC-005), y las 4 tareas de Validación de producto. Escalado al humano en el resumen de sesión.
- Próxima tarea: **H7 restantes automatizables**: aviso de consentimiento de grabación (Must, pequeña — candidata SPEC-019) → medición/control de coste IA por entrevista (Must, SPEC-020) → degradación elegante sin diarización (Should) → empaquetado/hardening final (Must, última). Manuales/humano: optimización de latencia (sesión real), tests de usabilidad (Riesgo #2) — escalar al cierre de H7 junto con el resto de verificaciones humanas acumuladas.
- Verificación humana pendiente de SPEC-005: abrir dist/mac-arm64/Maurya.app (clic derecho→Abrir), prompts TCC como "Maurya", key en ~/Library/Application Support/Maurya/.env.local.
- Última spec cerrada: **SPEC-026** (2026-07-10, unit 429/429 PASS a la primera; código 4b5d3c8, tests 3983b2e; rama moisesvilar/custom-prompts). **Prompts de IA personalizables** (post-MVP, RF-CFG-001 añadido al PRD §3.8 con autorización humana — el checklist ganó sección Post-MVP): pestaña «Prompts personalizados» en Ajustes, editable SOLO el bloque persona/enfoque (reglas del JSON/límites/partes dinámicas bloqueadas y visibles en solo lectura), editor Markdown WYSIWYG (TipTap 2.27.2 + tiptap-markdown, botonera 5 acciones, fuente de verdad = Markdown plano), defaults en src/main/prompts/defaults.ts, overrides en db.json (entidad customPrompts sin bump de schema), resolución override→default por uso; el asistente resuelve SOLO en startAssistant (systemBlocks byte-estables, caching SPEC-023 intacto, verificado por test). Lecciones QA: TipTap corre REAL en jsdom con polyfills de Range/elementFromPoint (tests/helpers/prosemirrorJsdom.ts) — no mockear el editor; los cambios de doc en tests van por botonera (jsdom no implementa beforeinput); cuando DbApi crece hay que extender tests/helpers/mockApi.ts o TODA la suite deja de compilar. Manual no bloqueante: experiencia real de teclado/caret del editor en la app.
- Última spec cerrada: **SPEC-027** (2026-07-11, unit 428/428 PASS exit 0 limpio; spec+checklist 9f84dc2, código bd2232d, tests 3542a9e, iter tests 2fb23ac, stub jsdom 65ccae8; rama moisesvilar/wysiwyg-markdown; report tests/reports/SPEC-027-run-20260710T222400Z.md). **H8 completo (2/2, junto a SPEC-025 highlight-goals)**. Editor markdown WYSIWYG para guión y nota (TipTap v3 pinneado 3.27.3 + @tiptap/markdown oficial: `contentType: 'markdown'` al montar, `editor.getMarkdown()` al serializar; MarkdownEditor/MarkdownView en components/markdown/, estilos .markdown-content en main.css) + disposición NoteScriptSections (apilados condicionales; Tabs "Notas"/"Guión" con forceMount cuando coexisten). Patrón clave: el editor solo emite onChange en ediciones reales → el dirty-check compara contra el string persistido sin falsos positivos de normalización y el round-trip semántico es determinista. Origen: petición humana directa (H8 · Mejoras post-MVP creado en el checklist por indicación humana).
- Lecciones QA nuevas (2026-07-11, SPEC-027): (1) **ProseMirror bajo jsdom** exige stubs de `Range.getClientRects/getBoundingClientRect` en tests/setup.ts — sin ellos el run queda 428/428 PASS pero exit 1 por unhandled errors en selectionchange (coordsAtPos→scrollToSelection). (2) **Radix Tabs con forceMount NO pone el atributo hidden**: la ocultación del panel inactivo es la clase Tailwind data-[state=inactive]:hidden, que jsdom no aplica → ambos paneles coexisten para getByRole; scope por tabpanel data-state. (3) La escritura libre en TipTap no es reproducible en jsdom (sin beforeinput nativo): ejercitar el editor vía toolbar (API TipTap, cambios de doc deterministas) y atajos que PM implementa como keydown handlers (Ctrl+A, Backspace). (4) El serializador puede dejar whitespace final en docs de un solo bloque: asertar el markdown semántico (trim), no igualdad exacta, salvo payloads multi-bloque.
- Pendiente humano (SPEC-027): verificación manual de escritura libre en el editor (teclear en guión y nota, alternar pestañas, guardar) — jsdom no la cubre. Desviación flaggeada aceptable: en el apilado "con guión y sin nota" el Guión va ARRIBA de los controles de nota (orden literal del wireframe; antes la Nota iba primera) — iterar la spec solo si el humano lo pide.
- **Conciliación de worktrees (2026-07-11, rama wysiwyg-markdown → main)**: tres specs post-MVP aterrizaron en paralelo colisionando en numeración — highlight-goals retuvo SPEC-025, custom-prompts renumeró a SPEC-026 (f08659b) y wysiwyg-markdown renumeró a SPEC-027. Convergencia de TipTap: custom-prompts traía v2 (2.27.2 + tiptap-markdown) y wysiwyg v3 (3.27.3 + @tiptap/markdown oficial) — se unificó en v3 migrando PromptMarkdownEditor (`contentType: 'markdown'` + `editor.getMarkdown()`; fuera tiptap-markdown). Merges semánticos: ScriptSection (lectura = MarkdownView SIN bloque de objetivos, que viven en ObjectivesSection), InterviewDetailPage (ObjectivesSection + NoteScriptSections), main.css (.markdown-content + .prompt-editor), spec-test-map (SPEC-014 AC-07 fusionado), checklist H8 2/2. Los stubs de geometría de tests/setup.ts conviven con tests/helpers/prosemirrorJsdom.ts de SPEC-026.

- Última spec cerrada: **SPEC-028** (2026-07-11, unit 507/507 PASS; rama moisesvilar/ux-improvements: spec 9464d19, plan b9365cd, código 7bc68dc, tests f2017fa). **H9 ítem 1 (marcar/desmarcar objetivos con comentario)**: campo `Interview.objectiveOverrides` (array por índice, entrada null = sin marca; solo lo escribe main; la invariante de updateInterview lo descarta junto a objectiveResults cuando cambian los objectives), canal `llm:override-objective` + objectiveOverrideService (clon del patrón evaluación: structured output {text}≤400, coste vía recordInterviewUsage sin guard de límite por ser manual, persistencia atómica marca+texto en un mutate, degradación sin clave → text=comment sin llamada), UI: lápiz ghost por objetivo → Dialog RadioGroup+Textarea (patrón InterviewFormDialog), precedencia manual > evaluación final > vivo, reason previa con line-through + data-overridden, componentes ui nuevos label.tsx y radio-group.tsx (paquete unificado radix-ui, sin dep nueva). QA a la primera (0 hallazgos). MANUAL pendiente: glue IPC verificado solo por lectura, calidad real de la reescritura con clave.
- Lección infra (2026-07-11, vuelta SPEC-028): en un worktree, `node_modules/electron` puede quedar SIN el binario (postinstall no corrido) → las 2 suites spike que importan electron real fallan a nivel de FICHERO con "Electron failed to install correctly" aunque todos los tests pasen (500/500 y 2 files failed). Arreglo: `node node_modules/electron/install.js` y re-ejecutar (NO es defecto de test ni de código; no iterar la spec por esto).

- Última spec cerrada: **SPEC-029** (2026-07-12, unit 510/510 PASS; rama moisesvilar/ux-improvements: spec 6dae9b5, plan dd94b43, código 9fbd08c, tests 6682ec0, fix flaky cd933c7). **H9 ítem 2 (edición markdown por defecto)**: Nota y Guión sin modo lectura — MarkdownEditor siempre montado; patrón «draft null = prístino» (dirty = draft !== null && draft !== persistido; en Guión + objectivesDraft null-prístino con displayedObjectives = draft ?? persistido) + editorResetKey contador que SOLO se incrementa en descarte confirmado y regeneración (nunca tras Guardar → caret intacto; tras guardar Guión sí setObjectivesDraft(null) por el filtrado de vacíos); Descartar siempre con AlertDialog; Regenerar unificado (outline, RefreshCw, testids note/script-regenerate-button, Tooltip en disabled). Deroga ACs de modo lectura de SPEC-014/017/027 (anotado por AC en spec-test-map). MarkdownView queda sin consumidores de producción (no borrado). MANUAL pendiente: caret real tras guardar, responsive/sticky, posición visual de los Regenerar.
- Lecciones QA (2026-07-12, SPEC-029): (1) botón disabled envuelto en TooltipTrigger se REMONTA al habilitarse (getStatus async) → un click sobre la referencia vieja se pierde en silencio; siempre waitFor(toBeEnabled()) por testid y re-consultar el elemento antes del click. (2) El heading "Guión" es asíncrono desde SPEC-027 (Skeleton de NoteScriptSections hasta resolver getNoteByInterview) → nunca getByRole síncrono sobre headings Guión/Nota; usar findByRole (flaky real visto en el run oficial: pasa en aislamiento, falla bajo carga).

- Última spec cerrada: **SPEC-030** (2026-07-12, unit 511/511 PASS a la primera; spec+plan bd6cb1e, código b49d3a8, tests 68b6100). **H9 ítem 3 (Grabación al final)**: RecordingSection movida al final en InterviewDetailPage Y CaptureDetailPage (decisión asumida: aplica a ambas por consistencia; el orden queda cabecera → Objetivos → Nota/Guión → Grabación). Plan autorado por el orquestador (cambio trivial, 2 ficheros). Deroga la posición "entre Objetivos y Nota/Guión" de SPEC-015/020/025 solo en lo posicional. Un único test asertaba el orden viejo (SPEC-020 AC-15) y se adaptó citando SPEC-030.

- Última spec cerrada: **SPEC-031** (2026-07-12, unit 522/522 PASS; spec 69ecbbc, plan d001d77, código 4a98236, tests 0cc5070). **H9 ítem 4 (prompts en acordeón + anti-inyección)**: CustomPromptItem con colapso controlado por ítem (desviación D2 aceptada: sin primitivo Radix Accordion — el contrato es el patrón; lápiz = trigger con aria-expanded, expansión múltiple, Guardar/Descartar solo con cambios patrón SPEC-029, AlertDialog con destino 'discard'|'collapse'), CustomPromptSheet.tsx ELIMINADO, sin sección Reglas fijas. Main: buildPersonaBlock(id) en src/main/prompts/index.ts = PERSONA_SAFEGUARD (constante estática) + persona delimitada entre PERSONA_BLOCK_START/END; los 3 servicios lo usan en vez de resolvePromptPersona directo; fase (guión) y contexto (nota) despegados FUERA de los delimitadores; byte-estabilidad del asistente intacta (solo se compone en startAssistant). Mitigación instruccional, sin filtrado local. Lección QA: la salvaguarda CITA los delimitadores entre comillas → indexOf plano da falso positivo al asertar posiciones; asertar por línea exacta. MANUAL pendiente: eficacia real de la salvaguarda con clave (humo), UX del editor in-place.

- Última spec cerrada: **SPEC-032** (2026-07-12, unit 528/528 PASS a la primera; spec+plan fc8c621, código 6fd84ac, tests 3d76335). **H9 ítem 5 (nombre por defecto de captura)**: título opcional SOLO al crear — vacío/blanco → `defaultCaptureTitle(new Date())` = «Captura dd-mmmm-yyyy hh:mm» (padStart en dd/hh/mm, mes es-ES minúsculas, 24h); placeholder con el mismo formateador; error inline del Título eliminado del Dialog de creación (Discovery sigue requerido; Editar sigue exigiendo título). Deroga los ACs de título requerido de SPEC-020. Lección QA: fake timers restringidos a Date (`vi.useFakeTimers({ toFake: ['Date'] })`) conviven con user-event sin advanceTimers.

- Última spec cerrada: **SPEC-033** (2026-07-12, unit 541/541 PASS; spec a4bffdd, plan 2ecabaa, código 1e66b28, tests 25f6b77). **H9 ítem 6 (autogeneración del guión al crear captura)**: scriptAutoGenerationService en main (clon del patrón SPEC-025: fire-and-forget síncrono, guards silenciosos in-flight/inexistente/sin-template/con-guión/sin-clave, eventos llm:script-generation a todas las ventanas, delegación íntegra en generateInterviewScript — coste y contexto histórico gratis), disparo en CapturesPage.handleCreate antes de navigate, ScriptSection con autoGenerating por eventos (done → onInterviewUpdatedRef sin tocar drafts/resetKey; error → toast con message). Desviación implementador aceptada: el indicador del empty state usa autoGenerating (no isGenerating) para no duplicar el botón en la manual. Carrera generating/mount aceptada y documentada (ventana de ms sin spinner; jamás spinner colgado; iteración futura sería canal pull de estado). Solo capturas (flujo empresa intocado, test negativo explícito).

- Última spec cerrada: **SPEC-034** (2026-07-12, unit 554/554 PASS a la primera; spec ee89c3b, plan c56e00f, código e6bf325, tests 40c156e). **H9 ítem 7 (controles de grabación reubicados en la Captura)**: refactor grande sin regresión — useRecordingController extraído LITERAL de RecordingSection (hooks de audio/permisos/consent/close-guard/auto-stop en un solo punto), RecordingSection dividida en router-por-prop/SelfControlled/View (variant capture omite el bloque Preparación; PROHIBIDO hook-y-descarte: duplicaría devicechange/onCloseRequested/TCC), TopBarSlotContext+TopBarPortal null-safe (primer contexto/portal del renderer; callback ref en Layout; display:contents para el wrap mobile), CaptureTopBarControls (badges+MicSelect compact) portalados solo en estado Preparación, capture-start-button en cabecera junto a Asignar empresa. InterviewDetailPage sin tocar. Los tests existentes NO rompieron (541 verdes durante el refactor). MANUAL: layout visual compacto/wrap mobile.

## PROBADO
<!-- [SPEC-NNN iter-M] qué se intentó → resultado (PASS/FAIL + causa). Un renglón por intento. -->
- [SPEC-001] Implementación Electron 38.8.6 + loopback por flags (CATap) → código completo, typecheck+lint 0 errores, smoke test OK (commit 553fbc9). Fase 0 acústica NO ejecutable por el agente: TCC micrófono=not-determined, screen=denied.
- [SPEC-001] `shadcn init` → timeouts de red; `lib/utils.ts` y `components.json` escritos a mano (los 8 componentes ui sí por CLI).
- [SPEC-001 QA iter-1] vitest run → 6/11 FAIL (todos: jsdom sin `navigator.mediaDevices`, usado por useAudioDevices:39). Verificador independiente CONFIRMÓ: defecto de test-infra (stub ausente en tests/setup.ts), no de implementación. Corrección en curso vía QA Dev. Report: tests/reports/SPEC-001-iter-20260703-220418.md

- [SPEC-002] Implementación WS nativo (sin @deepgram/sdk) → commit fcfb08b, typecheck 0, lint 0 en src/, smoke OK. Fase 0 contra Deepgram real: auth por subprotocolo OK, interim/final por canal OK, 401 solo clasificable vía fetch /v1/auth/token, flush CloseStream ~2 s.
- [SPEC-002] Rotura esperada de tests SPEC-001 por cambio de contrato (StopResult, api.transcription en MauryaApi): 3 errores de tipos en tests/ + 3 lint no-empty-function preexistentes en tests/setup.ts → para QA Dev.

## VERIFICADO
<!-- Hechos confirmados por ejecución, no suposiciones. Ej: "SPEC-001 e2e verde en Lovable el <fecha>". -->
- El entorno del agente hereda `ELECTRON_RUN_AS_NODE=1`: Electron corre como Node y la ventana no abre. Ejecutar con `env -u ELECTRON_RUN_AS_NODE npm run dev`.
- `setDisplayMediaRequestHandler` + flags `MacLoopbackAudioForScreenShare`/`MacCatapSystemAudioLoopbackCapture` se aceptan sin crash en Electron 38.8.6 / macOS 26.5.1 (pipeline técnico OK; señal acústica sin verificar).
- Repo no tenía .git: inicializado 2026-07-03 (main + pipeline/SPEC-001). Sin remote configurado → sin push.

## ABIERTO
<!-- Qué queda por intentar, dudas técnicas, specs bloqueadas escaladas a humano. -->
- **SPEC-001 verificada por el humano (2026-07-03): "Todo OK"** — permisos TCC concedidos, medidores independientes por fuente, WAV 2ch/16kHz/16-bit con ambas fuentes audibles (AC-01/02/03/06/07/08 físicos cerrados). Pendiente opcional: sesión 15 min (AC-16). El loopback CATap por flags FUNCIONA en esta máquina → señal fuerte de GO para H0.
- API key de Deepgram disponible en `.env.local` (`DEEPGRAM_API_KEY`, gitignored).
- Lanzar la app: `./start.sh` (creado 2026-07-03).
- **Verificación humana pendiente de SPEC-002**: sesión con voz real (interims/finales por fuente en pantalla), transcript.json junto al WAV, key inválida en runtime, pérdida de conexión + reintento, autoscroll. Cómo: `./start.sh`.
- Confirmar equipo real (el roadmap del PRD asume 1 fullstack full-time).
- Leak menor detectado por QA (no corregido, aceptable en spike): useAudioCapture no limpia el interval de 100 ms si se desmonta con captura activa.
